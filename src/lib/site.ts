/* 사이트 대표 주소 — 검색·공유(OG)·sitemap 이 전부 이 값을 쓴다.
 *
 * ⚠️ 도메인 이전(2026-08) 전부터 일부러 fantastrick.co.kr 로 둔다.
 *   지금 임시 주소(workers.dev)로 두면 검색엔진이 임시 주소를 원본으로 배우고,
 *   이전 후에 "같은 내용이 두 주소에 있다"며 새 주소를 복제본 취급한다.
 *   미리 대표 주소를 선언해 두면 임시 주소로 들어온 검색엔진도
 *   "원본은 fantastrick.co.kr" 로 읽는다 — 이전하는 날 아무것도 안 바꿔도 된다. */
export const SITE_URL = "https://fantastrick.co.kr";
