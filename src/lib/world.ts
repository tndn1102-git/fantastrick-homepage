/* 세계관 페이지의 "숨은 주소" 열쇠.
   ─────────────────────────────────────────────────────────────
   이 페이지는 홈페이지 메뉴에도, 검색에도, sitemap 에도 없다.
   **알림톡에 적힌 주소를 누른 사람만** 들어온다.

   주소 모양:  https://fantastrick.co.kr/w/<열쇠>

   ⚠️ 알아두실 것 — 이건 자물쇠가 아니라 '아무도 모르는 주소'다.
      링크를 받은 손님이 친구에게 전달하면 그 친구도 열린다.
      막아야 할 만큼 문제가 되면, 아래 열쇠를 새로 만들고 알림톡 주소만 바꾸면
      **옛 주소는 그 순간 404 가 된다**(퍼진 링크가 전부 죽는다).

   열쇠 새로 만드는 법:
      node -e "console.log('abel-'+require('crypto').randomBytes(5).toString('hex'))"

   ⚠️ 옛 열쇠를 지우기 전에, 그 주소가 적힌 알림톡을 이미 받은 손님이 있는지 생각할 것.
      한동안은 새 열쇠와 옛 열쇠를 **둘 다 남겨두면** 옛 손님 링크도 계속 살아 있다. */

export const WORLD_KEYS = [
  // 사자의 서 — 아벨 연구소 자료실 (2026-08-22 발급)
  "abel-eb2f565a19",
] as const;

// 알림톡·안내에 붙일 주소를 만들 때 쓴다.
export const WORLD_PATH = (key: string) => `/w/${key}`;
