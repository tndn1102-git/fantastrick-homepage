# 판타스트릭 홈페이지 — 변경 기록 (README)

> 무엇을 바꿨는지 시간 순으로 적는 곳이에요. (최신이 위)


## 2026-08-22 — 세계관 페이지 `/universe` 추가 (사자의 서)

인스타그램 세계관 계정 **@abellaboratory**(아벨 연구소, 게시물 7건 · 2022-02~2024)의 내용을
홈페이지 안으로 옮겼다. 캐러셀 슬라이드까지 전부 읽어 원문 문장을 그대로 썼다.

**구성** — 히어로(자료실 입구) → FILE 01 ADAM Inc. → 02 성물 → 03 사자의 서 발견(신문 지면) →
04 레드크라운 → 05 LET'S GET 캠페인 → NOTE 자료가 말하지 않는 것 → 원본 게시물 7건 → 예약 CTA.

**카피 규칙(고칠 때 지킬 것)**
- 여기 적힌 사실은 **전부 게시물 원문**이다. 없는 설정을 지어내지 않는다.
- 마지막 'NOTE' 절만 해석이다. 새 사실을 더하지 않고 **이미 나온 문장끼리 부딪히게만** 한다.
- **스포일러 금지** — 테마 진행·장치·정답은 쓰지 않는다. "들어가기 전 배경"까지.
- 가상 창작물 고지(`.uv-disc`)는 계정이 매 게시물에 붙이던 약속이다. 지우지 말 것.

**들어가는 길** — `/rooms/bookofduat` 에 안내 카드 추가 + `sitemap.ts` 등록.
헤더 메뉴에는 넣지 않았다(이미 7개라 빽빽함) — 원하면 넣을 수 있다.

**호스팅** — 새로 정할 게 없다. 이 홈페이지가 이미 Cloudflare Workers 위에 있고
`fantastrick.co.kr` DNS 도 CF 다. 정적 페이지 한 장이라 요청·용량 부담도 없다.
(Vercel Hobby 는 계정 전체 월 100만 요청이 한도이고 2026-07 에 실제로 찼던 적이 있다.
CF Workers 무료는 하루 10만 요청 = 월 약 300만.)

**검수** — `npm run build` 통과, PC(1440)·모바일(390) 실제 크롬 캡처 확인, 가로 넘침 0, 콘솔 에러 0.
## 2026-08-20 (저녁) — 실제 처리량 숫자 + 견적 버튼

캡처 바로 아래에 붙였다. 가격 비공개인 곳 6곳이 **예외 없이** 화면 옆에 견적 버튼을 두고 있었다.

| 숫자 | 값 | 근거 |
|---|---|---|
| 새 홈페이지로 받은 예약 | 157건 | `source='online'` |
| 예약금 입금 자동확인 | 97% | 109건 중 106건(`paid_source`) |
| 미입금 30분 자동취소 | 7건 | `auto_cancelled` |

⚠️ **옛 사이트에서 옮겨온 392건은 뺐다.** 합치면 551건이 되는데 그건 우리 프로그램이 처리한 게 아니다.
⚠️ **기준일(8/13~8/20)을 반드시 붙였다.** 날짜 없는 숫자는 시간이 지나면 거짓말이 된다
   (조사에서 도도포인트가 2022년 수치를 그대로 두고 있었다).

**갱신 방법** — 자동 계산 대신 수동으로 두기로 했다(사장님 선택). 페이지가 열릴 때마다 DB 를 세면
요청 수를 계속 먹는데, 이 페이지는 방문이 드물어 낭비다. 대신 한 줄로 끝나게 만들어 뒀다:

    node scripts/business-proof-numbers.mjs --apply     ← 한 달에 한 번

## 2026-08-20 — 비즈니스 페이지: **도해를 실제 운영 화면으로 교체**

사장님 지시: "추측대로 아무거나 넣지 말고, 실제로 파는 회사들이 어떻게 하는지 다 분석하고 그 방식 그대로 입혀라."

**조사** — 에이전트 2팀이 국내외 **22개사 제품 페이지를 직접 열어** 전수 확인(2026-08-20).
| 확인한 것 | 결과 |
|---|---|
| 기능 설명부에 실제 캡처 | **9/11**. 도해로 대체하는 곳 **0곳** |
| 도해가 사는 자리 | 구조도·"왜 우리인가"·업종 목록 **11/11** |
| 장수 | 소상공인 대상은 5~8장. 10장 넘는 곳 없음 |
| 첫 화면 | **근무표** (근태 파는 6곳 전부) |
| 프레임 | 브라우저 주소창 목업 확인된 곳 없음. 폰만 단순 목업 |
| 캡션 | 이미지 위 소제목 + 아래 한 줄, 이득 서술 (8곳 중 6곳) |
| 개인정보 | **더미 데이터가 표준. 블러 쓴 곳 0곳** |
| 강조 장치 | 화살표·말풍선 **0곳** |
| 가격 비공개 | 6/11. 단 **전원이 견적 CTA를 화면 옆에 붙임** |

⭐ **"직접 운영하며 만들었다"고 말하는 곳 = 22곳 중 0곳.**
규모도 대기업 로고도 없는 우리가 이길 수 있는 유일한 자리 → 캡처마다 증거 한 줄을 붙였다.

**바꾼 것**
- 근무표 도해(figure.swmock) → **실제 캡처 5장**(SHOTS 배열, 좌우 교차)
- `그림으로 옮긴 화면` 태그 제거 — B2B 에서는 "아직 없는 물건" 신호로 읽힌다
- 남은 도해 2개(입금 순서·힌트폰)는 **"처리 순서"** 로 이름만 바꿔 유지(구조 설명은 도해가 맞다)
- `business.css` 에 .shots/.shot 규칙 추가 — 주소창 목업 없음, 기울임·겹치기 없음

**개인정보 처리 (블러 안 씀)**
- 직원 실명 12명 → 가명, **로그인 아이디 20개** → staff01~ (`sujin1`·`hkminwoo3` 처럼 아이디에도 실명이 박혀 있었다)
- 전화번호 → 010-0000-0000 · 쿠폰 관리자 화면은 원래 개인정보 0건
- ⚠️ **DB 는 건드리지 않았다.** 브라우저에 그려진 글자만 바꿔 찍었다. 기록을 만드는 버튼(출근·퇴근 찍기, 일괄발행, 초기화)은 하나도 안 눌렀다.
- 우리 매장 이름(FANTASTRICK)은 **일부러 남겼다** — 그게 곧 증거다(사장님 확인)

**이미지**: `public/images/business/shot-*.webp` 5장, 각 34~49KB. 다시 만들려면 `scripts/prep-business-shots.mjs`

## 2026-08-19 (밤) — **"그쪽이 우리 홈페이지를 건드릴 수 있나" 안전 점검**

사장님 질문. 말로 답하지 않고 **실제로 공격하듯 시도해서** 확인했다. 결론: 못 한다.

| 무엇을 시도 | 결과 |
|---|---|
| 공개 API 에 POST·PUT·PATCH·DELETE | 전부 **405** (읽기 전용) |
| 옛 창구에 booked_save_appointment · booked_cancel_appt 등 | 200 이지만 **읽기 페이지만** 돌려줌 |
| ↳ **실제로 예약이 생겼나** (비어 있던 8/19 12:00 로 저장 시도) | 시도 전후 가능 시간 동일 = **아무것도 안 생김** |
| /api/admin/reservations · /api/admin/settings | **401** (로그인 필요) |
| 로그인 없이 연 /admin | 화면 껍데기만, 예약 자료 0건 |
| /wp-login.php · /wp-admin/ · /wp-json/… · /wp-content/… | 전부 **403** (열어준 건 admin-ajax.php 하나뿐) |
| 공개 API 응답에 손님 정보 | 이름·연락처·이메일 **없음**. 담긴 건 date·slots·open·closed 뿐 |

⚠️ 검증 방법에서 한 번 헛짚었다 — 처음엔 8/21 13:00 으로 시험했는데 그건 **원래 마감이던 시간**이라
   "사라졌다"는 잘못된 경보가 났다. **비어 있는 시간을 골라 시도 전후를 비교**해야 의미가 있다.
   앞으로 이런 검증은 반드시 "지금 열려 있는 값"으로 할 것.

📌 매장 대표번호(010-5536-0483)는 API 에 들어간다. 이미 테마 페이지·빠방 자료에 공개된 번호라
   새로 노출되는 건 없다(사장님 확인 후 그대로 두기로 함).

## 2026-08-19 (저녁) — 빠방이 "API 주시면 빨리 붙이겠다" → **정식 API 신설 + 구조 정리**

빠방에서 "API 등을 제공해주신다면 더 빨리 적용이 가능합니다" 라는 답이 왔다.
그래서 땜빵에 매달리는 대신 **정식 창구를 만들어 넘긴다.**

**정리한 것 (넘기기 전에 구조부터)**
하루 사이 같은 계산(예약 가능 시간)이 세 군데로 흩어져 있었다 —
`/api/slots` 안, 땜빵 안, 새 API 안. 흩어지면 한 곳만 고치고 잊는 순간
**손님 화면과 밖에 알린 값이 어긋난다.**
→ `src/lib/availability.ts` 하나로 모았다. 나머지는 모양만 입힌다.

**새로 만든 것**
| 무엇 | 어디 |
|---|---|
| 예약 가능 시간 계산 (유일한 출처) | `src/lib/availability.ts` |
| 공개 API | `/api/availability` — days·theme 인자, CORS 허용, 60초 캐시 |
| 설명 페이지 (그쪽에 이 주소만 보내면 됨) | `/reservation-api` |
| 보낼 안내문 | `docs/빠방-API-안내문.txt` |

응답에 **`legacyCalendarId`(17·23·24)** 를 같이 넣었다 — 그쪽이 이미 그 번호로 방을
구분해 두었으므로 표를 새로 만들 필요 없이 그대로 이어붙일 수 있다.

**바깥과의 약속**: 칸을 더하는 건 언제든 OK. **있던 칸을 없애거나 이름을 바꾸지 않는다.**
바꿔야 하면 `/api/availability/v2` 를 새로 판다.

**땜빵은 아직 켜둔다** — 그쪽이 옮기기 전에 끄면 노출이 다시 0이 된다.
없애는 조건 세 가지는 `src/lib/booked-compat.ts` 맨 위에 적어뒀다.

⚠️ 이날 middleware 에서 http 처리를 넣으며 **로컬 점검이 통째로 막히는 버그**를 만들었다.
   (localhost 에서도 Next 가 x-forwarded-proto 를 http 로 붙인다 → https://localhost 로 튕김)
   → **진짜 도메인일 때만** 넘기도록 고쳤다. 프록시 헤더만 믿으면 안 된다.

## 2026-08-19 (오후) — 빠방이 안 고쳐줘서 **우리가 옛 플러그인인 척** 하기로

오전에 응답에 `open`(남은 자리)을 넣어 배포했는데도 그쪽 목록은 여전히 비었다.
사장님이 지난주 목요일에 연락했고 "알았다" 는 답을 받았지만 안 고쳐줬다.
→ **그쪽 손을 안 빌리고 우리 쪽에서 끝낸다.**

**알아낸 것**
- 빠방은 **매장마다 수집 코드를 따로 짜서** 쓴다. 시간이 채워진 매장 134곳의 예약 주소가 32가지로 전부 다른 시스템이었다.
- 우리 몫 코드는 **옛 워드프레스 + Booked 플러그인**을 읽도록 짜여 있다.
  아직도 하루 5,884번 `/wp-admin/admin-ajax.php` 를 두드린다(referer = 그쪽에 등록된 `http://fantastrick.co.kr/booking/`).
- 홈페이지를 새로 만들며 그 플러그인이 사라지자 수집이 통째로 멈춘 것.

**규격을 어디서 얻었나** (추측 아님, 실물 확보)
- 인터넷 기록보관소에 남은 2022-01-17 자 `/booking/` 페이지 → 달력 표 모양·`data-calendar-id="17"`
- 같은 곳의 플러그인 스크립트 `booked/assets/js/functions.js?ver=2.2.6` → 주고받는 명령 이름 전부

**만든 것**
| 무엇 | 어디 |
|---|---|
| Booked 모양으로 예약 현황을 그리는 도구 | `src/lib/booked-compat.ts` |
| 옛 창구 되살리기 (`booked_calendar_date`·`booked_calendar_month`) | `src/app/wp-admin/admin-ajax.php/route.ts` |
| `/booking/` — 손님은 예약화면, 수집기는 옛 모양 페이지 | `src/app/booking/route.ts` |
| 방화벽에서 이 주소만 통과 | `scripts/waf-ajax.mjs allow` |

- 값은 **우리 예약 화면과 같은 함수**로 계산한다. 로컬 대조 15칸 전부 일치.
- 마감된 시간은 아예 안 그린다 → 상대가 어떻게 읽든 "그려진 것 = 예약 가능".
- ⚠️ 달력 번호 17·18·19 ↔ 테마 짝은 **17만 확실**(보관본에 있던 번호). 그쪽이 옛 번호를 들고
  있다면 다를 수 있다 — 들어오는 `calendar_id` 를 보고 `CALENDARS` 만 고치면 된다.
- ⚠️ 이 주소를 열면 하루 약 5,900건이 요청 한도(10만)에 잡힌다. 6% 쯤.

## 2026-08-19 — 외부 예약 프로그램(빠방)에 우리 방이 안 뜨던 **진짜 원인**

그쪽(빠방) 목록에 우리 예약 정보가 계속 안 떠서, 그쪽 시스템을 직접 들여다봤다.

**찾은 것** — 그쪽 검색 자료에 우리 매장은 **멀쩡히 등록돼 있다**(3곳, 영업중 표시도 정상).
그런데 **"예약 가능 시간" 칸이 오늘부터 7일치까지 전부 비어 있었다.**
그쪽은 남은 시간이 있는 방만 목록에 띄우기 때문에, 비어 있으면 아예 안 보인다.
(대조 확인: 같은 자료 안의 다른 방 200개 중 69개는 시간이 채워져 있었다 → 그쪽 전체 고장이 아니라 우리만의 문제)

**왜 비었나** — 우리 `/api/slots` 는 오랫동안 **"막힌 시간"만** 알려줬다.
"남은 시간"을 만들려면 우리가 몇 시에 운영하는지를 알아야 하는데 그 값이 응답에 없었다.
옛 워드프레스 사이트는 남은 시간을 그대로 줬으니, 홈페이지를 바꾸면서 그 칸이 빈 것이다.

**고친 것** — 테마별 조회 응답에 두 칸을 더했다.
- `slots` = 그날 회차 전부 · `open` = 그중 아직 안 막힌 것 (뺄셈을 우리가 해서 준다)
- 값은 우리 예약 화면과 **같은 함수**로 계산하므로 한 글자도 어긋날 수 없다
- 겸사겸사 DB 질문 3개를 한 번에 묶어 왕복을 줄였다 (전보다 느려지지 않는다)

⚠️ 이제 그쪽이 다시 긁어가면 채워진다. 반영은 그쪽 긁는 주기에 달렸다.

**그쪽 등록 자료는 전부 맞다** (2026-08-19 사장님 확인 — 처음엔 잘못 짚었다)
| 빠방 등록 | 우리 data.ts |
|---|---|
| 판타스트릭 → 태초의 신부 | `firstfoundbride` · store `s1`(1호점) |
| 판타스트릭 강남 2호점 → 사자의 서 | `bookofduat` · store `s2`(2호점) |
| 판타스트릭 TGC → LOCKDOWN CITY | `ldc` · store `s3`(TGC) |

'시간의 영속성'이 그쪽에 없는 것도 **맞다** — 등록하지 않은 테마다.
→ 그쪽에 고쳐달라고 할 것은 **없다.** 남은 문제는 위 "빈 시간" 하나뿐이었다.

## 2026-08-16 (밤) — 요청 한도 대응 ②: **옛 워드프레스 주소를 관문에서 차단**

밖에서 `/wp-admin/admin-ajax.php` 를 23초마다 두드리던 것(하루 약 4,200건)을 방화벽에서 끊었다.
`middleware.ts` 도 404 로 끊고 있었지만 그건 **워커가 깨어난 뒤**라 요청 수는 그대로 들었다.

**[막은 주소]** `/wp-admin` `/wp-login` `/wp-content` `/wp-includes` `/wp-json` `/wordpress` `/xmlrpc.php`
(= `middleware.ts` 의 `WP_PROBE` 와 같은 목록. 우리 홈페이지엔 하나도 없는 주소들)

**[정체]** IP `125.183.203.70`(LG유플러스 일반회선) 에서 도는 **자바 프로그램**(`Java-http-client/17`).
브라우저 아님 · referer 없음 · **다른 워드프레스 주소는 안 훑는다**(= 공격 스캐너가 아니라
"옛 사이트에 붙어 있다가 사이트가 바뀐 줄 모르고 계속 도는 연동 프로그램" 쪽).
8/13 도메인 이전 이후 계속 404 만 받고 있었으므로 **막아도 그쪽이 잃는 게 없다.**

**[확인]** 규칙 후 `/wp-admin` 등 7개 주소 전부 **403**, 손님 주소(`/` `/reserve` `/reservation`
`/events` `/api/config` `/images/*.webp`)는 전부 **200**.
`wrangler tail` 140초 녹화 → **워커에 도달한 워드프레스 요청 0건**(전엔 같은 시간에 5건쯤).
같은 녹화에서 **수집기는 하루 73,329건 그대로 받아가는 중** — 영향 없음(의도대로).

⚠️ **코드로는 못 걸었다. 대시보드에서 손으로 만들었다.**
- `Firewall Services:Edit` 를 토큰에 넣으면 **옛 방화벽 API(`/firewall/rules`) 는 읽기만** 열린다.
  쓰기는 `firewallrules.api.maintenance_mode` 로 **클라우드플레어가 잠갔다.**
- 새 Rulesets API(`/rulesets`) 는 **또 다른 권한**을 요구하는데, `/user/tokens/permission_groups`
  조회마저 막혀 있어 정확한 권한 이름을 확인할 수 없었다. → 추측으로 또 부탁하지 않고 대시보드로 갔다.
- 규칙 확인·수정이 필요하면 **대시보드 Security → WAF → Custom rules** 에서 한다.
  `scripts/waf-block-wp.mjs` 는 **지금 걸린 규칙 보기(읽기)** 용으로 남겨뒀다.
- ✅ 같이 넣은 `Analytics:Read` 는 제값을 했다 — `scripts/traffic-top.mjs` 가 살아나서
  "어느 주소가 요청을 먹나"를 실측할 수 있게 됐다(이번 4,200건도 이걸로 확인).

**[누적 효과]** 110,179 → 사진 −6,700 → 워드프레스 −4,200 → **약 99,300건/일.**
한도(100,000) 아래로 들어오긴 하지만 **여유가 1% 미만**이다.
다시 막히면 **워커 유료($5/월) 전환**하기로 사장님과 확정돼 있다.

## 2026-08-16 (저녁) — **라이브 배포 완료** (`npm run cf:deploy` · 버전 4883f63a)

오늘 것 전부 반영: 사진 정적 전환 · 예약금 계좌 안내(조회·챗봇) · 팝업 이탈 방지 · 카드 CSS.
**라이브(fantastrick.co.kr)에서 `scripts/smoke-test.mjs` 21/21 통과.**

⚠️ **배포 직후 1분쯤은 옛 화면이 섞여 나온다.** 처음 돌린 검사에서 2개가 실패했는데,
홈 HTML 이 **s-maxage=31536000(1년)** 으로 클라우드플레어에 캐시돼 있어서 **옛 HTML** 이 오고,
그게 이미 사라진 옛 JS 청크와 `/_next/image?url=…png`(옛 경로)를 불러 404 가 났다.
사진 파일 자체는 그때도 200 정상이었다. 잠시 뒤 다시 돌리니 21/21 통과.
→ **배포 직후 검사에서 404·옛 경로가 보이면 진짜 고장이 아니라 캐시다. 조금 기다렸다 다시 볼 것.**
(급하면 대시보드에서 캐시 비우기 — API 토큰엔 그 권한이 없다)

## 2026-08-16 (오후) — 예약금 **계좌를 다시 볼 수 있게** (조회·챗봇) + 팝업 이탈 방지

**[문제] 계좌를 볼 수 있는 곳이 세상에 딱 한 군데였다.**
접수 직후 뜨는 `[예약금 입금 안내]` 팝업뿐. 손님이 그걸 닫으면 다시 볼 방법이 없었다 —
**예약조회에도 없고, 문자에도 없다**(우리가 보내는 문자는 입금 확인 뒤의 확정문자 하나뿐).
게다가 그 팝업의 **[닫기] 가 체크 없이도 눌렸다**. 결국 "계좌를 못 찾겠다"는 전화가 온다.

**[고친 것 넷]**

1. **계좌 값을 `src/lib/pay.ts` 한 곳으로** — 세 화면이 같은 값을 쓴다. 계좌가 바뀌면 여기만 고친다.
   화면 부품(`components/DepositPay.tsx`)이 아니라 lib 에 둔 이유는, 챗봇처럼 **글자만** 필요한 곳이
   QR 라이브러리(`qrcode`)까지 끌고 오지 않게 하려는 것.
   - `components/DepositPay.tsx` = `<PayAccount>`(계좌 칸) + `<PayActions>`(토스·카뱅·복사·QR).
     기기 판별·QR 생성·복사 로직을 ReserveClient 에서 통째로 옮겼다(동작 동일).

2. **예약조회(/reservation) — 미입금 예약에만 입금 안내를 붙였다.**
   금액 · 계좌 · **그 예약의 실제 예약자 이름**("보내는 분 이름을 **홍길동** 으로") · 송금 버튼.
   조회는 이름+전화+비밀번호 4자리로만 되므로 새로 노출되는 정보가 없다.
   결제완료·취소·이용완료 건에는 안 나온다.

3. **챗봇에 `[예약금 입금 계좌]` 주제 추가.** 계좌·입금자명 주의·예약조회 링크.
   자유입력 분류도 손봤다 — `입금/송금/계좌번호/은행` → 새 주제, `환불` → 환불 규정(먼저 검사).
   "계좌"는 **환불 계좌**와 **입금할 계좌** 두 뜻이라 순서가 중요하다.

4. **팝업 이탈 방지 (사장님 지시).** `[확인했습니다]` 체크 전에는
   - **[닫기] 버튼 잠금**(`disabled`) — 원래 의도였는데 조건이 빠져 있었다. 복구.
   - **뒤로가기 차단** — 가짜 이력을 쌓아 두고 popstate 마다 되돌린다. 사실상 막힌다.
   - **새로고침·탭닫기** — ⚠️ **완전 차단은 브라우저 규격상 불가능하다**(악성 사이트가 사람을
     가둘 수 있어 금지). `beforeunload` 로 **확인창까지만** 띄운다. 문구도 브라우저가 정한다.
   - ⚠️ 체크 후엔 가짜 이력 한 칸이 남아 **뒤로가기를 한 번 더** 눌러야 이전 페이지로 간다.
     강제로 지우면 그 자체가 페이지 이동이라 팝업이 사라져서, 한 칸 남기는 쪽을 택했다.

**[덤] 예약조회 카드 스타일 누락 수정** — `.rev/.rev-h/.who/.date` 에 CSS 가 아예 없어서
테마명과 상태가 붙어 나왔다(`락다운 시티확정 대기`). ⚠️ `.who`·`.date` 는 관리자 화면에서도 쓰는
흔한 이름이라 **`.rev-list` 안으로 한정**해서 넣었다. 전역으로 주면 관리자가 깨진다.

**[검증] `scripts/smoke-test.mjs` — 오늘 작업 전체를 한 번에 보는 점검(21항목). `21/21 통과`.**
조회·접수 응답을 가로채므로 **진짜 DB 는 안 건드린다.** 오늘 만든 좁은 점검 2개
(`visual-check.mjs`·`check-deposit-ui.mjs`)는 여기에 흡수돼 지웠다.

봤던 것: 13쪽 전부 열림 · 사진 87개 중 깨짐 0 · 실패 요청 0 · JS 오류 0 ·
**`/_next/image` 요청 0건**(사진이 정말 서버를 안 깨운다는 증거) · 줄인 사진 9장 최대 118KB ·
예약조회는 3건(미입금·결제완료·취소) 중 **미입금 1건에만** 안내 · 계좌/금액/입금자명 정확 ·
챗봇 버튼·자유입력 모두 계좌 답변, "환불"은 환불 규정으로 · 팝업 체크 전 닫기 잠김·송금버튼 숨김·
**뒤로가기 막힘** → 체크 후 해제·폰에서 [토스로 바로 송금] 표시·정상 닫힘.

⚠️ **점검 스크립트를 쓸 때 걸린 함정 셋** (다시 겪지 말 것)
1. **공지 팝업(.nt-overlay)을 먼저 닫아야 한다.** 안 닫으면 모든 클릭이 가로막힌다.
   자료를 받은 뒤에 뜨므로 "떴는지 잠깐 기다렸다 닫기"로 처리했다.
2. **`next start` 를 멈춰도 포트가 안 풀린다.** 실제로 옛 빌드를 검사하고 "기능이 없다"고
   오판했다. 포트(3457)를 잡은 프로세스를 직접 죽일 것.
3. **스크롤만 하고 바로 재면 게으른 사진이 '깨짐'으로 잡힌다.** 실제로 9장이 깨진 줄 알았는데
   전부 200 정상이었다. 스크롤 → `loading=eager` → **다 끝날 때까지 기다린 뒤** 재야 한다.

## 2026-08-16 — 요청 한도(하루 10만) 대응 ①: **사진 자동 변환을 끄고 미리 줄여 넣기**

**[상황] 하루 요청이 한도를 넘고 있었다.** 8/14 = 131,373건 · 8/15 = 110,179건 (한도 100,000건).

**[먼저 잡은 사실 — 요금제]** 조회 스크립트가 "무료 = 하루 10만"을 전제로 짜여 있었는데,
CPU 시간(중앙값이 15.2ms 인 날도 있었음 · 무료 상한은 10ms)을 근거로 "이미 유료일 것"이라고
잘못 판단했다. **사장님이 대시보드를 확인해 준 결과 워커는 Free 가 맞다.** 한도는 실재한다.
(도메인 존은 `Free Website`, 워커도 Free — 둘은 별개 요금제라는 점만 확인해 둘 것)

**[요청 10만 건의 내역]** 5분간 실트래픽을 녹화해 분해했다(`wrangler tail` → `scripts/scraper-report.mjs`).

| 무엇 | 하루 | 비고 |
|---|---|---|
| 외부 수집기 `/api/slots` | 72,500 | **건드리지 않기로 확정**(아래) |
| 그 외 `/api/slots` | 10,900 | 손님 + 외국 봇 1대 |
| `/_next/image` | 6,700 | **이번에 0으로** |
| `/wp-admin/admin-ajax.php` | 4,200 | 우리 코드에 없음 — 관문 차단 대기 |
| 나머지 | 5,700 | 설정·신호등·페이지 |

**[⛔ 수집기는 손대지 않는다 — 사장님 결정]** IP `106.240.15.242`(LG유플러스 기업회선)가
2분 20초마다 120건씩(초당 8건 × 21초) 달력 전체를 긁는다. **그 회사에 예약 정보를 넘겨야 하므로
한 회차를 통째로 받아가게 두어야 한다.** 그래서 아래는 전부 **폐기**했다:
- 속도 제한(10초 15건) → 묶음을 중간에 잘라 뒷부분 예약이 안 넘어간다
- 시간대 차단(밤 11:30~08:00) → 그 시간 진짜 손님도 같이 막히고, 효과는 절반뿐
- `?all=1` 안내(120번 → 1번, 99% 감소) → **저쪽에 연락할 방법이 없다**
> 참고: 저쪽은 `?themeId=` 를 쓰는데 우리 코드는 `?theme=` 를 쓴다. 즉 `themeId` 가 붙은 요청은
> 100% 그 수집기다. 나중에 판별이 필요하면 이 차이를 쓰면 된다.

**[이번에 한 것] `/_next/image` 6,700건 → 0건**
- `next.config.ts` 의 `images` 를 **`unoptimized: true`** 로. 사진이 정적 파일이 되어
  **요청이 공짜·무제한**이 된다(Workers static assets 문서 근거).
- 끄기 전에 자동 변환이 하던 일을 미리 해 뒀다 — `scripts/images-to-webp.mjs`.
  실트래픽에서 실제 요청되는 크기가 `w=640`·`w=256` 두 가지뿐이라, 화면 `sizes` 값의 2배를 목표로 잡음.

| | 전 | 후 |
|---|---|---|
| 포스터 4장 + 마스코트 2 + 지도·이벤트·리뷰 | 8,579KB | **614KB (-93%)** |

- 참조 18곳을 `.webp` 로 교체(`src/lib/data.ts` 4 · `events` 8 · `page.tsx` 4 · `about` 2).
- **원본 9개는 `backups/images-원본-20260816/` 로 옮겨 보관**(지우지 않음).
- 실측 검증: `scripts/visual-check.mjs` 로 홈·소개·이벤트·테마·예약 5쪽을 크롬으로 열어
  **깨진 사진 0개 · 실패 요청 0건**. 끝까지 스크롤해 게으른 사진까지 확인함.
- ⚠️ **앞으로 새 사진은 미리 줄여서 넣어야 한다.** 아무도 안 줄여주므로 큰 파일을 그냥 넣으면
  손님이 그 크기 그대로 받는다. `images-to-webp.mjs` 의 `PLAN` 에 한 줄 추가하고 돌릴 것.

**[남은 것]**
- `/wp-admin/admin-ajax.php` 4,200건 — `middleware.ts` 가 404 로 끊고는 있지만 **워커는 이미 깨어난 뒤**라
  요청 수는 그대로 든다. 관문(WAF)에서 끊어야 진짜로 준다 → **토큰에 Firewall 편집 권한이 없어 대기 중**
  (`docs/클라우드플레어-속도제한-권한-열기.txt`).
- `/webhook/diag` 25초 → 60초(-700건)는 **태블릿 앱을 다시 깔아야 해서 보류**. 이득이 작다.
- **판정**: 이번 조치로 약 110,000 → **103,000건**. 아직 한도 위다. wp-admin 까지 끊으면 약 99,000건으로
  겨우 들어간다. **여유가 없으므로, 다시 막히면 워커 유료($5/월)로 전환하기로 사장님과 확정.**

**[새로 만든 조회 도구]**
- `scripts/worker-cpu.mjs` — 요청당 CPU 시간(중앙값·상위1%). 요금제 한계에 걸리는지 볼 때.
- `scripts/visual-check.mjs` — 5쪽을 크롬으로 열어 깨진 사진·실패 요청·JS 오류를 한 번에 점검.
- ⚠️ `scripts/traffic-top.mjs` 는 **토큰에 Analytics 읽기 권한이 없어 지금 실패한다.**

## 2026-08-13 (저녁) — 후기 **별점 제도 폐지**

- 사장님 지시로 별점을 화면·입력·저장 전 구간에서 걷어냈다.
- **손님 화면**: 후기 작성 폼의 [별점] 칸 삭제 · 후기 목록의 별 다섯 개 삭제.
- **홈 후기 섹션**: `4.8 / 5.0` 자리 → **후기 건수**(예: `1 건의 후기`).
  대표 후기 3건을 고르던 기준이 '별점 높은 순'이었으므로 **최신순**으로 바꿨다.
- **관리자**: 목록의 `4/5`·`별점 없음` 표시 삭제 · 외부 후기 등록 폼의 별점 선택칸 삭제.
- **서버**: `POST /api/reviews` · `/api/admin/reviews` 에서 rating 검증·저장 제거,
  조회 컬럼(select)에서도 뺐다.
- **CSS**: `.stars` `.star` `.star.on` `.rev-stars` `.rq-stars` `.s-stars` 규칙 삭제.
  `IconStar` 는 이제 아무 데서도 안 쓴다(컴포넌트 자체는 남겨둠 — 일반 아이콘이라 나중에 쓸 수 있음).
- ⚠️ DB `reviews.rating` 칸은 **그대로 둔다**. 옛 후기에 값이 남아 있지만 어디서도 읽지 않는다.
  지우고 싶으면 `alter table reviews drop column rating;` 한 줄. 급하지 않다.
- ⚠️ 배포 직후 홈 화면에 잠깐 옛 별점 화면이 보였다 — Cloudflare 가 HTML 을 캐시하고 있어서다.
  몇 십 초 뒤 저절로 풀렸다. 급하면 대시보드에서 캐시 비우기(API 토큰엔 그 권한이 없음).

## 2026-08-13 (저녁) — 1:1 문의 기능 **철회**

- 사장님 판단으로 **손님 문의를 홈페이지에서 받지 않기로** 했다.
  챗봇 `직접 문의할래요` → **매장 전화번호 안내까지만**.
- 지운 것: 챗봇 문의 폼(`InquiryForm`) · `POST /api/inquiry` · `/api/admin/customer-inquiries`
  · 관리자 문의 탭(`AskTab`·`ReplyBox`)·뱃지 · 관련 CSS.
- `customer_inquiries` 표는 **자료 0건**으로 남아 있다. 그냥 둬도 무해.
  지우려면 `supabase/migration_customer_inquiries_APPLY_ME.sql` 의 `drop table` 한 줄만 실행.
- 함께 검토했던 **텔레그램 무료 알림도 되돌림**(배포 안 됨). 문자 요금을 아끼려는 것이었는데,
  문의를 안 받게 되면서 이유가 없어졌다. B2B 도입 문의 알림에 쓰고 싶어지면 다시 붙이면 된다.
- ⚠️ 다시 "홈페이지에서 문의를 받자"가 나오면, 그 전에 **답변을 손님에게 어떻게 돌려줄지**부터
  정할 것. 안 정하면 문의만 쌓이고 답은 안 나가는 화면이 된다.
- `ALERT_PHONE`(사장님 개인폰 01040559699)은 **그대로 둔다** — B2B 도입 문의 알림이 이걸 쓴다.

## 2026-08-13 — 챗봇 1:1 문의 + 관리자 문의 탭 / 메인 스크롤 체감 개선 <sub>(문의 부분은 위에서 철회됨)</sub>

**① 1:1 문의 (사장님 지시)**
- 챗봇 버튼 `직접 문의할래요` → **`1:1 문의 남기기`**. **카카오톡·문자 문의 버튼 삭제.**
  이유 = 밖으로 내보내면 사장님이 카톡·문자·전화를 돌아다녀야 하고 **답을 했는지가 아무 데도 안 남는다.**
- 챗봇 말풍선 안에 폼(이름·연락처·내용) → `POST /api/inquiry` → `customer_inquiries` 표에 쌓임.
  접수되면 **사장님 폰으로 알림 문자** 한 통(`notifyOwner`).
- 관리자에 **문의 탭** 신설(답변 대기 건수 뱃지). 손님 글은 인용 스타일로 원문 그대로,
  아래 답변칸에 쓰고 저장하면 상태가 자동으로 `답변함`. **[문자 문구 복사]** 로 바로 보낼 문장을 만들어 준다.
- ⚠️ 발신번호 심사 전이라 **답변 자동발송은 아직 없다** — 복사해서 문자앱으로 보내는 방식.
  승인되면 그 자리에서 보내도록만 바꾸면 됨(문구는 그대로).
- **SQL 필요**: `supabase/migration_customer_inquiries_APPLY_ME.sql` (적용 완료 2026-08-13).
  표가 없어도 화면은 안 깨지고 손님에게 전화번호를 안내한다.
- 실측 확인: 브라우저 폼 → 저장 → 관리자 목록까지 한글 깨짐 없이 통과.
  ⚠️ **curl 로 한글 JSON 을 보내면 CP949 로 깨진다** — 진단할 때 curl 결과를 믿지 말 것(실제로 겪음).

**② 스크롤이 "뚝뚝 끊긴다" (사장님 지적)**
- **먼저 쟀다**: CPU 를 6배 느리게 해도 프레임 중앙값 16.7ms·끊김 0개 → **프레임은 안 떨어진다.**
  즉 성능이 아니라 **연출 타이밍**이 문제였다. (`scripts/scroll-perf.mjs` 로 언제든 다시 측정)
- 고친 것 셋:
  1. 등장 연출 시작 시점 `threshold 0.14` → `0.01 + rootMargin -6%`. 전에는 요소가 14% 보일 때까지
     기다렸다 0.7초 연출을 시작해서 **스크롤을 멈춘 뒤에야 내용이 따라 올라왔다.**
  2. 줄줄이 늘어선 카드의 지연 `0.1s×i`(4번째 카드 0.4초) → `0.06s×i, 최대 0.18s` + 연출 .7s→.55s.
  3. `CircuitFlow` SVG — 화면 밖에서도 스크롤 프레임마다 `getBoundingClientRect`+선 다시 그리기를
     하고 있었다. IntersectionObserver 로 **보일 때만** 계산.
- 히어로 높이 `100vh` → `100svh` 병기 — 폰에서 주소창이 숨었다 나올 때마다 히어로가 늘었다 줄며
  아래 내용이 통째로 밀리던 것(진짜 "뚝")을 없앰.

## 2026-08-13 — 현장 잔금을 예약금과 자동으로 구분

- **문제**: 손님이 플레이하러 와서 내는 **나머지 금액(잔금)** 이 매번 "맞는 예약 없음"으로 떴다.
  자동매칭(`matcher.ts`)은 **입금을 기다리는 예약**만 뒤지는데, 잔금을 내는 손님은 이미 확정된 상태라
  후보에 아예 없기 때문. 손댈 필요 없는 돈이 매일 쌓여서 **진짜 문제(오송금·이름 다름)가 묻혔다.**
- **구분 기준 = 입금 시각**: 예약금은 예약 직후(며칠 전), 잔금은 **플레이 직전**에 들어온다.
  → 이름이 같은 **확정 예약**의 시작 시각이 입금 시각 **앞뒤 1시간**(사장님 지정) 안이면 잔금.
  실제로 소급 검사한 82건이 전부 **플레이 −15분 ~ +30분** 구간에 몰려 있었다(규칙 검증).
- **새 파일** `src/lib/bank/balance.ts` — 판별 규칙 한 곳에. 테스트 `tests/balance.test.ts` 10개 통과.
- **연결**: `api/bank/deposit` 에서 matcher 가 **아무것도 못 찾았을 때만** 이 규칙을 돌린다
  (→ 진짜 예약금을 잔금으로 오해할 수 없다). 예약은 **건드리지 않고** `deposits.status='balance'` 딱지만.
- **화면**: 입출금 내역에 초록 `현장 잔금` 배지 + "플레이 7분 전에 들어온 돈이라…" 설명.
  `손이 필요한 것만 보기` 에서도 빠진다.
- **소급 적용**: `scripts/backfill-balance.mjs --apply` 로 지난 기록 **82건**을 `no_match → balance` 로 정리.
  (라벨만 바꿈 · 예약·돈 무관) 남은 `no_match` 328건은 대부분 이사 전 기록.
- ⚠️ **DB 변경 없음** — `deposits.status` 는 자유 문자열이라 마이그레이션 불필요.

## 2026-08-13 — 입금 자동승인 실전 재전환 (연습모드로 되돌아가 있던 사고 수리)

- **증상**: 박근영님 실전 입금(12만원, 13:46)이 자동매칭까진 성공했는데 `dry_run`(연습모드)으로만 기록되고 실제 입금확인이 안 눌림. 사장님이 수동으로 처리.
- **원인**: 7/30에 `BANK_DRY_RUN=false`로 전환했었는데, 이후 알림톡 키 등을 `cf-secrets.cjs` bulk로 재등록할 때 `.env.local`의 `true`가 같이 올라가 라이브가 연습모드로 **되돌아가** 있었다.
- **조치**:
  1. `printf 'false' | npx wrangler secret put BANK_DRY_RUN` — 라이브 실전 전환 (단건 등록)
  2. **재발 방지**: `cf-secrets.cjs`의 bulk 목록(KEYS)에서 `BANK_DRY_RUN` 제거 — 앞으로 bulk를 몇 번을 돌려도 이 값은 안 건드림. `.env.local`은 로컬 안전용 `true` 유지.
  3. 박근영님 건: 예약(8/19 21:00 락다운시티) `confirmed`·수동 입금확인·**예약확정 알림톡 발송 성공(sent)** 전부 확인. deposits 기록만 `dry_run→approved`로 정리(관리자 화면 연습모드 표시 제거).
  4. **라이브 E2E 재검증**: 연습 예약(입금테스트/3만/010-0000-9999) 생성 → 웹훅 POST → `decision:"approved"` → 예약 `confirmed`·`paid_source:auto` 확인 → 연습번호라 알림톡 차단(skipped, 정상) → 테스트 데이터 전부 삭제.
- 파일: `scripts/cf-secrets.cjs`, `.env.local`(주석), Cloudflare 시크릿.

## 2026-08-13 밤 — 옛 서버를 "점검 안내" → "자동 이동" 으로

🔴 **이전이 끝났는데도 일부 기기가 옛 IP 를 붙들고 점검 페이지를 봤다.**
시크릿 모드로도 재현됨 → 캐시가 아니라 **크롬 내부 DNS·소켓 캐시**(시크릿도 공유)가 원인.
실측: Cloudflare 경유는 http/https 모두 새 홈페이지 200, 일반 접속만 503(옛 서버).

→ 옛 서버 .htaccess 를 **302 자동 이동**으로 교체. 손님이 버튼을 누르거나 캐시를 지울 필요가 없어졌다.
- 경로를 그대로 이어 붙인다: 옛주소/rooms/ldc → 새홈페이지/rooms/ldc
- **301 이 아니라 302** — 캐시가 풀리면 사라질 과도기 상태다. 301 은 브라우저가 영구 기억해
  나중에 이 서버를 다른 용도로 쓸 때 되돌릴 수 없다.
- 예외(이동 안 함): /hint-phone(매장 태블릿), /wp-admin(옛 자료), /maintenance.html, /naver*
- 백업: docs/옛사이트-htaccess-점검중-백업.txt · 되돌리려면 "이동" 블록만 삭제

## 2026-08-13 저녁 — 운영 손질 4가지 (이전 당일 후속)

- **옛 사이트 5분 동기화 완전 중지** — 본 사이트가 됐으므로(사장님 지시). 예약 작업
  fantastrick-wp-sync 삭제(이미 Disabled 상태였음). /api/cron/wp-sync 창구는 남김(수동용).
- **관리자 취소 = 환불 과정 생략** — 사장님 취소는 대부분 전화로 끝난 건이라, 취소 시
  refunded=true 로 함께 표시(환불율은 근거로 기록). "계좌 입력 필요"에 영영 남던 문제 해결.
  손님 직접 취소는 그대로 환불 큐를 탄다. + 환불 대기 카드에 [취소 완료(환불 과정 생략)] 버튼.
- **카톡 못 받은 손님 명단 → 알림톡 탭으로 이동** (설정→문자 문구에선 제거 — 찾기 어려웠음).
- **문자 문구 탭의 발송 내역 제거**(사장님 지시 — 알림톡 탭이 대체). ⚠️ 실패 문자
  [다시 보내기] 버튼도 함께 사라짐 — 필요해지면 알림톡 탭에 재이식.
- 화면 오류 안전망(error.tsx) 신설 — 터지면 말없이 새로고침 1회, 반복 시 한국어 안내.

## 2026-08-13 — 🎉 도메인 이전 완료: fantastrick.co.kr = 새 홈페이지

### 타임라인 (전부 한 시간 안에 끝남)
옛 사이트 점검페이지(503) → 최종 동기화(112건·오류0) → 가비아 NS 교체(marek/pam)
→ 등록소 반영 **1분** → 사장님이 [Check nameservers now] 클릭 → 12:33 존 활성화
→ SSL 자동 발급(Google Trust) → 전 항목 검증 통과.

### 최종 상태
- https://fantastrick.co.kr = 새 홈페이지 (SSL 정상, http→https 301 강제)
- www → 대표주소 301 · /booking→/reserve 등 옛 주소 넘김 동작
- 메일(MX·SPF)·웹메일·db(예약 동기화) 전부 정상 — 동기화 실측 오류 0
- 네이버 소유확인·robots·sitemap 200
- tgc.fantastrick.co.kr — **사장님 결정으로 완전 폐기**(이전 전부터 죽어 있던 Vercel 잔재)
- old.fantastrick.co.kr = 옛 워드프레스 뒷문 (안정되면 삭제)

🔴 **SSL이 20분째 안 붙는 줄 알았다 — 사실 내 PC의 DNS 캐시였다.**
감시 루프가 시스템 리졸버로 옛 서버(가비아 인증서)에 붙어 exit 60을 냈다.
1.1.1.1 로 물으니 이미 Cloudflare IP, 엣지 직접 접속은 발급된 정상 인증서.
**전파 검증은 반드시 공용 리졸버(1.1.1.1)나 --resolve 로 할 것** — 로컬 캐시는 거짓말한다.

### 예약 오픈 시간 변경 팝업 (같은 날)
기존 공지 모달에 내용만 주입(배포 불필요·DB). 자정 오픈 → **저녁 9시** 오픈 안내
+ 캘린더에서 날짜 클릭하면 오픈일 확인 가능 문구. ~9/13 자동 종료.
⚠️ 처음에 "오전 9시"로 잘못 썼다가 수정 — updatedAt 갱신으로 "하루 안 보기"를 뚫고 재노출됨.

### 남은 후속 (급하지 않음)
- ~~메일 왕복 테스트~~ ✅ 사장님 확인(8/13 받기·보내기 모두 정상) · NHN 발신번호 심사 대기 계속
- 네이버 서치어드바이저·구글 서치콘솔에 sitemap 제출
- 알림톡 템플릿에 [예약 조회·취소] 버튼 추가(재심사 2~5일)
- 2~4주 후: 가비아 호스팅 해지(DB 백업 먼저) · old 레코드 삭제 · CF_DOMAIN_TOKEN 삭제

## 2026-08-12 — 도메인 이전 준비 ②: 미리 할 수 있는 것 전부

### 이전하는 날 = 네임서버 두 줄 교체뿐, 나머지는 오늘 다 해뒀다
- **@·www 를 워커(새 홈페이지)에 미리 연결.** 옛 서버 A 레코드는 지웠다 —
  네임서버 변경 전이라 실제 접속엔 무영향, 바꾸는 순간 즉시 새 사이트가 뜬다.
  옛 워드프레스 뒷문으로 old.fantastrick.co.kr(회색)만 남겨둠(안정되면 삭제).
- **robots.txt·sitemap.xml 신설** + metadataBase/canonical = https://fantastrick.co.kr 선언.
  임시 주소(workers.dev)로 들어온 검색엔진도 "원본은 fantastrick.co.kr"로 읽는다.
- **옛 워드프레스 주소 넘김**(next.config redirects): /booking→/reserve, /faqs→/faq,
  /policy→/privacy, /contacts→/business, wp-admin·wp-login·feed→/.
  /rooms/{id} 는 옛/새 구조가 같아 그대로 이어진다(옛 사이트 실측).
- **www→대표주소 301**(middleware) — www 호스트일 때만 동작, 지금 접속엔 무영향.
- **네이버 소유확인 파일 이식.** ⚠️ public/ 에 두면 정적 서버가 .html 을 307 로 튕긴다(실측)
  → 라우트로 정확한 주소에서 200 직접 응답.

🔴 **WP_DB_HOST 를 IP 로 바꿨다가 코드 주석 보고 즉시 원복** — 워커는 원시 IP 연결을 막는다(1104).
  db.fantastrick.co.kr 이 맞고, 이 이름은 새 DNS 에도 그대로 옮겨뒀으니 이전 후에도 안전.
  원복 후 wp-sync 실발송 확인(read 121 · errors 0).

## 2026-08-12 — 도메인 이전 준비 (Cloudflare 존 등록·DNS 정리까지)

### 아직 아무것도 안 바뀌었다 — 네임서버 변경 전이라 옛 사이트·메일·TGC 전부 그대로다
fantastrick.co.kr 을 Cloudflare 존으로 등록하고(상태 pending), DNS 를 전부 옮겨 적었다.
**스위치(네임서버 변경)만 남았고, 그건 사장님이 가비아에서 하는 마지막 단계다.**
- 배정 네임서버: `marek.ns.cloudflare.com` · `pam.ns.cloudflare.com` ← 가비아에 넣을 값
- 토큰: CF_DOMAIN_TOKEN(.env.local, 존 한정, ~8/31) — 끝나면 대시보드에서 삭제

🔴 **자동 스캔이 두 개를 빠뜨렸다 — tgc 와 hiworks.**
tgc(A 76.76.21.21, Vercel)는 손님이 실제 예약하는 사이트라, 그대로 넘겼으면 **주소가 통째로 사라졌다.**
미리 nslookup 으로 만든 실측 목록과 대조해서 잡았다. "자동 임포트를 믿지 말고 대조하라"가 실증됐다.

🔴 **스캔이 가져온 레코드 6개가 전부 주황(Proxied)이었다.**
Cloudflare 프록시는 웹(HTTP)만 통과시킨다 — db(MySQL)·mail/pop3(메일)은 주황이면 **그 순간 끊긴다.**
db·mail·pop3·webmail·tgc·hiworks 를 회색(DNS only)으로 정리. 주황은 @·www(웹)만 남김.
- 참고: mail 은 내 조사(A 211.47.76.59)와 스캔(CNAME mailapp.hiworks.co.kr)이 달랐는데,
  mailapp 이 그 IP 로 풀리는 걸 확인 — CNAME 이 맞고 오히려 하이웍스 서버 이동을 자동 추종한다.

## 2026-08-12 — 알림톡 연결 완료 + 블로그 후기 자동 수집

### 예약 확정 알림톡이 나가기 시작했다
카톡 쓰는 손님은 이제 **알림톡**으로 확정 안내를 받는다. 사흘간 비어 있던 확정문자가 대부분 메워졌다.
- 카카오 발신 프로필 `@판타스트릭` 등록(08-10) · 템플릿 `reserve-confirm` 카카오 승인(08-12 14:55)
- env 추가: `NHN_ALIMTALK_APPKEY` `NHN_ALIMTALK_SECRET` `NHN_SENDER_KEY` `NHN_TPL_CONFIRM`
- 1호점 폰으로 실제 알림톡 도착 확인.

🔴 **미승인 발신번호가 알림톡까지 막았다 (`-1031 All of receivers are failed to send`)**
알림톡에는 "안 가면 이 번호로 문자 보내라"(resendParameter)를 함께 싣는데, 그 번호가 심사 중이면
**카카오가 아니라 수신자 단위로** 요청이 통째로 거절된다. 알림톡은 카카오 채널로 나가 발신번호가
필요 없는데도 같이 막힌 것. → `-2312`·`-1031` 을 만나면 **대체발송을 빼고 한 번 더** 보낸다.
발신번호가 승인되면 첫 시도가 그냥 성공하므로 이 재시도는 다시 실행되지 않는다(되돌릴 필요 없음).

### 남은 것 — 문자 발신번호 심사
`01045470481` 사업자 번호로 08-10 20:04 접수, 아직 심사 중(`-2312 Not regist sendno`).
승인되면 **카톡 안 되는 손님에게 NHN 이 알아서 문자로 전환**해 보낸다. 코드는 손댈 것이 없다.

### 네이버 블로그 후기 자동 수집 (주소만 붙여넣으면 끝)
손님이 블로그에 써준 후기를 **작성자 동의를 받고** 출처를 밝혀 옮긴다.
관리자 › 후기 › "네이버 블로그 후기 옮기기" 에 주소를 넣으면 나머지가 전부 자동이다.
- 새 칸: `source_url`(원문 주소) `consent_note` `consent_at`(동의 기록) · `rating`·`phone` NULL 허용
- **별점 없음** — 블로그엔 별점이 없다. 없는 점수를 지어내면 손님을 속이는 것이라 별을 아예 안 그린다.
- **전문이 아니라 발췌(400자) + 원문 링크** — 전문을 옮기면 원글로 갈 이유가 사라져,
  써주신 분의 방문을 우리가 가져가는 모양이 된다. 출처는 `nofollow` 로 단다.
- 후기 날짜 = **원글이 쓰인 날**(옮겨 담은 날이 아니다). 목록 정렬도 이 값을 쓴다.

🔴 **네이버 본문 파싱에서 두 번 깨졌다**
- ① `se-main-container` div 를 정규식으로 잘라냈더니 **첫 `</div>` 에서 멈춰 1,660자만** 집어왔다
  (실제 3,442자 글을 "본문 없음"으로 판정). div 짝은 정규식으로 맞출 수 없다.
  → **문단 태그(`p.se-text-paragraph`)를 하나씩 줍는다.** 겹침을 셀 필요가 없어졌다.
- ② 앞에서부터 400자를 자르니 제목·인사말·글쓴이 자기소개만 담겼다.
  → **후기다운 표현이 몰린 대목**을 찾아 거기서부터 뽑는다. 스포일러 낌새(정답·엔딩·비밀번호)는 뺀다.

### 가비아 문자 (보류 — 안 쓰기로)
NHN 본인인증이 막힌 동안 가비아로 우회했다가 되돌렸다. 지금 **꺼져 있다**(`GABIA_SMS_ID` 삭제).
- 가비아 API 는 **발송 서버 IP 등록이 필수**라 Cloudflare 에서 직접 못 부른다(토큰 발급 단계에서 차단).
  고정 IP 인 가비아 웹호스팅에 중계 파일(`/ftsms/send.php`)을 두고 그것만 부르게 했었다.
- 그 도메인에는 SSL 이 없어 구간이 평문이라, 짐 자체를 **AES-256-GCM 으로 잠가서** 보냈다.
- ✅ **정리 완료(08-12)**: 서버의 `/ftsms/` 폴더 삭제(웹 404 확인) + `GABIA_*` 열쇠 4개 삭제.
  기존 워드프레스 파일은 하나도 건드리지 않았다(새로 만든 폴더만 지움).
  코드(`src/lib/sms-gabia.ts`·`gabia-relay/send.php`)는 **비밀값 없이 남겨둔다** —
  가비아 IP 차단과 우회 방법을 적어둔 기록이고, 다시 쓸 일이 생기면 열쇠만 넣으면 된다.
  지금은 `GABIA_SMS_ID` 가 없어 어떤 경로로도 호출되지 않는다.

## 2026-08-08 — 운영 프로그램에 힌트폰 추가 + 초보 사장님 말로 전면 교체

### 힌트폰 (운영 프로그램 화면)
사장님 지시로 "방 안에서 손님이 보는 화면도 만듭니다" 꼭지 신설. 근거는 **우리가 실제로 돌리는 두 가지**다.
- **락다운시티 힌트폰**(`D:\test3\hint-viewer` · `hint-phone`): 태블릿 2대 + GM 뷰어가 실시간 상태 공유.
  카드 태그 → 미션·디테일 표시, 스냅샷 복구, 시간 자동 동기화(v141), 사고 재시작 대응(v143).
- **스네이크코드**(`D:\test2\snake-matrix-2`): 코드 입력 → 통신문·진행률, 힌트 30개(1차 힌트 + 정답)와 뷰어.
- 화면에는 **손님 태블릿 ↔ 직원 화면** 두 짝 도해를 넣었다(모형 화면 규칙 그대로: 파란 점선 + 태그 + 이름·금액 없음).
- ⚠️ "둘 다 저희 매장에서 돌리고 있습니다" 문장은 **락다운시티는 확인됨, 스네이크코드는 사장님 확인 필요.**

### 초보 사장님 말로 (2026-08-07)
방탈출을 처음 여는 분은 제어기·모듈·마스터·슬레이브를 모른다. **50개 문장을 바꿨다.**
- 제어기 → **기계** / 모듈 → **판·늘림판** / 마스터 → **본체** / 슬레이브 → **늘림판** / GM → **직원**
- 제품 이름은 남기되 처음 나올 때 풀어 쓴다: "본체(마스터) 한 대가 장치 32개를 맡고, 모자라면 판(슬레이브)을 하나 더"
- 탭 이름·부제·문의 유형 칩·사양표·그래프 라벨·화면낭독기용 그림 설명까지 전부.

### 탭 아래 공통 블록 제거 (2026-08-07)
"탭을 바꿔도 아래는 똑같이 나온다"는 지적. 비교표·사후관리·경쟁사 우려·자주 묻는 것을 범위 밖에 둔 게 원인이었다.
이제 ① 통째로 = 비교표 + 경쟁사 우려 / ② 장치 = 사후 관리 + 자주 묻는 것 / ③ 프로그램 = 자기 내용만.
셋 다 [이어서 보기] + [문의]로 끝난다. 문의 유형 기본값도 보고 있는 화면에 맞춘다.
⚠️ 맞바꾼 것: 장치만 보는 손님은 비교표를, 턴키만 보는 손님은 사후 관리를 안 보게 된다.
   필요해지면 그 항목만 양쪽에 넣으면 된다(한 번에 하나만 그려져 화면이 지저분해지지 않는다).

## 2026-08-06 — 헤더 비침 수정 + 오른쪽 길잡이 (고르는 장이 지나간 뒤에도 바꾸게)

### 헤더 비침 (전역 수정 · globals.css)
- `header.scrolled` 의 배경이 **흰색 96%** 였다. 다크 화면에서는 티가 안 났지만 비즈니스의
  **밝은 화면(크림 종이·흰 SaaS)에서는 헤더 뒤로 지나가는 큰 제목이 유령처럼 비쳤다.**
- 알파를 1 로 올렸다. 4% 를 없애도 눈에 보이는 차이는 없고 비침만 사라진다.
  ⚠️ 전역 파일이라 홈·예약·관리자까지 함께 바뀐다. 배경이 원래 96% 라 영향은 비침 제거뿐.

### 오른쪽 길잡이 (넓은 화면) + 다시 고르기 (좁은 화면)
고르는 장은 한 번 지나가면 사라져서, 다시 고르려면 8,000px 을 거슬러 올라가야 했다.
- **≥1340px**: 화면 오른쪽에 세로 길잡이. 평소에는 **번호만(44px)**, 마우스를 올리면 172px 로 펼쳐진다.
  🔴 처음에 라벨을 펼친 채로 뒀더니 **본문을 56px 가렸다**(1440 화면에서 본문 오른쪽 끝 1310 / 길잡이 시작 1254).
  접어두니 1340px 이상에서 안 겹친다. 라벨은 접혀도 DOM 에 남겨 화면낭독기는 이름을 읽는다.
- 🔴 **고른 항목을 그 화면 색으로 꽉 채웠더니 같은 색 배경 위에서 사라졌다**(크림 위 크림).
  길잡이는 항상 어두운 판으로 두고, 고른 것은 **글자색 + 왼쪽 굵은 선**으로만 표시한다.
- **<1340px**: 세로 목록은 화면을 먹고 한 손 조작도 어렵다. 대신 오른쪽 아래 **[다시 고르기] 단추 하나**만.
  누르면 고르는 장으로 돌려보낸다. 순서대로 넘어가는 것은 [이어서 보기]가 이미 맡는다.
  (이 사이트는 폰에서 `.float`(예약)를 오른쪽 **위**로 옮겨 뒀으므로 오른쪽 아래는 비어 있다)
- 🔴 띄우는 조건은 "안 보이면"이 아니라 **"지나간 뒤"**(`boundingClientRect.bottom < 0`).
  전자로 하면 페이지 맨 위에서도 고르는 장이 아직 아래에 있어 "안 보임"이라 열자마자 뜬다.

**검증**: 접힘 44px → 펼침 172px, 라벨 노출 확인. 길잡이로 전환 시 첫 섹션 68px 안착.
PC/태블릿/폰 세 폭에서 노출 조건과 본문 겹침 없음 확인. 헤더 배경 `rgb(255,255,255)` 확인.

## 2026-08-06 — 탭을 버리고 "고르는 장(章)"으로 (에이전트 3명 논의 후 적용)

**"강조가 안 된다"는 지적을 두 번 받은 자리**다. 세 번째를 부르지 않으려고 에이전트 3명(비주얼·UX·프론트엔드)에
붙여 원인부터 다시 잡았다. 결론은 **크기 문제가 아니라 격(格) 문제**였다.

### 진단 (실측)
- **얇은 sticky 띠는 화면 언어상 "도구 막대(필터·정렬)"** 다. 아무리 색을 칠하고 번호를 달아도
  상품을 고르는 자리로는 안 읽힌다.
- 이 페이지 h2 는 최대 46px 인데 **갈림길의 물음이 15px** 이었다. 캡션(13.5px)보다 겨우 컸다.
  가장 중요한 결정이 가장 작은 글자로 쓰여 있었다.
- 안 고른 단추는 테두리 알파 `.16` + 배경 투명이라 거의 유령이었다. **대조군이 안 보이면 고른 상태도 안 읽힌다.**
- 🔴 **폰에서 03 이 화면 밖으로 잘려 있었다** — 필요 폭 132×3+간격 = 412px, 375px 폰 가용 폭 331px.
  게다가 가로 스크롤 막대를 숨겨놔서 더 있다는 신호조차 없었다.
- 🔴 **히어로가 이미 하나를 골라버렸다.** "방을 통째로 만듭니다"는 ①턴키만의 문장이라 페이지 전체가
  턴키 소개로 규정됐고, 그러면 아래 선택은 "턴키를 보는 방법 세 가지"로 읽힌다.

### 적용
- **띠를 버리고 한 장을 통째로 줬다**(`.scopepick`). 물음을 **h2(최대 46px)** 로, 카드는 186px 높이에
  번호 30px · 상품명 21px. 카드 바탕에 그 범위의 질감(제도지 격자 / 기판 격자 / 파랑 번짐)을 깔아
  **고르기 전에도 어떤 화면인지** 손에 잡히게 했다.
- **히어로 중립화**: "방을 만드는 일부터 방을 돌리는 일까지." 턴키 문장은 아래 표제로 이사.
- **범위 표제(`.pn-head`) 신설**: 각 화면이 큰 번호 + 이름으로 시작한다. 색을 하드코딩하지 않아
  세 스킨이 알아서 따라온다.
- **바뀌었다는 신호(`.pn-sweep`)**: 전환할 때 빛이 한 번 지나간다. 밝은 두 화면에서는 브랜드색으로.
- **sticky 제거**로 얻은 것: `backdrop-filter` 합성 비용, "sticky 라 스크롤 기준을 못 잡는" 함정,
  폰에서 상시 잠식하던 세로 공간이 한꺼번에 사라졌다. 되돌아갈 길은 [이어서 보기]와 주소(#device) 두 겹.
- 🔴 **연타 잠금(260ms)**: 세 화면이 카본 검정 ↔ 크림 ↔ 흰색이라 빠르게 눌러대면 화면 전체 밝기가
  초당 몇 번 뒤집힌다. 빛에 예민한 분에게 위험한 깜빡임이라 막았다.
- **`scrollTo({behavior:"smooth"})` 는 CSS 로 안 꺼진다**(명시 옵션이 CSS 를 이긴다).
  모션 최소화 설정을 실제로 지키려면 `matchMedia` 로 직접 꺼야 한다. 지금 넣었다.

### 검증
빌드·타입검사 통과. 세 화면 × PC/모바일 6화면 **안 보이는 요소 0 · 가로 스크롤 0 · 콘솔 오류 0**.
5,200px 아래에서 눌렀을 때 네 경우 모두 그 화면 첫 섹션이 헤더 바로 아래(68px)에 정확히 안착.

### 안 한 것 (다음 판단거리)
UX 쪽은 **세 범위를 각자 페이지로 분리**(`/business/turnkey` 등)하자고 했다. 링크 공유·검색·광고 랜딩에
유리하고 국내 경쟁사(헬리드)도 그렇게 한다. 다만 공통 블록 컴포넌트화까지 필요한 큰 공사라
이번엔 한 페이지 안에서 격을 세우는 데까지만 했다. 영업 링크가 필요해지면 그때 나눈다.

## 2026-08-06 — 패널 디자인 확정: T2 종이 도면 · D6 카본 시안 · S1 밝은 SaaS

시안 20종(`docs/시안-패널디자인-20/`)에서 사장님이 고른 셋을 실제 페이지에 입혔다.
**모티프만 바꾼 1차 개편은 "거의 차이를 못 느끼겠다"는 지적을 받았다.** 배경도 글씨체도 그대로였기 때문이다.
이번에는 배경색·글씨체·강조색·모서리를 통째로 갈았다.

| | ① 통째로 만들기 | ② 제어기·장치 | ③ 매장 운영 프로그램 |
|---|---|---|---|
| 시안 | **T2 종이 도면** | **D6 카본 · 시안** | **S1 밝은 SaaS** |
| 배경 | 크림 #f3efe6 + 22px 제도 격자 (**밝은 화면**) | 카본 #0d0f12 + 45도 잔무늬 | 흰색 (**밝은 화면**) |
| 제목 글꼴 | 나눔명조(세리프) | General Sans | General Sans |
| 라벨·숫자 | IBM Plex Mono | IBM Plex Mono | General Sans |
| 강조색 | 먹빛 파랑 #0d3b8c | 시안 #00d4ff | 파랑 #2563eb |
| 모서리 | 각(2px) | 각(3~6px) | 둥금(12~16px) |

### 구조 변경 (필수였던 것)
- 🔴 **패널 껍데기를 `.wrap` 바깥으로 뺐다.** 안에 두면 밝은 배경이 화면 가운데 카드처럼 떠 보인다.
  이제 `<div class="pn-*"><div class="wrap">…</div></div>` 구조라 배경이 화면 끝까지 칠해진다.
- 🔴 **패널마다 색 토큰을 통째로 다시 정의한다** — `--text·--muted·--faint·--line·--brand·--surf·--card`.
  그래야 안에 있는 기존 부품(figure·tier·rail·swtable·nextup…)이 전부 그 톤을 따라온다.
  ⚠️ 앞으로 패널 안에서 색을 쓸 때는 반드시 `var(--…)`. 하드코딩하면 그 부품만 톤이 깨진다.
- 밝은 패널 둘에는 `color-scheme: light` 를 선언했다. 안 하면 스크롤바·폼 기본 UI가 다크로 남아 튄다.
- 제어기 SVG 는 JSX 에 파랑(#6ea8ff)이 박혀 있어 CSS 로 시안을 덮었다.
  프레젠테이션 속성은 CSS 보다 우선순위가 낮아 이길 수 있다. 이를 위해 SVG 에 `.ports`·`.mcu`·`.hole`
  클래스를 추가했다(마스터·슬레이브 판은 `fill=격자무늬`라 색 규칙이 달라 따로 묶어야 했다).

### 검증
빌드·타입검사 통과. 세 패널 × PC/모바일 6화면 **안 보이는 요소 0 · 가로 스크롤 0 · 콘솔 오류 0**.
새 팔레트 15개 조합 대비 실측 **4.80~15.19:1 전부 AA 통과**(가장 빠듯한 것은 크림 위 흐린 글씨 4.80).

### 남은 판단
- **IBM Plex Mono 만 새 글꼴**이다. 라틴·숫자 자리에만 써서 한글 서브셋 부담은 없지만,
  배포 전에 실제 전송량을 재서 알릴 것. 나머지(나눔명조·General Sans)는 이미 받는 것이라 비용 0.
- 탭을 누를 때마다 화면이 **어둠 → 밝음 → 어둠**으로 바뀐다. 의도한 효과지만 눈이 한 번 적응한다.

## 2026-08-06 — 패널마다 성격에 맞는 디자인 (에이전트 3명 · 레퍼런스 기반)

사장님 지시로 탭 셋에 각각 어울리는 디자인을 입혔다. 전담 에이전트 3명이 **실제 사이트를 읽고** 낸 안을
조율해 적용. 세 화면이 갈라 보이되 **한 사이트로 읽히게** 하는 게 조율의 기준이었다.

### 갈라진 것 (딱 셋만)
| | ① 통째로 만들기 | ② 제어기·장치 | ③ 매장 운영 프로그램 |
|---|---|---|---|
| 모티프 | **건축 도면** | **기판·데이터시트** | **표의 행** |
| 기본 도형 | 선 | 사각 패드 | 셀 |
| 격자 | 48/8px 제도지 | 65/13px 도면지 | **가로 눈금만** 44px |
| 대표 장치 | 계통도·치수선·준공대장 | 무대·명판·눈금자·사양표 2열 | 장부면↔화면면·근무표·로그 |

**안 건드린 것(공통)**: 브랜드 색·글꼴·`.kicker` 문법·`.bz-sec` 110px·h2 스케일·곡률 위계·`.reveal`.
점선의 뜻도 통일했다 — **흰 점선 = 옛 방식(장부), 파란 점선 = 도해 표시**.

### 참고한 사이트 (에이전트가 실제로 읽은 것)
- 턴키: Sarner · Jora Vision · Snøhetta · NÜSSLI · TAIT/Thinkwell
  ⚠️ "도면을 배경 모티프로 쓴 시공사"는 **실증하지 못했다.** 그래서 도면 모티프의 근거는 유행이 아니라
  **우리가 실제로 도면을 그린다는 사실**이다(`.trio` 평면·동선 설계, `.rail` 도면·세트·마감).
- 제어기: Ubiquiti 스펙시트 · Playdate Specs(치수 도면) · Arduino UNO R4 · Loxone Miniserver 데이터시트 · Siemens LOGO! · Framework 슬롯 문서
- 운영 프로그램: Attio · Stripe Billing · 캐치테이블 플러스 · Shiftee · 채널톡
  (토스플레이스·도도포인트는 **반면교사** — 현장 사진과 요금 계산기는 우리가 못 쓰거나 이미 쓴 장치)

### 같이 고친 접근성 버그 3건 (실측)
- `.blk span` 파란 칩 위 `--faint` = **4.19:1** 미달 → `#c3d2ee` **6.33:1**
- `.blk.ghost` 를 `opacity:.42` 로 흐리게 만들어 글자까지 **≈2.6:1** → 불투명도 되돌리고 **채움을 빼고 점선만** (6.69:1)
- `.tiers` 의 인라인 `gridTemplateColumns` 가 미디어쿼리를 이겨 **360px 폰에서도 2열**로 남던 것 → CSS 로 이관

### 지켜야 할 상한 3개 (주석으로 못 박음)
"조금만 더 진하게" 하는 순간 AA 가 깨지는 지점들이다.
- 제어기 도면지 굵은격자 **알파 .075** · 제품 무대 파란 틴트 **.16** · `.blk` 채움 **.30**
- 운영 프로그램 가로 눈금 **알파 .055** (.07 로만 올려도 `--faint` 가 4.5 아래)

### 모션
새 애니메이션 3종(NEW 점·LED 링·평면도 배선)은 **끝 키프레임을 "보이는 상태"로** 잡고, 그 위에
`prefers-reduced-motion` 오버라이드를 또 걸었다. 전역 규칙이 애니메이션을 *마지막 키프레임에서 정지*
시키기 때문에, 끝 상태가 안 보이는 애니메이션은 그대로 사고가 된다(`.pulse` 선례).

### 검증
빌드·타입검사 통과. 세 패널 × PC/모바일 6화면 전부 **안 보이는 요소 0 · 가로 스크롤 0 · 콘솔 오류 0**.
새 색 조합 8개 대비 실측 = **5.43~9.09:1** 전부 AA 통과. 새 이미지·폰트 0.

## 2026-08-06 — 범위 선택을 "이동"에서 "내용 교체"로 (근거: NN/g + 긴 랜딩 실무)

- **왜 바꿨나**: 세 범위를 한 페이지에 이어 붙였더니 **12,066px**. 위쪽 칩은 그 자리로 스크롤만
  시켰는데, 사장님이 "이동 말고 내용만 바뀌게" 요청. 길이 자체가 문제였던 게 맞다.
- **웹 근거 정리**(찾아본 것):
  · NN/g — 탭 뒤에 숨은 내용은 **무시당한다.** 기본으로 열린 탭만 주목받는다.
  · NN/g — 내용이 자기 얘기고 훑기 좋으면 사람들은 **잘 내린다.** 쪼개서 클릭시키는 것보다 스크롤이 낫다.
  · 긴 랜딩 실무 — 5,000px 넘으면 **따라다니는 구역 이동 바**가 정석(전환 상승 사례).
  · SEO — 탭에 숨겨도 초기 HTML 에 있으면 색인은 된다(모바일 우선 색인). 다만 가중치가 낮게
    매겨진 사례가 있다. ⚠️ 우리는 지금 **안 고른 범위를 DOM 에서 아예 빼는 방식**이라 이 점이 남는다.
    검색 유입이 중요해지면 "펼침(아코디언, DOM 유지)"으로 바꾸는 것을 검토할 것.
- **결정**: 칩을 진짜 탭으로(내용 교체). 대신 탭의 고질병을 두 가지로 막았다.
  · **범위 끝마다 [이어서 보기]** 줄 — 나머지 둘로 바로 넘어간다. 위로 되돌아갈 필요가 없다.
  · **공통 구간은 늘 아래에** — 비교표 · 사후 관리 · 경쟁사 우려 · 자주 묻는 것 · 문의.
    상품과 무관하게 궁금한 것들이라 어느 범위를 보든 눈에 들어와야 한다.
- **고민 질문**은 선택 줄 위로 올려 항상 보이게(어느 범위를 고르든 먼저 읽는 것이 맞다).
- 범위를 바꾸면 선택 줄 바로 아래부터 보게 스크롤을 맞춘다. 이미 위쪽이면 건드리지 않는다.
  주소에 `#device`·`#software` 로 링크 가능.
- 🔴 등장 애니메이션 관찰(`IntersectionObserver`)을 `here` 의존성으로 다시 건다. 안 그러면
  범위를 바꿨을 때 새로 붙은 요소가 `opacity:0` 인 채 영영 안 보인다(탭 도입 때와 같은 함정).
- **길이 실측**: 12,066px → 통째로 **6,991** · 제어기 **9,331** · 운영 프로그램 **6,479**.
  세 범위 모두 안 보이는 요소 0 · 콘솔 오류 0 · 공통 구간 정상 노출 확인.

## 2026-08-06 — 비즈니스 개편 2차: 페이지를 둘로, 턴키를 앞으로 (키이스케이프 분석 반영)

사장님이 `business.keyescape.com` 을 보고 "이런 방식·느낌으로" 요청 → 에이전트 2종(구조·UX / 비주얼)에
분석을 맡기고 결과를 반영. **베낀 건 표현이 아니라 순서와 온도다.**

### 왜 키이스케이프를 그대로 따라가면 안 되나
- 그들이 파는 건 **제작 용역**(대기업 팝업·기업 교육)이고 가격은 전 페이지 비공개. 그게 가능한 이유는
  **JYP·에버랜드·현대차 로고 6개**가 있어서다. 우리가 같은 권위 화법을 쓰면 근거 자리가 빈 채로 남는다.
- 우리 자산은 로고가 아니라 **강남 3곳 11년 직영**이다. 화법도 여백이 아니라 현장 구어체가 맞다.

### 구조 변경
- 🔴 **페이지를 둘로 나눴다.** 고객이 둘인데 불안이 정반대라 한 페이지로는 둘 다 못 잡는다.
  · `/business` = 방탈출 사장님·창업 준비자 (본인이 결재, 값·고장 대응을 먼저 봄)
  · `/business/collab` = 브랜드·기관 담당자 (품의 필요, 범위·절차·회사 실체를 먼저 봄)
  서로 하단에 한 줄 링크만 건다. 헤더 메뉴에 둘 다 걸지 않는다(사장님이 협업 페이지를 먼저 보면 거리감).
- 🔴 **탭을 버리고 범위 선택기로.** 탭은 "둘 중 하나 고르라"는 화면이라 사다리 맨 윗칸(턴키)이 영영 안 보였다.
  지금은 위쪽 sticky 칩이 **그 자리로 스크롤만** 시키고, 아래에는 세 범위가 순서대로 전부 있다.
  칩의 켜진 칸은 스크롤 위치를 따라온다(손으로 내린 사람도 지금 어디인지 안다).
- **턴키를 맨 앞으로.** 등급표 3열 중 한 칸이던 것을 독립 섹션으로. 역량 3축(이야기·공간·장치) +
  공정 5단계(단계마다 "자체 인력" 배지) + 우리가 만든 방 4장 + **전화할 곳 다섯 vs 한 군데** 도해.
  ⚠️ 등급표에서 턴키를 뺀 이유: 기획·인테리어·배선·장치·시공 전부를 한 금액에 해준다고 읽히면
  실제 견적 때 사고가 난다(경쟁 조사에 테마당 2,000만원 사례 있음).
- **히어로 교체**: "제조사가 아니라, 방탈출 매장입니다" → **"방을 통째로 만듭니다."**
  옛 문장은 카피 규칙("A가 아니라 B" 금지) 위반이고, 제조 역량을 스스로 부정해 새 포지션과 어긋났다.
- **홈·소개와의 말 맞춤**: 홈의 [제작 역량 보기 →]·소개의 [제작·B2B 자세히 보기 →] 가 `/business` 를
  가리키는데 도착지는 제어기 판매뿐이었다. 턴키 섹션이 생기며 해소.
- **신제품 프레임**: "마스터 · 슬레이브 제어기" 섹션 신설. 기판 도해(SVG, 출력 32칸을 셀 수 있게 그림) +
  NEW 배지 + 데이터시트 톤 사양표. **사진·3D 렌더는 안 쓴다** — 어설픈 렌더는 사진이 없는 것보다 나쁘고,
  도면 톤이 하드웨어 회사엔 더 정직하다.
- **고민 질문 블록**을 상품 앞에. 손실 계산기를 먼저 들이대면 "우리 매장 잘 돌아가는데" 방어가 걸린다.
- **문의 유형 칩** 추가(통째로 시공 / 제어기 도입 / 운영 프로그램 / 그 밖에, 협업 페이지는 4종 별도).
  DB 는 `supabase/migration_biz_inquiry_kind_APPLY_ME.sql`. ⚠️ **칸이 없어도 문의는 접수된다** —
  서버가 `kind` 만 빼고 다시 넣고, 관리자 목록도 그 칸 없이 다시 부른다.

### 비주얼
- 섹션 여백 74px → 110px(사이트의 다른 화면 `.block` 이 116px 인데 여기만 빽빽했다).
- 제목 상한 34px → 46px, 히어로 50px → 62px. **크기가 커질수록 자간을 조이는 규칙**(h1 -.042 → h2 -.038 → lead -.01).
- 키커 앞 짧은 룰(사이트 `.shead::before` 문법과 통일), 히어로 격자 2중(26px+130px), 도해 카드에 깊이.
- 실적 카드의 포스터는 **작게·탈색**(grayscale .85). 포스터는 손님용 자산이라 크게 쓰면 B2B 톤이 무너진다.
  카드의 본체는 `기획 · 공간 · 배선 · 장치 · 제어` 태그 줄이다.

### 검증
빌드·타입검사 통과. PC/모바일 두 페이지 모두 **안 보이는 요소 0 · 가로 스크롤 0 · 콘솔 오류 0**.
범위 선택기 이동·켜짐 표시, 문의 유형 칩 실동작 확인. ⚠️ 아직 배포 안 함.

## 2026-08-06 — 비즈니스 페이지를 두 탭으로 (제어기 / 매장 운영 프로그램)

- **왜**: 하드웨어와 소프트웨어가 한 페이지에 섞여 있었다. 사장님 지시로 탭을 나눔.
  8/5 분석에서도 같은 지적이 있었다 — **하드웨어는 수백만원 한 번, 프로그램은 매달 쓰는 것**이라
  사는 결정 구조가 아예 달라서 섞으면 둘 다 흐려진다. 그때는 6:4 한 페이지로 갔던 것을 이번에 갈랐다.
- **나눈 기준**: 히어로·"경쟁사한테 사는 거 아니냐"·문의는 **공통**(양쪽 다 필요),
  · **제어기·장치** 탭 = 손실 계산기 · 누가 먼저 아느냐 · 확장 도해 · 등급 · 비교표 · 자주 묻는 것
  · **매장 운영 프로그램** 탭 = 지금/바뀌는 것 표 · 입금 자동확인 · 사후 관리
  (자주 묻는 것 3개는 전부 장치·공사·고장 이야기라 제어기 탭으로 옮겼다)
- **운영 프로그램 탭은 내용을 늘렸다** — 카드 4장뿐이라 탭 하나를 채우기엔 얇았다.
  8/5 분석 문서에 있던 before/after 대조(근태·급여·예약·쿠폰)와 케어(사후관리) 블록을 살려 넣고,
  우리가 실제로 돌리는 **입금 자동확인**(`/api/bank/deposit`)을 한 꼭지로 추가.
- 🔴 **값은 안 적었다** — 운영 프로그램 구독료는 정해진 값이 없다. 지어내면 안 되므로
  "제어기 넣으시면 함께 들어갑니다 / 따로도 상담" 으로만 두고 금액은 문의로 넘겼다. **사장님 확인 필요.**
- **탭 UI**: 스크롤을 내려도 따라오는 sticky. `top:68px` = 스크롤 중 고정 헤더 아래끝(실측 67px) 바로 밑.
  ⚠️ 처음에 62px 로 뒀더니 5px 이 헤더에 물려 탭 윗선이 잘렸다(실측으로 잡음).
  고른 쪽은 색만이 아니라 **채움**으로도 구분한다(색약 대응). 주소에 `#sw` 를 붙이면 프로그램 탭이 열린다.
- 🔴 **탭을 바꾸면 등장 애니메이션 관찰을 다시 걸어야 한다** — `IntersectionObserver` 를 처음 한 번만
  걸면, 탭 전환으로 새로 붙은 요소들이 `opacity:0` 인 채 영영 안 보인다. `useEffect` 의존성에 `tab` 포함.
- **검증**: 빌드·타입검사 통과. PC/모바일 두 탭 모두 **안 보이는 요소 0 · 가로 스크롤 0 · 콘솔 오류 0**,
  스크롤 중 탭바가 헤더에 안 가림.

## 2026-08-06 — 도입 문의가 관리자 화면에 쌓인다 (B2B 문의 접수)

- **왜**: `/business` 의 [도입 문의하기] 폼이 그때까지 **아무 데로도 안 갔다**(보내면 안내 문구만 떴다).
  사장님 결정으로 관리자 화면에 쌓는 방식으로 붙였다.
- **표 신설** `biz_inquiries` — `supabase/migration_biz_inquiries_APPLY_ME.sql`.
  매장명·연락처·방 개수·지역 + 상태(new → contacted → done / dropped) + 메모.
  ⚠️ 예약 표와 **FK 로 잇지 않았다**. 문의자는 아직 손님이 아니고, 두 표 사이 FK 가 늘면
  PostgREST embed 가 ambiguous 로 빈 배열을 준다.
- **API**: `POST /api/business/inquiry`(손님, IP당 3회/분 + 같은 번호 10분 내 재전송은 중복으로 무시),
  `GET/PATCH/DELETE /api/admin/inquiries`(관리자).
- **관리자 › 도입 문의 탭 신설** — 새 문의 건수 뱃지(1분마다), 상태 버튼(연락함 → 진행/완료 / 안 하기로),
  한 줄 메모, 전화번호는 눌러서 바로 통화. 되돌리기도 있다.
- 🔴 **SQL 을 아직 안 돌린 순간에도 화면이 안 깨지게** 했다. 표가 없을 때 오는 `PGRST205`(Supabase)
  와 `42P01`(포스트그레스)을 둘 다 잡아서 — 관리자에겐 "이 SQL 을 돌리세요", 손님에겐
  "지금은 접수가 안 되니 메일 주세요"로 안내한다. 실측으로 두 경우 다 확인.
- **검증**: 타입검사·빌드 통과. 표가 없는 상태로 손님 폼 전송·관리자 탭 진입까지 실제로 눌러봐서
  콘솔 오류 0 · 안내 문구 정상 확인.
- ⚠️ **남은 것**: 사장님이 Supabase SQL Editor 에서 위 SQL 1회 실행 → 그다음 `npm run cf:deploy`.

## 2026-08-06 — 비즈니스 페이지 전면 교체 (회사 소개 → 제어기·운영 프로그램 판매)

- **무엇**: `/business` 를 "역량 3축·서비스·5단계·레퍼런스·문의폼"(회사 소개형)에서
  **테마 제어기 + 매장 운영 프로그램을 파는 제품 페이지**로 통째로 교체. 사장님 선택(①안).
- **근거**: 사업설계 브리핑(2026-08-02, 사장님 제공) + 경쟁사 조사. 시안 v1~v3 원본은
  `docs/시안-제어시스템-B2B/`(plan=분석 보고서, preview3=채택본).
- **구성**: 히어로(제조사가 아니라 방탈출 매장입니다) → 손실 계산기 → 누가 먼저 아느냐(막대)
  → 확장 도해(모듈 스테퍼)+누적 비용 계단 그래프 → 등급 3종 → 비교표 → 운영 프로그램
  → 경쟁사 우려 정면돌파 → FAQ → 문의.
- **살아있는 장치 2개**(글로 설명하지 않고 손으로 만져보게 하는 것):
  · **손실 계산기** — 타임 요금·하루 타임 수를 넣으면 빠지는 금액이 바로 바뀐다. 우리가 금액을
    단정하지 않고 사장님이 자기 숫자를 넣는 구조.
  · **모듈 스테퍼** — [− 모듈 +] 를 누르면 도해에 모듈이 붙고 "장치 몇 개까지"가 올라간다(32→224).
- 🔴 **카피 규칙(고칠 때 반드시 지킬 것)**: 이모지 0 · 문장 속 대시(—) 금지 · "A가 아니라 B입니다" 반복 금지.
  업계어는 **장치**(장비 X) · **제어기/모듈**(블록 X) · **세팅**(리셋 X), 고장은 **"작동을 안 한다"**(은어 X),
  타임은 **"찬다"**. 방마다 장치 수가 달라 **"보통 몇 개" 같은 기준선 문장은 쓰지 않는다.**
  (에이전트 2종 조사 + 사장님 확인으로 굳힌 것. page.tsx 상단에도 같은 주석을 박아둠)
- **CSS는 `src/app/business/business.css` 로 분리**하고 전 규칙을 `.bizsys` 안에 가뒀다.
  `.bar`·`.slot`·`.step`·`.tier`·`.form` 은 관리자·예약 화면에도 있는 흔한 이름이라
  globals.css 에 풀어놓으면 엉뚱한 화면이 같이 바뀐다.
- **검증**: 타입검사·프로덕션 빌드 통과. 실제 렌더로 확인 — PC/모바일 **가로 스크롤 0·콘솔 오류 0**,
  스크롤 등장 요소 전부 표시, 계산기(8만×12타임 → 96만원)·모듈(96→160개) 실동작,
  모바일에서 고정 헤더가 히어로 제목을 안 가림(여유 36px).
- ⚠️ **아직 배포 안 함**(사장님이 "배포해" 하실 때 `npm run cf:deploy`).
- ⚠️ 이제 안 쓰는 CSS: `.biz-hero`·`.svc`·`.steps`·`.ref-grid`·`.cgrid`·`.cform`·`.cinfo`·`.block.contact`
  (globals.css). 지우진 않았다 — 홈에서 쓰는 `.cap` 계열과 얽혀 있어 따로 확인하고 지울 것.
- 파일: `src/app/business/page.tsx`, `src/app/business/business.css`(신규), `src/app/business/layout.tsx`(메타), `src/app/layout.tsx`(주석), `docs/시안-제어시스템-B2B/`(신규).

## 2026-08-06 — 기존 사이트 예약 동기화 다시 켬 (이번엔 기간 제한 없이)

- **왜**: 7/30에 건 작업 스케줄러가 "82시간 29분" 반복 기간으로 걸려 있어 **2026-08-03 00:30에 조용히 끝나 있었다**(마지막 실행 08-03 00:25, 이후 3일간 새 예약이 하나도 안 넘어옴). 목~일 테스트용으로 기간을 잘라 걸었던 게 원인.
- **조치**: `fantastrick-wp-sync` 작업을 **끝나는 날짜 없이(Repeat: Until: Duration = Disabled) 5분마다** 반복하도록 재등록. 실행 10분 넘으면 중단, 중복 실행 금지(IgnoreNew), 놓친 시각은 켜지면 바로 실행(StartWhenAvailable).
  ```powershell
  $a = New-ScheduledTaskAction -Execute "wscript.exe" -Argument '"D:\test3\fantastrick-homepage\wp-sync-hidden.vbs"' -WorkingDirectory "D:\test3\fantastrick-homepage"
  $t = New-ScheduledTaskTrigger -Once -At ((Get-Date).AddMinutes(-1)) -RepetitionInterval (New-TimeSpan -Minutes 5)   # ← Duration 을 안 주면 "무기한"
  $s = New-ScheduledTaskSettingsSet -ExecutionTimeLimit (New-TimeSpan -Minutes 10) -MultipleInstances IgnoreNew -StartWhenAvailable
  Register-ScheduledTask -TaskName "fantastrick-wp-sync" -Action $a -Trigger $t -Settings $s -User $env:USERNAME -Force
  ```
  ⚠️ `-RepetitionDuration ([TimeSpan]::MaxValue)` 는 "값이 범위를 벗어남" 오류가 난다. **인자를 아예 빼야** 무기한이 된다.
- **첫 실행 결과**: 밀려 있던 **새 예약 45건 반영**. 다만 **409(칸 중복) 4건 실패** — 8/6 20:00·8/7 20:00·8/8 12:40·8/8 16:40(전부 태초의 신부).
- ⚠️ **409의 정체 = 우리 쪽에 옛 손님이 남아 있는 것**. 예) 8/6 20:00 은 우리 사이트에 `류승희(confirmed, #38487)`가 자리를 잡고 있어 기존 사이트의 현재 예약자가 못 들어온다. 8/1 구조 변경으로 `--sync` 는 **추가만** 하고 저쪽의 취소·변경을 따라오지 않기 때문(우리가 처리의 주인이라 일부러 그렇게 함).
- **409 4건 해소 (같은 날 처리)**:
  - 먼저 기존 사이트에 SELECT 로 확인 — 막고 있던 `#38487·#38528·#38564·#38575` 는 **저쪽에서 아예 사라진(삭제된) 예약**이었다. "옮겨간 것"이면 우리 쪽에서 지우면 영영 못 돌아오므로(memo #ID 가 이미 있어 재추가 안 됨) **지우기 전에 반드시 이 확인을 한다.**
  - 우리 쪽 4건을 `status=cancelled` 로 정리 + 변경이력 기록. **`refund_rate` 는 일부러 안 넣었다** — 넣으면 `isRefundOwed` 가 참이 되어 실체 없는 예약이 [환불 처리] 큐에 "돈 돌려줘야 함"으로 올라온다. `admin_note` 에 "동기화 잔재" 로 표시.
  - 다음 동기화에서 **진짜 예약자 4명(전종훈·이슬이·김문희·이예지) 정상 반영**, 409 0건.
  - 🔎 함정: 워드프레스 `_appointment_timestamp` 는 **이미 한국시각인 epoch** 다. 조회할 때 +9시간을 또 더하면 9시간 밀린 자리를 보게 되어 "그 칸에 아무도 없음"으로 잘못 읽힌다.
- **무료 한도 영향 없음**: 1회당 Supabase 요청은 GET 1건(23.6KB) + 새 예약 수만큼 POST. 5분마다 = 하루 288회 ≈ **6.8MB/일, 약 200MB/월** (Supabase 무료 전송량 5GB의 4%, 요청 수는 무제한). 오히려 7일 무활동 시 프로젝트 일시정지를 막아준다. 기존 사이트에는 5분마다 SELECT 한 번뿐.
- 파일: `wp-sync-hidden.vbs`(주석의 종료일 표기 수정).

## 2026-08-03 — 🚦 입금 감시 신호등 (26시간 멈춤을 아무도 몰랐던 사고 대응)

### 무슨 일이 있었나
**2026-08-02 10:17 ~ 08-03 12:26, 약 26시간** 동안 매장 태블릿에 카카오톡이 화면에 없어서
화면 감시(접근성)가 채팅 글자를 하나도 못 읽었다. 그런데 **아무도 몰랐다.**
신호는 `bank_diag` 표에 5분마다 꼬박꼬박 쌓이고 있었는데 **그걸 보는 화면이 없었다.**

다행히 입금은 안 놓쳤다. 태블릿 앱에 경로가 둘이라 **알림 캡처**가 전부 대신 받았기 때문이다.
그런데 바로 그 알림 캡처는 **진단을 하나도 안 남기고 있었다** — 즉 실제로 일하던 쪽이
감시 사각지대였다. 이게 조용히 죽었으면 입금이 통째로 유실되고 흔적조차 없었을 것이다.

### 관리자 [입금·환불] 맨 위에 신호등 한 줄
- 새 API `GET /api/admin/bank-health` — `bank_diag` 를 읽어 세 신호로 요약(읽기 전용).
  **화면 감시**(서비스 살아있나) · **카톡 화면**(채팅방이 떠 있나) · **알림 캡처**(예비 경로).
- **정상이면 한 줄로 접히고, 문제가 있으면 저절로 펼쳐진다.** 매일 보는 화면이라 평소엔
  자리를 차지하면 안 된다.
- 🔑 **하나 멈춤(🟡)과 둘 다 멈춤(🔴)을 반드시 구분한다.** 경로가 서로 예비라서 하나 멈춰도
  입금은 계속 들어온다. 하나 멈췄다고 빨강을 켜면 빨강에 익숙해져 **진짜 빨강을 무시하게 된다.**
- 기준: 하트비트는 5분 주기 → 3번 빠지면(15분) 죽은 것. 카톡 화면은 30분 주의 / 2시간 문제
  (실측상 새벽엔 20분쯤 조용한 구간이 있어서, 밤마다 빨강이 켜지지 않게 느슨히 잡았다).
- ⚠️ "모른다"를 "멈췄다"로 말하지 않는다 — 태블릿 앱이 0.4.0 미만이면 알림 캡처 신호가
  없는 게 정상이다. 코드에서 이 분기가 "하나 멈춤"보다 **먼저** 와야 한다(순서 바꾸면
  멀쩡한 태블릿을 고장났다고 말한다).

### 🔑 재설치 없이도 예비 경로를 본다 — **역추론** (사장님이 "태블릿 재설치 안 하고 싶다")
세 번째 칸을 [알림 캡처] → **[예비 경로]** 로 바꾸고, 두 가지 방법으로 본다.
- **(a) 진짜 하트비트** — 앱이 0.4.0 이상이면 `notif_heartbeat` 이 5분마다 온다. 제일 정확.
- **(b) 역추론** — 입금은 들어왔는데 화면 감시의 전송 기록(`send_result`)이 짝으로 없으면,
  **화면 감시가 아닌 다른 게 그 입금을 넣었다** = 예비가 살아 있다는 증거.
- ⚠️ (b) 로는 **절대 "죽었다"고 말하지 않는다.** 최악이 "판단 보류"다 — 입금이 없는 시간대엔
  조용한 것과 죽은 것이 구분되지 않기 때문. 되돌려 볼 시간은 18시간(밤에 8시간쯤 끊기는 구간이
  실제로 있어서, 짧게 잡으면 매일 새벽 보류로 넘어가 쓸모가 없어진다).
- ⚠️ (b) 는 태블릿 알림 캡처인지 PC 캡처인지 **구분하지 못한다.** 목적("화면 감시 말고 다른 게
  받아주고 있나")엔 같은 값이라 칸 이름을 정직하게 [예비 경로]로 두었다.

**거짓 빨강을 없앤 게 핵심이다.** 이 역추론이 없으면, 화면 감시가 카톡을 못 읽는 동안
(=이번 26시간) 실제로는 입금이 다 들어오고 있는데도 🔴 "입금이 안 잡히고 있습니다"가
26시간 내내 떠 있게 된다. 그러면 사장님이 빨강을 안 믿게 된다.
이제 🔴는 **태블릿이 하트비트조차 안 보낼 때만** 켜진다.

### 짝이 되는 태블릿 앱 변경 (bank-auto, v0.4.0)
알림 캡처(`KakaoNotificationListener`)도 이제 화면 감시와 똑같이 상태를 보고한다 —
`notif_connected` / `notif_heartbeat`(5분) / `notif_send` / `notif_disconnected` / `notif_destroyed`.
**앱을 태블릿에 새로 깔아야 알림 캡처 칸에 불이 들어온다.** 그전까진 "알 수 없음"으로 뜬다.

## 2026-07-31 — 태블릿 자동입금확인 실전 투입 + 관리자 화면 정리

오늘의 큰 줄기: **손님 예약은 기존 사이트로 들어오고, 예약금 입금은 매장 태블릿이 본다.**
그러려면 "기존 사이트가 모든 것의 기준"이던 규칙을 한 군데 깨야 했다.

### 🔑 입금·확정은 홈페이지가 주인 (규칙 변경 — 두 파일이 짝이다)
- 전에는 웹훅이 `wp-import` 예약을 자동확인 후보에서 뺐다. 5분 동기화가 기존 사이트 기준으로
  입금·확정을 **되돌려서**, 자동확인이 눌러봐야 5분 뒤 사라졌기 때문이다.
- 이제 역할을 나눈다 — **기존 사이트 기준**: 예약 존재·날짜·시간·이름·취소 /
  **홈페이지 기준**: 입금확인. 즉 **입금·확정은 올라가기만 하고 내려오지 않는다.**
- ⚠️ `src/app/api/bank/deposit/route.ts`(제외 필터 삭제)와 `scripts/import-from-wp.mts`(`holdPaid`)는
  **짝**이다. 한쪽만 되돌리면 자동확정이 5분 뒤 사라지는 버그가 되살아난다.
- 동기화 삭제 규칙도 함께: **입금된 예약은 지우지 않고 '취소'로 남긴다** — 돈이 들어온 기록이
  조용히 사라지면 환불할 건이 장부에서 증발한다.

### 🔴 30분 자동취소가 동기화 열쇠를 지우던 버그
`sweepExpiredReservations` 가 기존 사이트에서 온 대기 예약까지 취소하면서 memo 를
"미입금으로 자동 취소"로 덮어썼다. 그런데 그 memo 에는 **동기화 열쇠(#ID)** 가 들어 있어
동기화가 예약을 못 알아보고 삭제·재생성한다. → wp-import 는 만료 정리에서 제외.
(30분 룰은 홈페이지에서 직접 예약한 손님과 한 약속이지, 저쪽 예약에 쓸 규칙이 아니다)

### 관리자 화면
- **[입출금 내역] 에 '통장(카톡) 기준' 신설** — 왼쪽 통장, 오른쪽 그에 맞는 예약을 나란히.
  자동확인이 **왜 보류했는지**를 문장으로 말한다(금액이 4천원 다름 / 이름이 다름 /
  이미 입금확인됨 …). 전에는 **돈은 들어왔는데 예약을 못 찾은 입금이 화면 어디에도 없었다.**
- **[자동 취소] 탭 신설**(입금확인↔환불처리 사이) — 손님 컴플레인 대응용. 시각을 셋으로 나눠
  `접수 / 입금 마감 / 정리됨` 을 보여준다. ⚠️정리(sweep)는 사이트 접속이 있어야 돌아서
  마감보다 한참 뒤일 수 있다 — **손님에게 말할 기준은 '입금 마감'** 이다.
- **사장님 한 줄 메모를 `admin_note` 전용 칸으로 분리**(마이그레이션
  `migration_admin_note_APPLY_ME.sql`, 적용 완료). memo 는 시스템 칸이라 손대면 안 된다.
  덕분에 **기존 사이트 예약에도 메모를 쓸 수 있게 됐다**(전엔 숨겨져 있었음).
- **이미 찬 시간은 아예 못 고르게** — 예약 옮기기·수동 등록 드롭다운에 `13:20 — 예약있음`,
  마감 칸은 `— 마감`. 전엔 안내문만 "못 옮겨요"라 하고 화면은 막지 않아 눌러봐야 알았다.
  손님 예약 화면도 "마감" → **"예약있음"** 으로 구분(남이 먼저 잡은 것과 우리가 닫은 것은 다른 말).
- 상세 팝업에서 **메모칸·[입금 취소] 제거**, **[오늘] 탭 제거**(예약 탭이 기본이라 겹침).
- 입금 대기 줄에 **접수 시각** 추가 — 카운트다운의 기준점이 안 보였다.

### 개인정보
태블릿은 카카오톡 "화면"을 읽으므로 채팅 목록이 뜨면 **남의 대화 미리보기**까지 "입금/송금"
필터에 걸려 서버로 올라온다(실제로 저장됐다). `looksLikeBankNotice()` 로 은행 문구가 아니면
원문을 저장하지 않게 막고, 이미 쌓인 2건은 원문을 지웠다. **앱 단 차단은 다음 APK 때.**

### 확인된 것 / 남은 것
- ✅ 태블릿 → 서버 → 파싱까지 **실제 손님 입금으로 검증**(13:50 이서영 30,000원 도달).
- ⬜ **자동확정이 실제로 눌리는 것은 아직 못 봤다** — 대기 예약이 있어야 한다.
  기존 사이트에 새 예약이 대기로 들어오면 그걸로 확인한다(사장님 판단).
- 파일: `src/app/api/bank/deposit/route.ts`, `src/app/api/admin/deposits/route.ts`(신규),
  `src/app/api/slots/route.ts`, `src/lib/expire.ts`, `src/lib/bank/parser.ts`,
  `src/app/admin/page.tsx`, `src/app/reserve/ReserveClient.tsx`, `scripts/import-from-wp.mts`,
  `supabase/migration_admin_note_APPLY_ME.sql`(신규).

## 2026-07-30 — 태블릿 입금 감시: 알림 캡처 → 화면 감시(접근성)로 전환
- **왜**: 매장 태블릿에서 카카오톡 푸시 알림이 늦게/안 떠서(절전·멀티기기) 알림 캡처가 계속 실패. 앱을 열면 밀린 메시지가 몰려오는 증상. 사장님 지시로 **"카뱅 채팅창을 24시간 띄워두고 그 화면의 새 메시지를 읽는"** 방식으로 확정.
- 앱(`bank-auto/android`)에 `ChatWatcherService`(접근성) 추가 — OCR 아니라 접근성 트리 텍스트를 읽어 글자 100% 정확, 카카오톡 화면만 감시, 켜질 때 기존 문구는 기준선 학습만(과거 재전송 없음), "입금/송금" 문구만 전송. 기존 알림 리스너는 이중 안전망으로 유지(서버 event_id 로 중복 차단). 자세한 내용·설치 순서는 `bank-auto/SETUP-TABLET.md`.
- **APK 를 홈페이지 정적 파일로 서빙**: `public/apk/banknotify-020.apk` → 태블릿 브라우저로 바로 설치(`/apk/banknotify-020.apk`). 이 PC에 JDK·SDK 가 없어 빌드는 GitHub Actions(bank-auto `android-build.yml`)에서 받아온 산출물. ⚠️debug 서명이라 기존 앱 삭제 후 설치.
- **검증 진행**: 앱의 "서버로 실제 전송해보기"가 홈페이지에 도달 확인(`deposits` 에 홍길동 30,000원 / no_match 기록 = 경로·파싱·매칭판정 정상). **남은 것 = 카뱅 채팅방 띄워둔 상태에서 1원 실입금이 화면 감시로 잡히는지 최종 확인.**
- 파일: `public/apk/banknotify-020.apk`(신규).

## 2026-07-30 — 관리자: 예약 탭이 기본 화면 + 예약 행 한 줄 메모
- **예약 탭을 첫 탭·기본 화면으로**(사장님 지시). 탭 순서 = 예약·오늘·입금·환불·리뷰·설정, 로그인하면 바로 예약(날짜별)부터.
- **예약 행 한 줄 메모**(`MemoLine`): 날짜별·목록 두 화면 모두, 행을 펼치지 않고 시간·이름·전화 아래에서 바로 적는 메모. Enter/포커스아웃 시 저장, 30초 자동새로고침이 입력 중인 값을 덮지 않게 포커스 가드.
- ⚠️ **wp-import 복사본에는 메모 입력을 숨김** — 그 예약들은 memo 가 실시간 동기화의 열쇠(예약번호 #ID)라, 고치면 동기화가 못 알아보고 지웠다 다시 만들며 메모가 증발한다. 자체 예약(웹·전화)에만 표시. 운영 테스트 끝나고 복사본을 지우면 전 예약에서 메모 사용 가능.
- 파일: `src/app/admin/page.tsx`, `src/app/globals.css`(`.memoline`).

## 2026-07-30 — 입금 카톡알림 자동 매칭 가동 (BANK_DRY_RUN 해제)
- **`BANK_DRY_RUN=false`(Cloudflare)** — 이름+금액 정확 일치 1건이면 실제로 입금확인을 누른다. 로컬 .env.local 은 true 유지(로컬 실험 안전).
- **웹훅 매칭에서 wp-import 미러 제외**: 기존 사이트 복사본의 입금 상태는 5분 동기화가 기준이라, 웹훅이 같이 누르면 두 경로가 서로 덮어씀. `.or("source.is.null,source.neq.wp-import")` — ⚠️`.neq`만 쓰면 source가 NULL인 자체 예약까지 걸러져 자동확인이 전멸함.
- **라이브 E2E 검증**: 연습 예약(입금테스트/3만) 생성 → `/webhook/deposit`(→ rewrites로 `/api/bank/deposit`)에 태블릿과 동일 형식 POST → `decision:"approved"` → 예약 `deposit_paid:true·confirmed` 확인 → 정리(입금해제+취소). 문자는 연습번호라 차단(정상).
- **남은 것(사장님)**: 매장 태블릿 BankNotify 앱의 서버 주소를 Vercel 주소 → `https://fantastrick-homepage.tndn1102.workers.dev` 로 교체(시크릿은 그대로). 앱은 "주소+/webhook/deposit"으로 보내는데 next.config rewrites 가 받아준다.
- 파일: `src/app/api/bank/deposit/route.ts`.

## 2026-07-30 — 시간표를 실제(Booked 원본)와 일치시킴 (신부 평일·사자 금)
- **근거 수집**: 기존 사이트 DB에서 Booked 플러그인의 요일 기본값 원본(`booked_defaults_17/23/24/25`) + 최근 90일 실예약 분포(테마×요일×시각)를 직접 읽어 대조(임시 조사 스크립트, SELECT만·실행 후 삭제).
- **태초의 신부**: 평일이 7칸(12:00,14:00,15:20…)이 아니라 **월~금 전부 8칸**(12:00,13:20,14:40,16:00,17:20,18:40,20:00,21:20)이 실제 기본값. 이번 주 월~목의 7칸은 사장님이 Booked에 날짜별 예외로 줄인 것(다음 주 월·화·수 실예약이 이미 14:40·17:20 등으로 들어와 있음). → default를 8칸으로, 요일 예외는 주말만 남김. 칸을 줄이는 날은 관리자 › 시간대 마감으로.
- **사자의 서**: 금요일은 평일 그리드(12:30~)가 아니라 **주말 그리드**(12:00,13:10,14:20,15:30,16:40,17:50,19:00,20:10,21:20)가 실제. → byDow "5" 추가.
- 락다운시티·시간의 영속성은 전 요일 일치(변경 없음). 수정 후 가져오기 검사 = **실예약 115건 전부 시간표 안**(전엔 19건 불일치). 테스트 55개 통과, `cf:deploy` 라이브 반영 확인.
- ⚠️ DB(app_settings)에 theme_slots 키가 없어서 라이브 시간표 = 코드 `data.ts`의 THEME_SLOTS 폴백 — 시간표 수정은 코드 수정+배포로 한다(관리자 화면에서 저장하면 그때부터 DB가 우선).
- 파일: `src/lib/data.ts`.

## 2026-07-30 — 기존 사이트 예약 실시간 동기화 (목~일 운영 병행 테스트)
- **목적**: 목금토일 4일간 기존 사이트(fantastrick.co.kr)로 들어오는 실제 예약을 새 홈페이지에도 준-실시간 반영해 운영처럼 테스트.
- `import-from-wp.mts` 에 **`--sync` 모드 추가**: memo 속 예약번호(#ID)를 열쇠로 ①취소된 예약 삭제 ②날짜·시간·승인상태 바뀐 예약 수정 ③새 예약 추가. 워드프레스가 항상 기준(WP에는 여전히 SELECT만). 삭제→수정→추가 순서(uq_res_slot 칸을 먼저 비워야 옮긴 예약이 들어감).
- 읽기 범위를 `NOW()` → `CURDATE()` 로 넓힘: 당일 이미 시작한 회차가 동기화 중 "취소로 오인되어 삭제"되지 않게.
- **작업 스케줄러 `fantastrick-wp-sync` 등록**: 5분마다 `--apply --sync`, 2026-08-03 00:30 자동 종료. 로그 `wp-sync.log`(깃 제외). ⚠️ 이 PC가 켜져 있어야 동기화가 돈다(꺼지면 사이트는 살아있고 반영만 멈춤, 켜지면 재개).
- 5분마다 cmd 창이 깜빡이던 문제 → 작업을 `wp-sync-hidden.vbs`(wscript, 창 없음) 경유로 교체. 409 반복(7/29 20:00 시간의 영속성) → 운영자 테스트 예약(online·장유찬)이 칸을 차지하던 것, 취소 처리로 해소(uq_res_slot 은 취소건 제외).
- 첫 동기화: 옛 연습 데이터 102건 정리, 실예약 113건 반영, 재실행 시 0·0·0(멱등) 확인.
- 관리자 비밀번호를 사장님 지정 값으로 재교체(Cloudflare 시크릿 + .env.local).
- ⚠️ 발견: 목·금 실제 예약 시각 19건이 새 사이트 요일 시간표에 없음(예: 신부 목 13:20/14:40…) — 관리자 › 설정에서 목·금 시간표 보강 필요(동기화·표시는 문제 없음, 손님이 그 시각을 새로 고를 수 없을 뿐).
- 파일: `scripts/import-from-wp.mts`, `.gitignore`.

## 2026-07-30 — 실제 운영 테스트 준비 (NHN 문자만 보류)
- **관리자 비밀번호 임시 1234 → 강한 값으로 교체** (로컬 `.env.local` + Cloudflare 시크릿 동시. 값은 깃에 안 올림 — `.env.local` 참고).
- **최신 코드 라이브 배포** (`npm run cf:deploy`, 버전 e7ed74dd): 달력 '남은 N칸' 제거·NHN 전환·당일취소 개선 등 7/29 커밋들이 라이브 반영됨.
- **폐기 시크릿 정리**: Cloudflare 워커의 알리고 3개(`ALIGO_API_KEY`·`ALIGO_USER_ID`·`ALIGO_SENDER`) 삭제. 코드가 더 안 읽는 값.
- **라이브 검증**: 테스트 55개·프로덕션 빌드 통과. 라이브에서 주요 페이지 7곳 200 확인 + 연습용 번호(010-0000-9999)로 **예약 생성 → 조회 → 미입금 취소(환불 없음)** 전 흐름 실동작 확인. 새 비번 로그인 OK. 문자는 NHN 키 미등록이라 'skipped' 로그만 남음(의도대로).
- ⚠️ **가비아 문자는 못 씀** (사장님 질문 답): 가비아 API는 **발송 IP 사전 등록**이 필수인데 Cloudflare Workers는 나가는 IP가 매번 바뀜 — 알리고와 같은 이유로 탈락. NHN Cloud 유지.
- **남은 것(사장님)**: ① NHN Cloud 발신번호/알림톡 심사(보류 중) ② Vercel 대시보드에서 프로젝트 Pause(옛 크론 정지) ③ 매장 태블릿 BankNotify를 새 주소로 연결 후 `BANK_DRY_RUN=false` 전환(그 전까진 매칭만 하고 실제 승인 안 함) ④ 도메인 연결(맨 마지막).
- 함정 메모: dev 서버 켜진 채 `next build` 하면 `/api/admin/*` 모듈 못 찾는 빌드 실패 — dev 끄고 `.next` 지운 뒤 빌드.

## 2026-07-29 — 예약 달력의 '남은 N칸' 표시 제거 (사장님 지시)
- 달력 날짜 칸의 `N칸`/`마감` 숫자와 "숫자 = 그 날 남은 칸" 안내를 전부 뺐다. 달력은 다시 날짜 숫자만 보여준다.
- `ReserveCalendar` 의 `countFor` prop, `.rcal-n`·`.rcal-cell.soldout` 스타일, `ReserveClient` 의 `remainingFor()` 삭제.
- 이 기능 때문에 `blockedForDate()` 로 빼놨던 계산도 **원래대로 useMemo 안으로 되돌렸다**(호출부가 하나뿐이라 함수로 둘 이유가 없어짐). 마감·임박 시간칩 동작은 그대로.
- 파일: `src/components/ReserveCalendar.tsx`, `src/app/reserve/ReserveClient.tsx`, `src/app/globals.css`.

## 2026-07-29 — 문자 발송 솔라피 → NHN Cloud 전환
- **이유**: 솔라피는 **발신번호 등록**이 끝내 안 됐다(개인 명의·본인인증 완료 상태인데도 등록 단계에서 진행 불가). 발송 코드 문제가 아니라 계정 쪽 관문이라, 업체를 바꾼다.
- **왜 NHN Cloud 인가**: Cloudflare Workers 는 나가는 IP 가 매번 바뀐다 → **발송 서버 IP 등록이 필요한 업체는 원천적으로 못 쓴다**(알리고 "인증오류-IP", 뿌리오도 문서상 필수 `3003 invalid ip`). NHN Cloud 는 `appKey` + `X-Secret-Key` 두 개로만 인증한다.
- `src/lib/sms.ts` 재작성:
  - 문자 = `POST sms.api.nhncloudservice.com/sms/v3.0/appKeys/{appKey}/sender/sms|mms`
  - 알림톡 = `POST kakaotalk-bizmessage.api.nhncloudservice.com/alimtalk/v2.1/appkeys/{appkey}/messages`, `resendParameter.isResend=true` 로 **실패 시 문자 대체발송**(솔라피 `disableSms:false` 와 같은 역할)
  - 성공 판정은 `header.isSuccessful` 하나로 통일
- ⚠️ **솔라피와 다른 점 두 가지 — 안 맞추면 사고난다**
  1. **길이에 따라 경로가 갈린다.** 솔라피는 알아서 SMS/LMS 를 골랐지만 NHN 은 90바이트 초과 시 `/sender/mms`(title 필수)로 보내야 한다. 한글 2바이트(EUC-KR 기준)로 세는 `smsByteLength()` 추가 + `tests/sms-length.test.ts` 로 못 박음(UTF-8 3바이트로 세면 단문도 장문으로 나가 요금 3배).
  2. **템플릿 치환 키에 `#{}` 를 붙이지 않는다.** 솔라피는 `{"#{이름}": "홍길동"}`, NHN 은 `{"이름": "홍길동"}`. 어긋나면 치환이 안 된 채 `#{이름}님` 그대로 손님에게 나간다.
- env: `NHN_SMS_APPKEY·NHN_SMS_SECRET·NHN_SENDER` + (알림톡) `NHN_ALIMTALK_APPKEY·NHN_ALIMTALK_SECRET·NHN_SENDER_KEY·NHN_TPL_CONFIRM·NHN_TPL_CANCEL`. 솔라피·알리고 env 는 `.env.local` 에 주석으로 남김.
- **개인정보처리방침 위탁업체 수정**: `알리고` → `NHN Cloud`. 솔라피로 옮길 때 이 줄만 안 고쳐져 실제와 달랐다(위탁 고지는 실제 업체와 일치해야 함). 관리자 화면 안내 문구의 "알리고"도 함께 정리.
- **남은 것(사장님)**: NHN Cloud 가입 → 발신번호 등록 → 콘솔에서 appKey·SecretKey 발급. 값 주시면 `wrangler secret` 등록 + 발송 테스트.
- 파일: `src/lib/sms.ts`, `tests/sms-length.test.ts`, `src/app/privacy/page.tsx`, `src/app/admin/page.tsx`, `src/app/api/admin/sms/route.ts`, `.env.local`.

## 2026-07-29 — 배포처를 Cloudflare 하나로 정리 (Vercel 자동배포·크론 제거)
- **왜**: `git push` = Vercel 자동배포라 소스만 올려도 두 곳이 갈렸다. 라이브는 Cloudflare 하나로 간다.
- `vercel.json`에 `git.deploymentEnabled:{main:false}` → 앞으로 push 해도 Vercel 이 빌드하지 않는다. **git push 는 소스 보관용**, 배포는 `npm run cf:deploy` 하나.
- `vercel.json`의 `crons` 삭제. 주간 백업은 Cloudflare 워커 `fantastrick-cron-backup`(`cron-backup/`, 월 03:00 UTC)가 이미 맡고 있어 그대로 두면 **주 2회 + 옛 코드로 백업이 섞인다**.
- ⚠️ **설정만으론 안 끝난다**: Vercel 크론은 **그때 배포된 프로덕션에 붙어 있다.** 다시 배포하지 않는 이상 `vercel.json` 을 고쳐도 옛 배포의 크론은 계속 돈다 → **Vercel 대시보드에서 프로젝트를 Pause 하거나 Git 연결을 끊어야** 실제로 멈춘다(사장님 계정 필요).
- 파일: `vercel.json`.

## 2026-07-29 — 예약 화면(/reserve) 재설계: 포스터 배너 + [달력 | 시간] 2열
- **이유**: 기존은 `①지점 드롭다운 → ②테마 드롭다운 → ③달력 → ④시간` 4단계라, 손님이 **"이번 주 언제 되지?"를 끝까지 눌러봐야만** 알 수 있었다. 국내 방탈출 예약 화면 10곳(지구별·서울이스케이프·비트포비아던전·키이스케이프·셜록홈즈·제로월드·넥스트에디션 등) 원문을 조사해 보니, 지점을 먼저 묻는 건 **테마가 20~30개**라 한 화면에 못 담기 때문이었다. 우리는 **테마 4개 + 3지점 모두 강남** → 지점을 먼저 물을 이유가 없다.
- **바뀐 흐름**: `테마(포스터 카드 4장) → [달력 | 시간] 2열 → 인원 → 예약자 정보`. 지점 드롭다운 폐지(테마가 지점을 이미 알고 있음), 단계 열림(showDate/showTime) 폐지.
- **달력 = 월 전체 + "남은 N칸"**: 날짜 칸마다 그 날 예약 가능한 칸 수를 찍는다(`ReserveCalendar`에 `countFor` prop 추가). 누르기 전에 여유 있는 날을 고를 수 있다. 0이면 '마감'으로 표시하되 **막지는 않는다**(설정을 못 받아온 사이 잘못 계산되면 멀쩡한 날을 못 고르게 되므로). 계산은 시간칩과 **똑같은 규칙**(그 요일 시간표 − 마감·예약 − 임박)이고, 이미 받아오던 `/api/slots?all=1` 만 써서 **추가 API 호출이 없다**.
- **가로 배너**: 세로 포스터(2:3)를 폭 100%로 두면 휴대폰 첫 화면이 포스터만으로 차서 달력이 접힘선 아래로 밀린다 → 눕혀서(108px, 모바일 76px) 달력을 첫 화면 안으로 올렸다. 실측: 390×844에서 달력 상단 427px = 첫 화면 안.
- **예약금을 앞에서 보여줌**: 테마별 3만~12만으로 **4배 차이**인데 전에는 신청 버튼을 누른 뒤에야 보였다. 포스터 카드와 배너에 예약금·환불규정을 명시.
- **시간칩 접근성**: 기존 `.opt` 는 마감칸을 `opacity:.45` 로 흐리게 해 딥블루 위 대비 **3.43:1(AA 미달)** 이었다. 새 `.rv-slot` 은 투명도 대신 **색 + 빗금 + 취소선 + 아이콘 + 글자**로 알린다(색만으로 구분하지 않음). 터치 타겟 44px(coarse 포인터 48px).
- **지점색 주의**: 브랜드 3색은 이미 지점 식별에 쓰인다(1호점 `--brand` / 2호점 `--violet` / 3호점 `--blood`). 그래서 **마감을 빨강으로 쓰지 않는다**(3호점과 의미 충돌). 또 `--blood(#b20e19)`는 딥블루 위 대비가 2:1대라 `.site-dark` 에서 `#ff8a80` 으로 갈아끼운다.
- 파일: `src/app/reserve/ReserveClient.tsx`, `src/components/ReserveCalendar.tsx`, `src/app/globals.css`(`.rv-*` 블록 신설).

## 2026-07-27 — 문자 발송 알리고 → 솔라피(Solapi) 전환
- **이유**: 알리고는 **발송 서버 IP 화이트리스트**가 필수인데, Cloudflare Workers는 IP가 계속 바뀌어 등록 불가 → "인증오류-IP". 솔라피는 **API키+시크릿 HMAC 서명** 방식이라 IP 무관, Cloudflare에서 바로 작동.
- `src/lib/sms.ts` 재작성: `solapiSend()`(HMAC 헤더 + `POST api.solapi.com/messages/v4/send`). 알림톡은 `kakaoOptions{pfId,templateId,variables,disableSms:false}` 로 보내 **실패 시 솔라피가 SMS 자동 대체**(알리고 failover 대체). 변수는 `#{이름}#{테마}#{날짜}#{시간}`. 테스트번호 차단·sms_log·템플릿 로직은 그대로.
- env: `SOLAPI_API_KEY·SOLAPI_API_SECRET·SOLAPI_SENDER·SOLAPI_PFID·SOLAPI_TPL_CONFIRM·SOLAPI_TPL_CANCEL`. (알리고 env는 폐기, .env.local 주석)
- 가격 참고: 솔라피 알림톡 8원·SMS 13원(알리고와 큰 차이 없음). 카카오 알림톡 템플릿 2종(확정·취소)=`Desktop\판타스트릭_카카오알림톡_템플릿.txt`.
- **남은 것(사장님)**: 솔라피 가입·발신번호 등록·카카오채널 연동(pfId)·템플릿 심사(템플릿ID) → API키/시크릿 등 값 주면 내가 Cloudflare 시크릿 등록·발송 테스트.
- 파일: `src/lib/sms.ts`, `.env.local`.

## 2026-07-27 — 개인정보처리방침 + 예약 동의 (출시 법적 요건)
- **개인정보처리방침 페이지 신설** (`/privacy`): 실제 수집 항목(이름·전화·예약비번4자리·환불계좌·후기)·이용목적·보유기간(전자상거래법 5년/3년)·제3자제공·위탁(알리고·카카오·Supabase·Cloudflare)·마케팅 별도동의·정보주체 권리·보호책임자(대표 승현수·연락처). BIZ_INFO 연동. `.legal` 스타일.
- **예약폼에 개인정보 수집·이용 동의 체크박스**(필수): 최종 단계(showInfo, 제출 버튼 앞)에 추가. `agree` 상태 + 미동의 시 제출 차단(submit()에서 검사). /privacy 링크 포함.
- **푸터에 개인정보처리방침 링크**(`.foot-privacy`) 추가.
- 검증: /privacy 렌더 OK, 예약 최종 단계 체크박스 렌더 OK.
- 파일: `src/app/privacy/page.tsx`·`layout.tsx`, `src/app/reserve/ReserveClient.tsx`, `src/components/Footer.tsx`, `src/app/globals.css`.

## 2026-07-27 — 줄바꿈 정리 (내용 불변, PC·모바일 어색한 줄바꿈 수정)
- 문제: 모바일에서 `<스네이크>` 의 `<` 가 앞 단어와 분리돼 줄 끝에 기호만 남고, 소개·FAQ의 `<br>`이 모바일에선 "으로 구현해," 같은 조각을 외톨이 줄로 남김.
- ① **`.nb`(white-space:nowrap)**: `<이름>`·`[SYSTEM]` 같은 짧은 괄호 묶음을 통째 유지. 방 상세 시놉시스/주의사항에 `nb()` 헬퍼로 적용(`rooms/[id]/page.tsx`). (word-break:keep-all 은 `<`+한글 경계 줄바꿈을 못 막음)
- ② **`text-wrap:pretty` 전역**(body): 마지막 줄 단어 하나 외톨이(위도우) 완화. 미지원 브라우저 무해.
- ③ **`pc-br`**: `@media(max-width:600px){br.pc-br{display:none}}`. 내가 넣은 문단 그룹핑 `<br>`을 pc-br로 바꿔 **PC는 줄바꿈 유지, 모바일은 자연스럽게 이어짐**. JSX가 태그 옆 공백을 지워 붙는 문제 → `<br>` 앞에 공백 추가. 대상: about(브랜드스토리·직영제작·마스코트·히어로), faq(예약변경·지각). business h1·footer의 의도적 2줄은 유지.
- 파일: `src/app/globals.css`, `src/app/rooms/[id]/page.tsx`, `src/app/about/page.tsx`, `src/app/faq/page.tsx`.

## 2026-07-27 — 이벤트 탭에 '생일 이벤트' 추가 (상시진행)
- 이벤트 탭에 **생일 이벤트 카드**(3번째) 추가. 내용: 테마 이용 시 동의서에 마케팅 수신 동의한 분께 생일 달 1일에 **전 테마 5,000원 할인 쿠폰**을 문자 발송, 사용기한=생일 해당 월 1일~마지막 날, 원하는 테마 사용 가능. 상시진행 배지.
- **메인 이미지 = 제미나이(Nano Banana 웹 UI 자동화)로 생성**: 판타·트리키가 고깔모자 쓰고 생일 케이크·풍선·선물로 축하하는 딥블루 프리미엄 장면(텍스트 없음). `public/images/event-birthday.png`(1080²). 카드 썸네일 + 팝업 헤더에 적용.
- 파일: `src/app/events/page.tsx`(BIRTHDAY 상수+카드+팝업, open 타입에 "birthday" 추가), `public/images/event-birthday.png`.

## 2026-07-26 — Cloudflare Workers 이전 (OpenNext) — Vercel 한도 회피
- **이유**: Vercel 무료 한도. → Cloudflare Workers 무료 한도가 넉넉해 이전.
- **방법**: `@opennextjs/cloudflare` 어댑터로 Next.js 15를 Cloudflare Workers에 배포. `wrangler.jsonc`(nodejs_compat·global_fetch_strictly_public·ASSETS·IMAGES·WORKER_SELF_REFERENCE), `open-next.config.ts`, `next.config.ts`에 `initOpenNextCloudflareForDev()` 추가. 배포 스크립트 `npm run cf:deploy`(=opennextjs-cloudflare build && deploy).
- **라이브**: https://fantastrick-homepage.tndn1102.workers.dev (전 페이지·이미지·Supabase API·관리자 1234·백업까지 검증 완료). Vercel(fantastrick-homepage.vercel.app)은 당분간 병행 유지.
- **시크릿**: `wrangler secret bulk`로 6개(SUPABASE_URL·SERVICE_ROLE_KEY·ADMIN_PASSWORD·CRON_SECRET·BANK_WEBHOOK_SECRET·BANK_DRY_RUN) 등록.
- **크론**: OpenNext 워커엔 scheduled가 없어 **별도 크론 워커 `fantastrick-cron-backup`**(`cron-backup/`)가 매주 월요일 메인 앱 `/api/cron/backup` 호출. (Vercel cron도 남아있어 병행 중엔 주 2회 백업 — 무해, 12개 초과분 자동삭제)
- ⚠️ **배포 방법 2개로 갈림**: git push → Vercel(자동), `npm run cf:deploy` → Cloudflare(수동/내가). `initOpenNextCloudflareForDev()`는 Vercel 빌드엔 무영향.
- **남은 것**: 공개 도메인을 Cloudflare로 연결(=진짜 전환, 도메인 붙이는 맨 마지막 단계). 그 전까진 두 URL 병행.
- 파일: `wrangler.jsonc`, `open-next.config.ts`, `next.config.ts`, `package.json`, `.gitignore`, `cron-backup/worker.js`, `cron-backup/wrangler.toml`.

## 2026-07-26 — 자동 백업 (매주 1회 · Supabase Storage)
- **저장 위치 = Supabase Storage 비공개 버킷 `backups`** (이미 쓰는 Supabase라 새 계정·설정 0, 무료 1GB, JSON은 KB단위. 실수 삭제 복구용). ⚠️ 한계: Supabase 프로젝트 통째로 유실 시 백업도 같이 감 → 필요하면 나중에 이메일/GitHub 사본 옵션 추가.
- 구조: `src/lib/backup.ts`(공유 buildBackup) → 수동 다운로드(`/api/admin/backup`)와 자동 백업이 같은 로직. `/api/cron/backup`(GET/POST)이 백업 JSON을 만들어 Storage에 업로드 + 최근 12개만 보관(오래된 것 삭제). Vercel Cron `vercel.json`에 **매주 월요일 03:00 UTC(=월 12시 KST)** 등록.
- 인증: Vercel Cron은 `Authorization: Bearer ${CRON_SECRET}`, 관리자는 세션으로 수동 "지금 백업 실행" 가능. 목록 조회 = `/api/admin/backups`(서명 URL 30분).
- 관리자 UI: 설정 › 전체 백업 카드 아래 **자동 백업 패널**(설명 + "지금 백업 실행" + 최근 백업 목록·받기).
- 로컬 테스트 통과(backup 파일 업로드·목록·다운로드 확인).
- ⚠️ **배포 시 필요**: Vercel 환경변수 `CRON_SECRET` 존재해야 크론 인증됨. 크론은 **프로덕션 배포에서만** 동작. (Hobby 플랜은 크론 하루 1회·주간 OK)
- 파일: `src/lib/backup.ts`, `src/app/api/cron/backup/route.ts`, `src/app/api/admin/backups/route.ts`, `src/app/api/admin/backup/route.ts`(리팩터), `src/app/admin/page.tsx`, `vercel.json`.

## 2026-07-25 — 관리자 로그인: 아이디 없이 비밀번호만
- 관리자(/admin) 로그인을 **아이디 없이 비밀번호만**으로 변경(사장님 요청). UI에서 아이디 입력칸 제거, 라우트에서 아이디 검사 제거(비밀번호 상수시간 비교는 유지).
- 비밀번호는 **환경변수 `ADMIN_PASSWORD`** 값. **로컬(.env.local)은 임시 `1234`로 설정**(⚠️ 실사용 전 어려운 값으로 변경 예정).
- ⚠️ **라이브(Vercel) 반영**: 배포 시 Vercel 환경변수 `ADMIN_PASSWORD`도 `1234`로 설정해야 라이브에서 1234로 로그인됨(안 바꾸면 기존 비번 유지). `ADMIN_ID`는 이제 미사용.
- 파일: `src/app/admin/page.tsx`, `src/app/api/admin/login/route.ts`, `.env.local`(git 미포함).

## 2026-07-25 — 리뷰 이벤트 카드 메인 사진 교체(트리키 → 리뷰이벤트 포스터)
- 이벤트 탭 리뷰 카드의 메인 사진을 트리키 마스코트 → **리뷰 이벤트 포스터**로 교체. 사장님이 준 원본(화면 촬영본, "1만원 상품권 증정")을 **HTML→스샷으로 인스타 정사각 그래픽 재제작**하되 내용을 **"테마 5,000원 할인 + 굿즈 증정"**으로 변경(쿠폰 티켓 + 참여 3단계 + 로고). 한글 깨짐 없이 깨끗하게.
- 파일: `public/images/review-event.png`, `src/app/events/page.tsx`(리뷰 썸네일 + **팝업 헤더도 포스터로 교체**, 아래엔 읽기 쉬운 참여방법 텍스트 유지), `src/app/globals.css`(`.ev-modal-poster`).

## 2026-07-25 — 마스코트 새 디자인 반영(판타·트리키) + 트리키 게임패드 유지
- 사장님이 준 **새 마스코트 디자인 시트**(판타=흰셔츠+오렌지백팩+파란캡·손흔들기 / 트리키=흰셔츠+파란망토+회색귀밴드)를 **히어로·소개탭·이벤트 리뷰**에 반영. 트리키는 요청대로 **게임패드 든 모습 유지**.
- 제작: 디자인 시트에서 히어로 포즈 크롭 → **Gemini 웹 UI(무료, Playwright 자동화)** 로 전신·투명 벡터 버전 생성(트리키는 프롬프트에 게임패드 명시). Nano Banana가 투명을 체커무늬로 그려서 **flood-fill 후처리로 진짜 투명화**. Next 이미지 캐시 우회 위해 `mascot-fanta-v4.png`·`mascot-tricky-v4.png`.
- 파일: `public/images/mascot-fanta-v4.png`, `public/images/mascot-tricky-v4.png`, `src/app/page.tsx`(히어로), `src/app/about/page.tsx`, `src/app/events/page.tsx`. (구 mascot 이미지·v2·v3는 미사용 — 나중에 정리 가능)

## 2026-07-25 — 문장 속 대시(—) 제거 + 소개 마무리 CTA 문 배너 이미지
- **문장 속 "—" 전부 제거**(화면에 렌더되는 본문만; 시간범위 `11:00–23:30`·전화번호·탭 제목 구분자·리뷰 평점 자리표시 "—"는 문장이 아니라 유지). 대상: 소개(브랜드스토리·철학·마스코트), 이벤트 요약, FAQ(이름·값→`·`), 홈(매장 리드·지도 alt), 비즈니스, 예약 안내, `data.ts` 테마 부제(→괄호), `theme-content.ts`(SYSTEM ONLINE·머더룸 안내).
- **소개 마무리 CTA("문을 열 준비가 되셨나요?")** 배경을 저퀄 CSS 문 → **문 배너 이미지로 교체**. 1차 시네마틱 사원 문은 "유치"하다는 피드백 → **미니멀·세련·웅장** 버전으로 재생성(딥네이비 대형 문간에 은은한 빛, 건축적·심플). 무료 이미지 생성(Pollinations.ai Flux, 비용 0). 파일 `public/images/about-door-v2.jpg`(Next 이미지 캐시 우회 위해 v2 파일명). 직사각형을 불투명 cover로 채우고 어두운 스크림+글자 그림자로 가독성 확보. 기존 `.afd-*` CSS 제거.
- 파일: `src/app/about/page.tsx`, `src/app/events/page.tsx`, `src/app/faq/page.tsx`, `src/app/page.tsx`, `src/app/business/page.tsx`, `src/app/reserve/ReserveClient.tsx`, `src/lib/data.ts`, `src/lib/theme-content.ts`, `src/app/globals.css`, `public/images/about-door-v2.jpg`.

## 2026-07-25 — FAQ 정비 (지시서)
- 자주 묻는 질문(/faq): ① "예약 확인·변경" 답변을 **"예약 조회에서 1회 직접 변경 가능, 1회 초과 시 매장 전화"**로 수정(실제 예약변경 모달 지원 반영) ② "늦으면?" 답변 문장 줄바꿈 ③ "몇 명까지?"에 맨 아래 작은 글씨로 **"1인 이용 시 2인 가격 결제 후 플레이 가능"** 추가 ④ 주차 안내는 공통 `BOOKING_INFO.parking` 사용이라 언구비공영주차장 자동 반영.
- 파일: `src/app/faq/page.tsx`.

## 2026-07-25 — 테마 상세/이벤트 정비 (사장님 지시서)
- **테마 상세(/rooms)**: ① 입금계좌 박스(`.rm-acct`)가 다크 테마에서 안 보이던 문제 수정(원인=`--bg2` 토큰이 `.site-dark`에 누락→밝은 배경 유지. `.site-dark`에 `--bg2` 다크값 추가로 `.rm-acct`·`.refund-box` 동시 해결, 4테마 공통) ② 주차 안내에 **언구비공영주차장 추가**(4테마 공통) ③ 락다운시티 금액 라벨 `3인→3인 이하`·`4인→4인 이상` ④ 시간의 영속성 `(방탈출 테마가 아닙니다)`→`(방탈출 테마와 상이합니다)`.
- **이벤트(/events)**: ① 리드 "판타스트릭에서 진행 중인 이벤트입니다."로 변경 ② 포토카드에 **상시진행 배지**(초록, 아이콘) 추가(옵저버·리뷰 모두 상시) — 마감형(앰버 달력 배지)도 데이터로 지원 ③ 옵저버 유의사항 "옵저버는 같이 플레이해도 되고 관전만 해도 괜찮습니다."로 변경(신부·사자의서·락다운시티, 시간의 영속성은 유지) ④ 리뷰 이벤트: "드립니다", "블로그 주소 첨부", 굿즈 상세("전 지점 원하는 테마 사용 가능, 굿즈는 TGC점 테마 키링 중 택 1").
- 파일: `src/lib/theme-content.ts`, `src/app/events/page.tsx`, `src/app/globals.css`.

## 2026-07-25 — 소개 페이지 문구 정비 + 매장 전화번호 + '살짝 열린 문' 배경
- 소개(/about) 문구 수정: 히어로·브랜드스토리 줄바꿈 정리, "직접 사는 이야기"→"직접 경험하는 이야기", 철학 "방은…오래 남습니다"→"테마는…여운이 남습니다", 마스코트 "그대로 닮았습니다"→"그대로 대표합니다".
- 매장 섹션: 리드 축약("강남역·신논현역 근처."), **각 매장 카드 하단에 전화번호 추가**(1호점 010-4547-0481 · 2호점 010-4995-0482 · TGC 010-5536-0483, `.ast-tel`).
- 마무리 CTA: 위 별표(IconStar) 제거 + **배경에 '살짝 열린 문틈으로 따뜻한 빛이 새어나오는' 연출**(CSS로만, 이미지·저작권 없이 — `.af-door`/`.afd-*`). 텍스트엔 그림자로 가독성 확보.
- 파일: `src/app/about/page.tsx`, `src/app/globals.css`.

## 2026-07-25 — 예약 팝업 투명·계좌 안 보임 수정 (다크 테마)
- 다크 테마에서 `--surface`가 거의 투명(흰색 5%)이라 **모든 `.modal` 팝업(예약금 입금·예약 취소·변경·조회결과)이 투명하게 떠 뒤 내용과 겹쳐** 보이던 문제 → `.site-dark .modal` 불투명 딥블루 + 오버레이 딤 강화로 한 번에 해결.
- **예약금 입금 팝업의 계좌 박스**가 밝은 배경(#eef2f8)+밝은 글자라 계좌번호·예금주가 안 보이던 문제 → 다크 유리 배경+밝은 글자로 수정. 공지 팝업(흰 배경) 버튼 대비도 보강.
- 파일: `src/app/globals.css`.

## 2026-07-24 — 이벤트 탭: 옵저버 카드 → 팝업 안에서 테마 4장 넘겨보기
- `/events` 를 인스타 포토카드 게시판으로 구성. 그리드에는 **"옵저버 제도" 카드 1개**(썸네일은 테마 포스터 4장 2x2 콜라주 + "테마 4" 배지) + **"리뷰 이벤트" 카드 1개**.
- **옵저버 카드를 누르면 팝업이 열리고, 그 팝업 안에서 테마 포스터 4장**(태초의 신부·사자의 서·락다운시티·시간의 영속성)을 **‹ › 화살표·키보드 좌우·손가락 스와이프·점(도트)**으로 하나씩 넘겨봄(하단 "1 / 4" 카운터). 각 장 = 포스터 헤더 + 그 테마 규칙/유의사항 + "이 테마 보기" 버튼.
- 옵저버 규칙: 신부·사자의서=신규 2인과 오면 옵저버 무료(주말·공휴일 제외) / 락다운시티=인원 무관 무조건 3인 가격(주말·공휴일 포함) / 시간의 영속성=신규 3인과 오면 본인 무료(주말·공휴일 포함). 리뷰 이벤트=후기 남기면 테마 5,000원 할인 + 굿즈.
- 파일: `src/app/events/page.tsx`(재구성), `src/app/globals.css`(`.ev-poster`/`.ev-shade`/`.ev-nav`/`.ev-poster-head`/`.ev-collage-grid`/`.ev-dots`/`.ev-modal-count`).

## 2026-07-23 — 테마 갤러리 원복 + 전 페이지 다크 테마 통일

**① 테마 갤러리 원복**: cantor8식 핀 고정 가로스크롤 갤러리(ThemeGallery)를 접고, **원래 드래그+화살표+스크롤바 캐러셀(ThemeCarousel)**로 되돌림. (여러 번 튜닝했지만 레퍼런스 느낌이 안 나 사용자 요청으로 원복)

**② 전 페이지 다크(파란) 테마 통일**: 홈에만 있던 `.home-dark`가 다른 페이지는 하얀색이라 이질감 → 전 페이지로 확장.
- `.home-dark` → `.site-dark` 로 이름 변경. **`ThemeShell`(신규 클라이언트 컴포넌트)** 가 layout 에서 `{children}`을 감싸 **관리자(/admin) 외 모든 페이지**에 `.site-dark` 적용. 대부분 컴포넌트가 토큰 기반이라 자동 다크화(폼·표·카드·아코디언 포함).
- **헤더**: 전 페이지 최상단에서 흰색(over-hero), 스크롤 시 흰 배경. **푸터**: 전 페이지 다크(foot-dark).
- **커스텀 히어로**(하드코딩 밝은 배경이라 자동 안 됨) 개별 오버라이드: `.about-hero`(+ah-mesh/ah-grid), `.biz-hero .bg::after`(사진 위 딥블루 오버레이), `.room-hero .rh-veil`(딥블루 베일).
- `themeColor` 파랑(#062a80)으로.
- 빌드 통과, about/faq/reserve/reservation/business/reviews/rooms + 홈 전부 다크·헤더/푸터·폰트 통일 확인.

## 2026-07-23 — 폰트 그로테스크 통일(General Sans) + 테마 갤러리 cantor8식 재구성

**① 폰트**: cantor8 본문 폰트는 "PP Neue Montreal"(상업용 **유료**)이라, 무료 대체 **General Sans**(Fontshare, 상업용 무료)로 적용.
- `layout.tsx` 에 Fontshare 스타일시트 링크 추가. `globals.css` 에 `--font-sans:"General Sans", <Pretendard 스택>` 정의 → `body` 폰트로. **라틴은 General Sans, 한글은 자동으로 Pretendard 폴백**.
- 홈(`.home-dark`)은 제목의 세리프(나눔명조)·점수의 Bodoni 도 그로테스크로 통일(`--font-display:var(--font-sans)`). cantor8 처럼 전부 산세리프. (FANTASTRICK 워드마크 로고 폰트는 유지)

**② 테마 갤러리**(`page.tsx` ThemeGallery 신규, 옛 ThemeCarousel 대체): cantor8 "Product Suite" 섹션 참고.
- 카드가 **위아래로 엇갈려**(odd 위/even 아래) 가로로 늘어서고, **세로 스크롤(휠)에 따라 좌로 흐른다**.
- 방식: 섹션을 `(뷰포트+트랙 넘침)` 만큼 높게 잡고 안쪽을 `position:sticky` 로 고정 → 세로 스크롤 진행률을 트랙 `translateX` 에 매핑(핀 고정 가로스크롤). 제목은 고정.
- 모바일·모션최소: 핀 없이 **네이티브 가로 스와이프**(엇갈림 해제)로 폴백.
- 옛 캐러셀 CSS(.theme-carousel 등)는 미사용 상태로 남겨둠(참고용).
- 빌드 통과, 데스크톱(엇갈림·가로스크롤)·모바일(스와이프)·폰트 확인.

## 2026-07-23 — 히어로 성좌망 애니메이션 + 섹션 전환효과 강화 (cantor8식 "살아있는" 모션)

색만 맞추니 레퍼런스(cantor8)의 움직임이 없다는 피드백 → 실제 모션 추가.

- **히어로 성좌망을 정지 배경이미지 → 인라인 SVG 컴포넌트(`HeroWeb.tsx`)로 교체**. 이제 실제 애니메이션: ①선이 좌표를 따라 **그려지고**(draw-in, `pathLength=1`+`stroke-dashoffset`) ②노드가 **반짝이고**(twinkle, 스태거 딜레이) ③전체가 **느리게 흐른다**(drift 42s). 좌표는 스크립트로 생성(결정적 시드).
  · 기존 `globals.css` 의 7KB 정지 data-URI 는 제거(정규식으로 `.ht-web` 규칙만 안전하게 치환).
- **섹션 전환 강화**: 스크롤 등장(reveal)에 부드러운 **blur-in**(5px→0) 추가, 섹션 제목 위 미니룰이 **좌→우로 그려지는** 전환.
- reduced-motion(모션 줄이기) 설정 시 전부 정지 상태로 폴백.
- 빌드 통과, 히어로 모션(그리는 중/완료 프레임)·섹션 배경 정상 확인.

## 2026-07-23 — 첫 페이지 전체를 다크 테마로 통일 (cantor8.io 톤 전면 적용)

히어로만이 아니라 **첫 페이지 전 섹션(히어로→테마→후기→제작→오시는길→푸터)** 을 하나의 딥블루로 이어지는 모노톤으로 통일. 레퍼런스(cantor8) 스타일을 페이지 전체로 확장.

- **방식(핵심)**: `page.tsx` 홈 전체를 `<div className="home-dark">` 로 감싸고, `globals.css` 에 `.home-dark` 스코프에서 **CSS 토큰(색 변수)만 다크로 갈아끼움**(`--text/--muted/--surface/--line/--cyan/--gold` 등). 대부분 컴포넌트(테마카드·후기·제작·매장)가 토큰 기반이라 자동으로 다크로 따라옴 → 컴포넌트별 개별 수정 최소화.
  · ⚠️함정: `.home-dark` 에 `color:var(--text)` 를 **명시**해야 함. 안 하면 색 없는 제목(h2.title)이 상위 body 의 어두운 --text 를 상속받아 파랑 위에서 안 보임(lead 처럼 색을 직접 준 것만 밝게 나옴).
- **개별 오버라이드**(하드코딩 밝은 배경): `.block.alt`(옅은 흰 틴트), `.biz`(다크 글로우), `.store`(유리 패널), `.tcard.soon` 썸네일, `.btn.gold-ghost`(밝은 라벤더), `.notice.info`, 별점 골드 밝게. 섹션 여백↑(116px)·제목 크게(clamp 30~56px)로 cantor8식 여백감.
- **푸터**: `Footer.tsx` 에서 홈(`pathname==="/"`)일 때만 `foot-dark` 클래스 → 딥블루 푸터 + 흰 로고. 다른 페이지는 기존 밝은 푸터 유지.
- 프로덕션 빌드 통과, 데스크톱·모바일 전 섹션 렌더 확인(가독성·유리카드·지도·푸터 정상).

## 2026-07-23 — 히어로 배경 리디자인: 대담한 파랑 단색 + 성좌 단서망 (cantor8.io 참고)

첫 화면 배경을 밝은 흰색(옛 세 겹 조명)에서 **브랜드 파랑 한 색으로 꽉 채운 대담한 배경**으로 교체. 밋밋함 해소가 목표.

- **참고**: 사장님이 준 레퍼런스 <b>cantor8.io</b>를 brand-hero-visual-designer 에이전트로 분석 → "대담한 단색 채움 + 시선 끄는 라인아트 1개 + 여백" 공식만 차용(색·중앙주인공은 우리 걸로). 파랑은 우리 대표색이라 유지.
- **배경**(`.hero-t`): `radial-gradient`로 가운데 밝고(무대조명) 가장자리 딥한 파랑 비네트. 거의 단색.
- **성좌 단서망**(`.ht-web` 신규): cantor8 네트워크 그래픽을 방탈출 "흩어진 단서를 잇다"로 번안 — 흰 점+선 SVG(스크립트로 좌표 생성, data URI). 중앙(워드마크 자리)은 마스크로 비우고 가장자리로 갈수록 보이게, 천천히 페이드인.
- **어두운 배경 대응**: 조명·격자(흰 저투명)·고스트글자·그레인 블렌드 조정, 슬로건/eyebrow/워드마크(FANTAS=밝은 하늘파랑·TRICK=밝은 코랄)/rule/스크롤안내 전부 밝은 톤으로, 마스코트에 색 글로우(판타=파랑·트리키=빨강)로 실루엣 분리, 보조 CTA(외주문의) 라벤더로.
- **헤더**(`Header.tsx`): 홈 최상단(스크롤 전)엔 `over-hero` 클래스 → 로고 흰색화 + 메뉴/버튼 흰색. 스크롤하면 기존 흰 배경 헤더로 복귀.
- 데스크톱·모바일 렌더 확인(가독성·마스코트·헤더 흰색화 정상).

## 2026-07-23 — 회사 소개 페이지(`/about`) 신설 + "소개" 메뉴

헤더 메뉴 맨 앞에 **소개**(`/about`) 추가하고, 회사 소개 페이지를 새로 만듦.

- **리서치**: market-analyst 에이전트로 판타스트릭 실제 정보(자체 데이터+웹) 수집 + 타사 소개페이지(키이스케이프·넥스트에디션·제로월드) 벤치마크 → 검증된 카피만 사용.
- **제작**: frontend-nextjs-engineer 에이전트로 `src/app/about/page.tsx`(서버 컴포넌트)·`layout.tsx`(metadata)·`RevealOnScroll.tsx`(스크롤 등장) 생성, `globals.css`에 `.about-*` 추가(라이트 테마·기존 변수 재사용). 마스코트 이미지 활용.
- **섹션**: 히어로(슬로건) → 브랜드스토리(Fantasy+Trick) → 철학(진짜라고 느껴질 때까지) → 핵심가치 4(몰입·디테일·직영제작·이야기) → 직영 3축(콘텐츠·공간·기술) → 숫자(매장3·테마6·3축·4.4★) → 매장3 → 마스코트(판타&트리키) → CTA.
- ⚠️**미확인이라 일부러 뺀 정보**(사장님 확인 필요): **창업연도/연차("11년")·"하이퍼리얼리즘" 공식표현·수상이력**. 확정되면 히어로 SINCE·숫자·연혁에 추가 가능. 잼핏 평점(4.4★, 163건)은 인용정책 확인 권장.
- 빌드·타입·린트 통과, 데스크톱·모바일 렌더 확인(가로 오버플로 0).

## 2026-07-23 — 히어로 배경에 마스코트 판타·트리키 추가

브랜드 마스코트(판타=파란 판다+자물쇠, 트리키=코랄 레서판다+게임패드)를 히어로 양쪽에 배치. 색이 히어로 타이포 FANTASY(파랑)·TRICK(빨강)과 딱 맞음.

- **에셋 = AI 전신 일러스트 생성**: TGC 매장 간판 사진에서 두 캐릭터를 크롭 → 그걸 레퍼런스로 <b>Gemini(gemini-3-pro-image, 무료 웹 UI)</b>에 넣어 "전신 버전"을 생성. `public/images/mascot-fanta.png`·`mascot-tricky.png`(투명 PNG).
  · ⚠️Gemini "투명 배경"이 파일엔 흰색/체크무늬로 구워져 나옴 → sharp <b>flood-fill</b>(모서리부터 배경만 제거, 캐릭터 내부는 외곽선으로 보호)로 진짜 투명화. 트리키는 체크무늬 두 톤이라 "저채도 회색" 판정으로 제거.
- **배치**: `page.tsx` 히어로에 `.ht-mascot`(판타 좌·트리키 우) 추가. `globals.css`에 CSS — 하단 절대배치, z-index 3(텍스트 z:5보다 뒤), 등장(htMasc)+플로팅(htFloat) 애니메이션, 모바일 축소(132px), pointer-events:none, reduced-motion 대응.
- 데스크톱·모바일 렌더 확인(텍스트 안 가림, 투명 정상).

## 2026-07-22 — 홈 섹션 제목 최종: 영문 병기(5번) + eyebrow 제거

23번(프로 스튜디오)에서 <b>1차 5번(영문 병기)</b>으로 최종 변경 + 각 섹션 위 영문 라벨(eyebrow) 삭제(제목에 영문이 들어가 중복이라).

- 테마: <b>테마 · Themes</b> (부제 "직영으로 만든 방탈출·머더룸" 유지)
- 후기: <b>후기 · Reviews</b>
- 비즈니스: <b>제작 · Production</b> (부제·CTA 유지)
- 매장: <b>오시는 길 · Location</b> (부제 유지)
- 제거한 eyebrow: INTERACTIVE CONTENTS · REVIEWS · BUSINESS · B2B · STORES · 오시는 길. (히어로의 PREMIUM ESCAPE ROOM 은 제목이 아니라 유지.)
- 렌더 확인: 4개 섹션 제목 정상, eyebrow 제거 후 간격 자연스러움, 콘솔 에러 0.

## 2026-07-22 — 홈 섹션 제목 리카피 (오글거림 제거 → 프로 스튜디오 톤)

기존 제목이 2인칭 감성·시적 은유·동사나열이라 오글거린다는 지적 → 경쟁사(키이스케이프 등) 톤 분석 후 30개 시안 제시, 사장님이 <b>23번(프로 스튜디오 톤)</b> 선택. 홈(`page.tsx`) 4개 섹션 제목 교체:

- 테마: "당신이 만들어가는 이야기" → <b>테마 라인업</b> (부제도 "테마 목록"→"직영으로 만든 방탈출·머더룸")
- 후기: "다녀간 이들이 남긴 이야기" → <b>플레이어 리뷰</b>
- 비즈니스: "짓고, 만들고, 움직이게 합니다" → <b>제작 스튜디오</b>
- 매장: "강남에서 만나는 세 개의 무대" → <b>지점 안내</b>
- 영문 eyebrow(INTERACTIVE CONTENTS·REVIEWS…)와 나머지 부제는 유지. 시안 문서: `Desktop\판타스트릭_섹션제목_10샘플.html`·`_추가20샘플.html`.

## 2026-07-22 — 모바일 메뉴(드로어) 삼성 인터넷 렌더 버그 수정

가끔 오른쪽 위 메뉴(햄버거)를 열면 <b>"콘텐츠" 하나만 뜨고 나머지 메뉴가 안 보이던</b> 문제(사장님 삼성 인터넷 브라우저).

- **원인**: 드로어가 `display:none`→`block` 전환과 동시에 `@keyframes` 애니메이션을 돌리는 구조였음. 이 조합이 <b>삼성 인터넷(Samsung Internet)에서 패널의 첫 항목만 그려지고 나머지 자식이 안 그려지는</b> 렌더 버그를 일으킴(간헐적).
- **수정**: 드로어를 <b>항상 DOM에 두고 `.open` 클래스로 여닫는 transition 방식</b>으로 변경(`visibility` + `transform`/`opacity` 트랜지션). 자식이 항상 그려져 모든 브라우저에서 안정적. 슬라이드 애니메이션·스크림·ESC/스크롤잠금 동작은 그대로.
- 안 쓰게 된 `@keyframes fadeIn/slideIn` 제거. `src/app/globals.css`의 `.drawer` 계열만 변경.
- 검증: 모바일 에뮬레이션에서 홈·B2B·스크롤·빠른 여닫기 모두 5개 항목 정상 표시. (원 버그는 삼성 인터넷 전용이라 크롬에선 재현 안 됐지만, 근본 원인인 display+keyframe 조합을 제거함.)

## 2026-07-22 — 한글 줄바꿈 정리(word-break: keep-all) — 단어 중간 잘림 방지

테마 상세 등에서 한글이 "매장" → "매/장", "거리" → "거/리"처럼 <b>단어(어절) 한가운데서 잘려</b> 해상도마다 줄바꿈이 제각각이던 문제를 고침(사장님 지적).

- `globals.css`의 `body`에 <b>`word-break: keep-all`</b> 전역 적용 → 한글 어절은 통째로 유지되고 <b>띄어쓰기에서만</b> 줄바꿈. 모든 해상도에서 일관된 줄바꿈, 외로운 한 글자 방지.
- 안전장치로 `overflow-wrap: break-word` 함께 둠 → 계좌번호·URL 같은 긴 토큰이 칸을 넘칠 때만 끊어 가로 스크롤(오버플로) 방지.
- 검증: 360·412·768px에서 렌더 확인 — 단어 중간 잘림 사라짐, 가로 오버플로 0, 콘솔 에러 0.

## 2026-07-22 — 미입금 예약 처리 정리 (시간변경 차단 + 취소 시 100% 환불 안내 제거)

미입금(예약금 입금 전) 예약에서 두 가지를 바로잡음(사장님 지적).

- **시간 변경은 입금 확정된 예약만 가능**: 미입금 대기 건은 30분 자동취소 대상이라 옮겨봤자 사라질 수 있어 혼란. 이제 손님 화면 [시간 변경] 버튼은 <b>입금 확정 예약에만</b> 뜨고, 미입금이면 "예약금 입금이 확인되면 시간 변경이 가능해요" 안내. 서버(`change/route.ts`)도 `deposit_paid` 아니면 409로 막음.
- **🔴미입금인데 취소 시 "100% 환불" 안내가 나가던 버그 수정**: 입금도 안 했는데 취소 팝업이 "전액(100%) 환불됩니다" + 환불 계좌 입력을 강요했음(돌려줄 돈이 없는데). 이제 미입금 취소는 <b>환불 안내·계좌 입력 없이 "환불 없이 바로 취소"</b> 간단 확인만. 서버(`cancel/route.ts`)도 미입금이면 계좌 요구 안 하고 `refund_rate=null`(환불 없음)로 저장, 이력엔 "미입금 취소(환불 없음)".
- 입금 확정된 예약의 취소·환불 흐름(100/80/0%)은 그대로 유지.
- 바뀐 파일: `src/app/api/reservations/cancel/route.ts`·`src/app/api/reservations/change/route.ts`·`src/app/reservation/page.tsx`. 검증: 타입검사·빌드 통과 + 브라우저 E2E(미입금/입금완료 예약 각각 취소·시간변경 UI 확인, 콘솔 에러 0).

## 2026-07-22 — 예약금 팝업에 "바로 송금" 버튼 (토스·카카오뱅크·계좌복사 하이브리드)

예약 접수 후 뜨는 <b>예약금 입금 안내</b> 팝업에서, 손님이 계좌를 손으로 옮겨 적지 않고 <b>바로 송금</b>할 수 있게 함.

- **버튼을 [확인] 하나 → [닫기] + 송금 방법들**로 바꿈. "위 내용을 확인했습니다" 체크하면 송금 버튼이 열림.
- **휴대폰**: ①<b>토스로 바로 송금</b> — 토스 앱이 <b>계좌·금액 채워진 송금화면</b>으로 열림(`supertoss://send` 딥링크). ②<b>카카오뱅크 앱</b> — 계좌 복사 + 카뱅 앱 열기(`kakaobank://`). ③<b>계좌 복사</b>.
- **PC**: 앱이 없으니 <b>토스 송금 QR</b>(폰으로 스캔하면 토스 송금 열림) + <b>계좌번호 복사</b>. QR은 `qrcode` 라이브러리로 브라우저에서 생성(외부 호출 0).
- **안전장치**: 토스·카뱅 버튼을 눌러도 <b>계좌번호는 항상 먼저 복사</b>돼서, 앱이 안 열려도 손님이 붙여넣을 수 있음(손해 0).
- **기기 판별**: `navigator.userAgent`로 휴대폰/PC 구분. 계좌는 상수 한 곳(`PAY_BANK/PAY_ACCT/PAY_ACCT_NO/PAY_HOLDER`)에서 관리해 딥링크·복사·표시가 항상 같은 값.
- 계좌 표시 줄에도 [복사] 버튼 추가. 이모지는 안 씀(사장님 지시).
- 바뀐 파일: `src/app/reserve/ReserveClient.tsx`·`src/app/globals.css`. 새 라이브러리: `qrcode`(무료). 검증: 타입검사·빌드 통과 + 실제 브라우저 E2E(PC=QR+복사, 모바일=토스·카뱅·복사, 콘솔 에러 0).

## 2026-07-22 — 🔴사장님이 취소한 입금완료 건도 환불할 수 있게 (돈 버그)

사장님이 **입금까지 끝난 예약을 직접 취소**하면 돌려줄 돈이 생기는데, 그 돈을 처리할 방법이 화면 어디에도 없던 문제를 고침(3차 RPA 점검 최우선 지적).

- **무엇이 문제였나**: 환불 대기 큐가 "손님 계좌가 있는 건"만 보여줬음. 사장님 취소건은 손님 계좌를 모르니 큐에 **영영 안 뜨고**, 계좌를 넣을 입력창도 없었음. 게다가 한 달 뒤 자동삭제(보관정책)가 이 건을 "환불 안 걸린 것"으로 보고 **돈이 남은 채 기록째 지워버림**.
- **고친 것**:
  - `money.ts` — 환불 대기를 둘로 나눔: `isRefundOwed`(돈이 남았나, 계좌 무관) / `isRefundReady`(계좌까지 있어 바로 보낼 수 있나). 예전 `isRefundPending`(=계좌 있어야만 True)은 폐기.
  - 통계·뱃지·자동삭제 가드를 모두 `isRefundOwed` 기준으로 바꿔, 계좌 없는 환불 대기건도 **건수·금액에 잡히고 자동삭제에서 보호**됨.
  - 관리자 [입금·환불 › 환불 처리] 탭 맨 위에 **"계좌 입력 필요" 칸** 신설 — 은행·계좌번호·예금주를 넣고 저장하면 아래 "바로 보낼 수 있음"으로 내려감(`NeedAcctRow`).
  - `PATCH /api/admin/reservations` 가 `refund_bank`·`refund_account`·`refund_holder` 를 받도록 함(전엔 안 받아 400). 계좌 입력 시 변경 이력에도 남김.
  - 예약 상세의 환불 안내도 계좌 유무에 따라 문구 분기("계좌 복사하고 보내주세요" ↔ "손님 계좌를 입력한 뒤 보내주세요").
- 바뀐 파일: `src/lib/money.ts`·`src/lib/expire.ts`·`src/app/api/admin/reservations/route.ts`·`src/app/admin/page.tsx`·`src/app/globals.css`(`.acct-form`).
- 마이그레이션 필요 없음(refund_bank/account/holder 칸은 이미 있음).

## 2026-07-20 — 오래된 예약 기록 자동 숨김(1주)·자동 삭제(1달)

예약 조회 화면에 취소·지난 예약이 계속 쌓여 보이던 문제 → 보관 정책 도입(사장님 지시).

- **일주일 지나면 손님 조회에서 숨김**: 취소·이용완료가 7일 지나면 손님 조회 화면에서 안 보임(DB엔 남고 관리자는 계속 봄). `GET /api/reservations` 가 `isHiddenFromLookup` 으로 걸러 내려줌.
- **한 달 지나면 DB에서 삭제**: 30일 지난 취소·이용완료 예약을 자동 삭제. 이력(reservation_logs)은 FK cascade 로 함께 삭제, 입금기록(deposits)은 링크만 해제(set null).
- 🔴 **안전장치**: 환불 안 끝난 취소건(돌려줄 돈 남음)은 30일 지나도 삭제 안 함(`isRefundPending` 가드). 미래·활성 예약도 당연히 보존.
- 동작 방식: 기존 `sweepExpiredReservations` 옆에 `maybePurgeOldReservations`(인스턴스별 1시간 1회 throttle)를 손님·관리자 GET 에 붙임(Vercel 크론 안 씀). 배포 시점 삭제 후보 0건(데이터가 07-09부터라 아직 30일 안 지남) → 5주쯤 뒤부터 자연 작동.
- `src/lib/expire.ts` 에 `HIDE_AFTER_DAYS=7`·`DELETE_AFTER_DAYS=30`. 검증: 숨김 5케이스·삭제 4케이스(환불미완료 보존·미래 보존 포함) 전부 통과.
- ⚠️ 다른 기록(문자로그 60일·입금웹훅 90일·미승인후기 90일 등)은 보고서에만 제안, 이번엔 예약만 적용. 보고서: `Desktop\판타스트릭_기록보관정책_보고서.html`.
- 참고: 삭제 기간을 1달로 잡아서 관리자 "N번째 방문" 재방문 인식도 최근 1달치만 봄(사장님 선택).

## 2026-07-20 — 손님이 예약 시간·날짜를 스스로 변경하는 기능

예약 조회 후, 취소하지 않고 **시간·날짜만 옮길 수 있게** 함(관리자 '예약 옮기기'를 손님용으로 연 것).

- **방식**: 취소→재예약이 아니라 예약을 **그대로 옮기기** → 예약금·입금상태 그대로 유지(돈 다시 안 냄).
- **규칙(사장님 확정)**:
  - 같은 테마 안에서 **날짜·시간만** 변경(다른 테마는 취소 후 재예약).
  - 시작 **24시간 넘게 남았을 때만** 가능(= 취소 100% 환불 조건과 동일). 이 규칙이 "80% 위약금 회피" 구멍도 막음.
  - 예약 1건당 **딱 1번만**(이력의 '손님 시간변경' 개수로 셈, 마이그레이션 없이).
  - 새 시간은 **새 예약과 똑같은 조건** 통과(오픈 규칙·요일 시간표·임박·마감·중복).
- **새 파일**: `src/app/api/reservations/change/route.ts`(변경 API, 본인확인 3요소) · `src/app/reservation/ChangeModal.tsx`(변경 팝업) · `src/components/ReserveCalendar.tsx`(달력을 공용 컴포넌트로 분리 — 새 예약·시간변경 공용).
- 조회 API(`GET /api/reservations`)가 예약마다 `changed`(이미 1회 썼는지)를 함께 내려줌 → 화면에서 [시간 변경] 버튼을 미리 감춤. 24시간 이내면 "시작 24시간 전까지 가능" 안내.
- 검증: 타입검사·빌드·테스트 45개 통과 + 실제 브라우저 E2E — 정상 변경(입금·확정 유지)·2번째 차단·24시간 이내 거부·비번 틀림 404, 페이지 에러 0. 테스트 데이터 전량 삭제.

## 2026-07-19 — 관리자 페이지 이모지도 SVG 아이콘으로 교체

손님 화면에 이어 관리자 페이지(사장님 전용)의 이모지 37종도 SVG로 통일.

- `Icon.tsx` 에 관리자용 아이콘 18종 추가(말풍선·종·다운로드·그래프·목록·확성기·편지·사람·카드·장부·눈·더하기·새로고침·번개·방향 4종).
- 화면 마크업 61곳 교체: 헤더 마스크·탭 아이콘·시계·전화 말풍선·통계 그래프·입금/환불 지폐·카드·장부·손님 이력·달력 이동(◀▶)·펼치기(▲▼)·미리보기·백업·발송내역 등.
- 덤: 관리자 후기 별색을 손님 화면과 같은 `#b06f00` 으로 통일(전 `#e0930c`, 대비 미달이던 것 — 3차 점검 지적사항).
- **일부러 남긴 것**: ①저장/실패 알림 메시지(잠깐 뜨는 토스트)의 ✅·⚠️ — `msg.startsWith("⚠️")` 로 성공/실패를 판정하는 신호라 빼면 판정이 깨진다 ②`<option>` 안 별점(★)·"3호점 ★" 매장 태그 — HTML상 `<option>` 안엔 SVG를 못 넣는다.
- 검증: 타입검사·빌드·테스트 45개 통과, 관리자 로그인해 실제 렌더 확인(콘솔 에러 0).

## 2026-07-19 — 손님 화면 이모지를 SVG 아이콘으로 전체 교체

이모지(🔒·⚠️·★·✅·✕·☰·🚫·⏱·💰·📅·🎭·🚇·✎·▦·⚙)는 **기기·OS마다 그림이 달라** 윈도우/안드로이드/아이폰에서 제각각으로 보였다. 어디서 봐도 똑같이 나오는 SVG 아이콘으로 통일.

- **새 파일 ** — 아이콘 15종을 한 곳에서 관리(디자인 일관성 규칙). 크기=부모 글자크기(1em)·색=부모 글자색(currentColor) 자동 상속. 서버·클라이언트 컴포넌트 양쪽에서 사용 가능(순수 SVG).
- 교체 위치: 별점(홈·후기), 자물쇠(예약 달력·난이도·FAQ), 성공/경고 알림(예약·조회·후기), 닫기·메뉴(헤더·모달), 마감🚫·임박⏱(예약 시간칸), FAQ 헤더 4개, 비즈니스 역량 아이콘 3개.
- **제외**: 코드 주석 속 이모지(화면에 안 보임), 화살표(→←›)·※·①②③④(기기 무관하게 일관된 활자 기호), **관리자 페이지**(사장님만 보는 내부 화면 — 필요하면 별도 작업).
- 검증: 타입검사·빌드·테스트 45개 통과. 실제 화면(FAQ·예약 달력·비즈니스·헤더) 캡처로 아이콘 렌더 확인. reduced-motion·접근성(aria-label은 그대로 유지) 영향 없음.

## 🔴 지금 남은 일 (2026-07-17 3차 점검 결과 · 다음에 여기서 시작)

순서대로. 위가 제일 급합니다.

| 순서 | 할 일 | 왜 |
|---|---|---|
| **1** | **환불 계좌 입력창 만들기** | **돈 문제.** 사장님이 취소한 입금완료 건의 돌려줄 돈이 장부에 안 뜸 (아래 상세) |
| **2** | **글꼴 보정값 바로잡기** | 내가 만든 회귀 — `/faq` 430폭 CLS 0.1117 (아래 상세) |
| **3** | 관리자 별색 · 시간 임박 안내 · 팝업 포커스 | 중간 3건 |
| **4** | QA 테스트번호 대역 바꾸기 | 연습예약 30건이 실수로 지워질 위험 |
| **5** | 잔손질 5건 + **거짓 주석 정정** | ☰ 글꼴 · h1 낭독 · 제목 중복 · 검색 오류 · 주석 숫자 |

### 1) 🔴 돌려줄 돈이 장부에 안 뜨고, 계좌 넣을 창도 없다

입금까지 끝난 예약을 사장님이 [취소 처리]하면 `refund_rate=100` 이 찍히며 30,000원 부채가 생기는데:
- **[입금·환불] 탭 어디에도 안 뜬다.** 통계 `refundPending` 금액에도 누락.
- 원인: `lib/money.ts` 의 `isRefundPending()` 이 **`refund_account` 필수** → 관리자 취소건은 계좌를 모르니 영영 큐 진입 불가.
- **더 나쁜 것**: 이력엔 `"손님 계좌를 받아 [환불 처리]에서 입력하세요"` 라고 적히는데 **그 입력창이 화면에 없다.**
  `admin/page.tsx` 는 `refund_bank/account/holder` 를 **읽기만** 하고(1093~1165), PATCH API 는 그 값을 아예 안 받아 호출하면 `400 "변경할 내용이 없습니다"`.
  **사장님이 지시를 따를 방법이 물리적으로 없다.**

### 2) 🔴 글꼴 보정값 — 하나로 뭉친 게 잘못 (내가 만든 회귀)

어제 넣은 `size-adjust:85.4%` 는 **한글만 재서 나온 값**인데 숫자·영문에까지 걸렸고, **개발 PC(윈도우=맑은 고딕) 기준**이라 손님 폰(안드로이드=Noto Sans KR)엔 안 맞는다.

| | 한글 | 숫자 | 영문 |
|---|---|---|---|
| 맑은 고딕 기준 | 85.4% | **97.6%** | **100.4%** |
| Noto Sans KR 기준 | **94.8%** | 96.9% | 97.7% |
| **지금 적용된 값** | 85.4% | **85.4%** ❌ | **85.4%** ❌ |

→ 숫자가 13% 좁게 그려져 **가격이 숫자로 가득한 `/faq` 에 새 튐**(430폭 0.1117, 3회 전부 재현).

**고치는 법**: 글꼴별로 **가족 이름을 나눠** 각각 size-adjust 를 주고(한 `@font-face` 안에선 하나만 줄 수 있다), **`unicode-range` 로 한글에만** 적용.
```css
@font-face{ font-family:"P폴백-노토"; src:local("Noto Sans KR"),local("Noto Sans CJK KR");
  size-adjust:94.8%; unicode-range:U+1100-11FF,U+3130-318F,U+AC00-D7A3; font-weight:100 900; }
@font-face{ font-family:"P폴백-맑은"; src:local("Malgun Gothic");
  size-adjust:85.4%; unicode-range:U+1100-11FF,U+3130-318F,U+AC00-D7A3; font-weight:100 900; }
body{ font-family:"Pretendard Variable","Pretendard","P폴백-노토","P폴백-맑은",system-ui,sans-serif; }
```
⚠️ **iOS(Apple SD Gothic Neo)는 못 쟀다** — 윈도우에 그 글꼴이 없다. **추측값 넣지 말 것**(틀리면 지금보다 나빠진다). 실기기에서 재기 전엔 체인에서 빼 둘 것.

### ⚠️ 알리고(문자) 가입할 때 먼저 할 일

**장유찬·장재욱 = 사장님 본인 테스트 예약**(9건, 2026-07-17 확인). 이 예약들은 **진짜 번호**(`010-4055-####`, `010-3456-####`)라
문자 안전장치(`isTestPhone` — `010-0000-` 으로 시작하는 번호만 차단)에 **안 걸린다.**
지금은 알리고 미가입이라 `sms_log` 가 전부 `skipped` 지만, **알리고를 켜는 순간 이 예약으로 테스트하면 실제 문자가 사장님 폰으로 나간다.**
→ 알리고 가입 전에 이 예약을 지우거나 번호를 `010-0000-*` 로 바꿀 것.

---

## 2026-07-17 — 3차 RPA 점검 (로봇 4대 · 프로덕션 전수)

**재검증 10/11 통과 · 회귀 33/33 통과 · 신규 11건**

### 통과 (어제 고친 게 진짜 고쳐짐)

| 항목 | 전 | 후 |
|---|---|---|
| 서버 위치 | `icn1::iad1` (미국동부) | **`icn1::icn1`** (서울) |
| 시간표 불러오기 | 882ms | **111ms** (8배) |
| 설정 불러오기 | 443ms | **115ms** |
| 재방문 전송량 | 291KB | **3KB** (-98.8%) |
| `/_next/image` 캐시 | `max-age=0` | **`max-age=86400`** (상속 확인) |
| `/business` 화면 튐 | 0.2362 | **0.0296** |
| `/reviews` 화면 튐 | 0.3056 | **0.0122** |
| 후기 칩 필터 | 누를 때마다 서버 왕복 | **서버 요청 0건** |
| 리마인더 옛 기록 | [다시 보내기] 노출 | **84개 차단 / 16개 정상 유지** |
| 색 대비 | 2.11~4.34 ❌ | **4.66~5.64** ✅ |

- **회귀 0건** — 환불 4단계 · 입금취소 복귀 · 3요소 조회 · 중복차단 · 시간표 · 예약금 위조 방어(99,000 위조 → 서버 63,000) · 히어로 연출 · 머더룸 줄맞춤 0.00px · 콘솔 에러 0 · 자동 테스트 45개, 전부 그대로.
- **손님 자료 무사** — wp-import 103건 지문(md5) 점검 전과 동일. 실제 발송 문자 **0건**(`sms_log` 227건 전부 `skipped`).

### 실패 / 신규
- **[높음]** 글꼴 보정값 (위 참조 — 내가 만든 회귀)
- **[높음]** 환불 계좌 입력창 없음 (위 참조)
- **[중간]** 관리자 후기 별색 인라인 하드코딩(`admin/page.tsx:1444`, 대비 2.52) — 색 수정이 여기만 놓침
- **[중간]** 입력 중 시간 임박 → ④단계 무설명 증발 (실측: 21:49:33 선택 → 21:50:33 자동해제)
- **[중간]** 예약금 팝업 포커스 미관리 (동의 체크박스까지 Tab 21번)
- **[중간]** QA 테스트번호 대역이 wp-import 30건과 충돌
- **[낮음]** ☰·✕·‹› 아직 Arial (**globals.css 주석은 "고쳤다"고 거짓말 중**) / 홈 h1 낭독 "FANTASY+TRICK" / `/reserve?theme=` title 중복 / 문자검색 오류가 조용히 "내역없음" / reminder 주석 92→실제 201

보고서: `바탕화면\판타스트릭_3차점검보고서.html`

> 무엇을 바꿨는지 시간 순으로 적는 곳이에요. (최신이 위)

## 2026-07-17 — 2차 RPA 점검에서 나온 것 전부 수정 (서울 이전 · 캐시 · 화면 튐 · 색)

로봇 4대로 다시 점검한 뒤, 나온 문제를 순서대로 다 고쳤습니다.

### 1) 서버(함수) 위치를 미국 동부 → 서울로 — **체감 제일 큼**

자료창고(Supabase)는 서울인데 홈페이지 함수는 **미국 동부(iad1)** 에서 돌고 있었다.
손님이 시간표를 볼 때마다 태평양을 왕복한 셈. `vercel.json` 에 `"regions":["icn1"]` 한 줄.

| | 전 | 후 |
|---|---|---|
| 응답 헤더 | `icn1::iad1` | **`icn1::icn1`** |
| `/api/config` | 443ms | **53ms** (-88%) |
| `/api/slots` | 882ms | **144ms** (-84%) |

### 2) 포스터 캐시 — 1차 수정이 헛발질이었던 것을 바로잡음

`next.config.ts` 의 `images.minimumCacheTTL` 은 **Vercel 서버가 보관하는 기간**만 정한다.
손님 폰이 보관하는 기간은 **원본 파일의 Cache-Control 을 그대로 물려받는다** — 그래서 1차 수정 뒤에도
`x-vercel-cache: HIT` 인데 손님에게 가는 값은 `max-age=0` 이었다(= 올 때마다 다시 받음).
→ `headers()` 에 `/images/*`, `/fonts/*` 규칙 추가. `/_next/image` 로 변환된 포스터도 이 값을 물려받아 같이 해결.

⚠️ 일부러 **1년이 아니라 하루**(+자동 갱신)로 잡았다. 1년으로 박으면 포스터를 같은 이름으로 갈아끼워도
이미 방문한 손님 폰에는 **1년 내내 옛 포스터**가 남는다.

### 3) 화면 튐(CLS) — 진짜 원인은 제목 글꼴이 아니라 **본문 글꼴**이었다

**증상**: 폰에서 `/business` 0.2362 · `/reviews` 0.3056 (기준 0.1). 손님이 버튼을 누르려는 순간 화면이 움직임.

**틀린 시도 2번** (같은 실수 반복 방지용 기록 — `layout.tsx` 에도 남겨둠):
- `font-display:block` → 글자만 숨기고 **자리는 기본 글꼴 크기로 그대로 잡아둔다** → 0.2357 (효과 0)
- `font-display:optional` → 나눔명조가 **아예 안 쓰여 세리프 제목을 잃는데도** 여전히 튐

**진짜 원인**: 본문 글꼴(Pretendard)이 느린 인터넷에서 6초쯤 뒤 도착. 그때까지 쓰는 시스템 글꼴이
**한글 기준 17% 더 넓어서**, 도착하는 순간 모든 문단의 줄 수가 줄며 화면이 통째로 움직였다.
`/business` 는 히어로가 세로 가운데 정렬이라 이 변화가 증폭됐다.

**해결**: 임시로 쓰는 시스템 글꼴을 **미리 85.4% 로 줄여** Pretendard 와 폭을 맞춤(`globals.css` 의 `@font-face "Pretendard 임시"` + `size-adjust`).
숫자 근거는 같은 문장 폭 실측(한글 본문 0.8541 · 한글 제목 0.8549).

**결과 (폰 390 · 3G · CPU 4배, 8개 화면 전부)**

| 화면 | 전 | 후 |
|---|---|---|
| `/business` | 0.2362 | **0.0006** (320·360·390·430 전부 통과) |
| `/reviews` | 0.3056 | **0.0002** |
| 홈 | 0.0060 | **0.0019** |
| 나머지 5개 | — | 전부 0.003 이하 |

제목은 **나눔명조(세리프) 그대로** 유지되는 것까지 확인(CDP 실측).

### 4) `/reviews` 를 서버가 미리 그리도록 (`/reserve` 와 같은 방식)

후기를 화면이 뜬 뒤에 받아와서 푸터가 밀렸다. 서버 컴포넌트로 바꿔 완성된 화면을 보낸다.
**덤**: 테마 칩이 서버 왕복 없이 즉시 걸러진다(전에는 칩 누를 때마다 다시 물어봄).

### 5) 색 대비 (WCAG AA 4.5:1)

| 무엇 | 전 | 후 |
|---|---|---|
| `--cyan` 파랑 링크·작은 글자 (16개 화면) | `#2f6fe0` 3.97~4.34 ❌ | `#2a63c9` **4.76~5.64** ✅ |
| 푸터 "관리자" | 2.11 ❌ (`opacity:.55` 가 곱해져 실효 `#9fa4ad`) | **4.66** ✅ (투명도 제거 + 누르는 칸 24px↑) |
| 별점 ★ | `#e0930c` 2.32 ❌ | `#b06f00` **3.79** ✅ (그림 기준 3:1) + 점수 읽어주기 |

`--cyan` 은 눈에는 거의 같은 파랑인데 4개 배경(흰·기본·진한·선택칩) 전부 통과하는 값으로 골랐다.

### 6) 나머지

- **h1 없던 5개 화면**에 h1 지정 (홈 로고·예약·후기·자주묻는질문·예약조회) — 검색·화면낭독기가 "이 페이지가 무엇인지" 알아보는 기준.
  홈은 FANTASTRICK 로고 타이포가 h1(`margin:0;font-weight:400` 으로 브라우저 기본값 지워 연출 그대로 유지).
- 후기 필터 칩에 `aria-pressed` — 색만으로 알리지 않게.
- 발송 내역에 남은 **옛 리마인더 기록 92건**: [다시 보내기] 버튼 숨김 + 서버도 거절(`isActiveSmsType`).
  기록 자체는 이력이라 지우지 않았다.

---

## 2026-07-17 — 2차 RPA 점검에서 찾은 잠복 결함 3건

- **홈 후기 카드가 영영 안 보일 뻔**: 등장 효과를 붙이는 장치가 "처음부터 있던 요소"만 지켜봐서,
  후기를 받아온 **뒤에** 생기는 카드는 투명한 채로 남았다. **사장님이 첫 후기를 승인하는 순간** 빈 칸 3장이 될 상태.
- **`<button class="btn">` 만 Arial**: 버튼은 글꼴을 물려받지 않는데 `.btn` 에 지정이 없었다(같은 글자 폭 112.67 vs 95.72px).
- **테마 상세 → [예약하기] 딥링크 화면 튐 0.18**: 1차 때 "높이 560px 예약"으로 막았는데 그건 테마를 안 고른
  첫 화면(554px) 기준이라, 테마를 들고 들어오면(996px) 그대로 뚫렸다. **손님 주 진입 경로**였다.
  → 숫자로 막는 대신 서버가 테마를 알고 그리도록 바꿔 원인을 없앰.

> 무엇을 바꿨는지 시간 순으로 적는 곳이에요. (최신이 위)

## 2026-07-17 — 🐛 테마 포스터를 눌러도 상세 페이지가 안 열리던 것 (사장님 발견)

**상세 페이지는 원래 다 있었다.** `/rooms/{테마}` 에 포스터·시놉시스·주의사항·이용금액·예약금·입금계좌·"이 테마 예약하기" 버튼·환불규정·오시는 길이 4테마 모두 들어있었고(내용은 기존 fantastrick.co.kr 원문, `lib/theme-content.ts`), 홈 카드도 이미 거기로 연결돼 있었다. **문제는 클릭이 아예 안 먹은 것.**

**원인**: `globals.css` 의 `.theme-rail.dragging .tcard{pointer-events:none}`.
마우스를 **누르는 순간** `.dragging` 이 붙어서 **카드 링크가 통째로 죽고**, 손을 뗄 때 클릭이 링크에 도달하지 못했다. 1px 도 안 움직이고 그냥 눌러도 안 열렸다. (테마 목록에 마우스 드래그를 붙이면서 같이 들어온 줄)

**고친 것**
- 그 CSS 한 줄 제거. "끌고 나서 손 떼면 링크가 열리는" 문제는 `page.tsx` 의 `onClickCapture` 가 이미 막고 있다(5px 넘게 움직였을 때만 클릭 취소) — CSS 로 또 막을 필요가 없었다.
- 포스터를 끌 때 브라우저가 링크·이미지를 끌고 다니는 유령 이미지는 `draggable={false}` + `-webkit-user-drag:none` 으로 막았다.

**검증(실제 크롬)**: 가만히 클릭 → 4테마 전부 상세로 이동 ✅ / 300px 끌기 → 다음 카드로 넘어감(스냅) ✅ / 끈 뒤 손 떼기 → 상세로 안 감 ✅

## 2026-07-17 — 🐛 마감 정보 오기 전에 시간칸을 못 누르게 (사장님 발견)

**증상**: 날짜를 고르면 시간칸이 바로 뜨는데, **이미 예약된 칸도 2~3초 동안 예약 가능한 것처럼** 보였다. 그 사이에 그 칸을 고른 손님은 이름·전화·비밀번호까지 다 입력하고 나서야 "이미 예약된 시간입니다"를 봤다.

**원인**: 시간칸은 코드가 바로 그리지만, "어느 칸이 찼나"는 그 뒤에 서버(`/api/slots`)에 물어본다. 답이 오기 전까지 화면은 **아무것도 안 찬 상태**로 보였다.

**중요 — 이중예약 사고는 아니었다**: 서버가 이미 막고 있다(`uq_res_slot` 유니크 → `23505` → 409 "이미 예약된 시간입니다"). 데이터가 깨지는 문제가 아니라 **손님을 헛고생시키는 문제**였다.

**고친 것**
- 답이 오기 전에는 **시간칸 전부 못 누르게** + "예약 가능한 시간을 확인하는 중이에요…" 안내.
- 🔴 **함께 고친 숨은 버그**: 날짜를 빠르게 여러 번 바꾸면, 먼저 요청한 **옛 날짜의 답이 늦게 도착해 새 날짜의 마감정보를 덮어썼다**(16일 마감정보가 18일 위에 덮이는 식). 이제 늦게 온 답은 버린다.

**검증**: 응답을 일부러 3초 늦춰 그 상황을 재현 → 답 오기 전 10칸 전부 못 누름 / 억지로 눌러도 다음 단계 안 열림 / 답 온 뒤 정상 표시(가능 2 · 마감 8). 라이브에서도 재확인.

## 2026-07-16 — 🧭 예약창을 "한 단계씩 열리는" 방식으로 (사장님 요청)

전에는 테마·날짜·인원·시간·이름·전화·비밀번호가 **한 화면에 전부 펼쳐져** 있어서 손님이 어디부터 봐야 할지 헷갈렸다. 이제 **앞 단계를 골라야 다음이 나타난다.**

```
① 테마 선택  →  ② 날짜  →  ③ 시간  →  ④ 인원·이름·전화·비밀번호·안내
```

- **인원이 시간 뒤로 이동**했다 (전에는 시간보다 위에 있었음).
- 고른 것은 계속 보이고 **언제든 다시 바꿀 수 있다**(뒤로 가기 가능).
- **테마를 바꾸면 골라둔 시간이 풀린다** — 테마마다 시간표가 완전히 달라서(사자의 서 70분 간격 등) 그냥 두면 그 테마에 없는 시간이 골라진 채로 남는다. 날짜는 그대로 둔다(보통 같은 날을 원하므로).
- 다음에 뭘 해야 할지 안내를 붙였다: "테마를 선택하면 날짜를 고를 수 있어요" 식.
- 아직 **예약 오픈 전인 날짜**(이용일 7일 전 저녁 9시 오픈)를 고르면 그 자리에서 안내만 뜨고 다음 단계로 넘어가지 않는다. **휴무·마감인 날**도 시간을 고를 수 없으니 ④가 자연히 안 열린다.
- 나타나는 효과는 히어로가 쓰는 `htUp` 애니메이션 재사용(사이트 전체 움직임 통일) + `prefers-reduced-motion` 존중.

**검증**: 실제 크롬으로 ①→②→③→④ 를 눌러가며 각 단계에서 **무엇이 열리고 닫히는지 전부 확인**. 테마를 바꾸면 ④가 닫히는 것까지 확인. **모바일(390px)** 도 가로 삐져나옴 없음. 타입체크 통과.

## 2026-07-16 — 🏦 입금 자동확인을 홈페이지가 직접 받도록 (PC 불필요) + 📥 기존 사이트 예약 가져오기

**① 입금 알림 받는 문 `POST /api/bank/deposit` 추가**

태블릿(BankNotify 앱)이 카톡 입금알림을 여기로 보내면 → 이름·금액을 뽑아 → 입금 기다리는 예약 중 **이름+금액이 정확히 맞는 1건**을 찾아 → 입금확인 처리.

- **왜 옮겼나**: 전에는 PC 에 프로그램(`bank-auto`)을 따로 켜둬야 했다. 워드프레스 DB 에 직접 붙어야 해서. 새 사이트는 예약이 같은 DB 에 있으므로 홈페이지가 직접 받으면 된다 → **PC·방화벽·Tailscale 전부 필요 없어짐.**
- **입금확인 로직은 다시 짜지 않았다** — 관리자 화면의 [입금 확인] 버튼이 쓰는 `PATCH /api/admin/reservations` 를 **그대로 호출**한다. 입금확인 → 확정 → 문자 → 변경이력이 전부 그 안에서 처리됨. 두 곳에 같은 규칙을 두면 언젠가 어긋나서.
- **파서·매처는 `bank-auto` 에서 그대로 이식** (`src/lib/bank/`) — 검증된 **테스트 28개도 함께** 가져와 통과 확인 (`npm test`, vitest 추가).
- **안전장치**: 토큰(`X-Webhook-Token`) 인증 · 같은 알림 두 번 오면 무시(`deposits.event_id` unique) · **이름+금액 정확일치 + 후보 딱 1건**일 때만 처리(동명이인·여러건이면 손 안 댐) · `BANK_DRY_RUN=true` 면 매칭만.
- **검증(로컬 실제 실행)**: 매칭 ✅ / 재전송 무시 ✅ / 금액 다르면 거부 ✅ / 없는 사람 거부 ✅ / 실제 입금확인 → `status 대기→확정`·`paid_source=auto`·변경이력이 **관리자 버튼과 완전히 동일** ✅
- 새 표: `deposits` (`supabase/migration_deposits_APPLY_ME.sql`) · 새 환경변수: `BANK_WEBHOOK_SECRET`, `BANK_DRY_RUN`

**② 문자 발송 안전장치 (`src/lib/sms.ts`)**

연습용 번호(**010-0000-XXXX**)로는 **절대 문자가 안 나가게** 막았다.

- **왜**: 이 사이트는 매일 아침 "내일 예약" 확정 건에 리마인더 문자를 자동 발송하고, 관리자가 입금확인·취소를 눌러도 문자가 나간다. 연습 데이터에 진짜 번호가 섞이면 **아무 잘못 없는 손님에게 문자가 간다.**
- 문자가 나가는 길은 `sendSms`/`sendAlimtalk` 둘뿐이라 거기서 막음 → 크론·관리자버튼·재발송 **어느 경로로도** 안 나감.
- **검증**: 알리고 키가 **켜져 있는 상태**로 시험 → 연습용 번호는 외부 호출 **0회**, 일반 번호는 정상 발송 시도 (8/8 통과).

**③ 기존 사이트(fantastrick.co.kr) 실제 예약 103건 가져옴** (`scripts/import-from-wp.mts`)

새 사이트를 실제 운영처럼 굴려보기 위해 **앞으로 남은 예약만** 복사.

- ⚠️ **기존 사이트에는 `SELECT` 만.** 수정·삭제 안 함. 데이터는 한 방향으로만 흐름.
- ⚠️ **전화번호는 전부 가짜(010-0000-XXXX)로 바꿔서 넣음** — 진짜 번호 유출 0건 확인.
- 🔴 **큰 함정 발견**: 워드프레스 Booked 는 **한국 시각을 UTC 인 척** 저장한다. 그냥 옮겼으면 **103건 전부 9시간씩 밀릴** 뻔했다. 8건 교차검증(timeslot 과 대조)으로 확인 후 바로잡음.
- ✅ **덤으로 얻은 검증**: 가져온 104건의 예약 시각이 **전부 새 사이트 시간표(`THEME_SLOTS`) 안에 들어맞음** → 손으로 옮긴 시간표가 실제 운영과 일치한다는 증거.
- 인원수는 기존 사이트에 정보가 없어 **2명으로 표시**(메모에 명시). 예약금은 **새 사이트 기준**(시간의 영속성 63,000 — 기존 60,000 아님).
- 가져온 것은 전부 `source='wp-import'` 표시 → 실제 오픈 전에 `--reset` 으로 한 번에 지울 수 있음.
- 사용법: `npx tsx scripts/import-from-wp.mts` (미리보기) / `--apply` (실제) / `--apply --reset` (지우고 새로)

## 2026-07-16 — 🗑 안 쓰는 이미지 7MB + 협업 문서 삭제 (사장님 확인 후)

- **지운 이미지 6장** — `logo-white.png` · `murderroom-1.png` · `murderroom-2.png` · `permanence-1.jpg` · `permanence-2.jpg` · `permanence-poster.jpg`. **`public/images` 14.9MB → 7.6MB (약 7MB 감소).**
- **지우기 전 확인한 것**: ①저장소 **전체**(코드·문서·설정) 검색해서 참조 0 확인(유일한 언급은 README 의 "지울지 확인 필요" 문장 자신) ②**원본이 `assets/images/` 에 16장 그대로 보관중** — 필요하면 언제든 되돌릴 수 있음 ③깃 히스토리에도 남아 있음.
- **남긴 이미지 10장은 전부 실제 사용중**: favicon · logo-black(폰트 만들 때 원본) · logo-blue(헤더·푸터) · poster 4장 · stores-map · lockdowncity-street · permanence-escape.
- **`협업_시작안내.md` 삭제** — 남을 초대해 같이 작업하던 시절 문서. 지금은 사장님 단독 저장소라 안 씀.
- **검증**: 7개 페이지(홈·비즈니스·리뷰·테마상세2·예약·FAQ)를 **끝까지 스크롤하며 깨진 이미지(404) 검사 → 0건**. 빌드 통과.

## 2026-07-16 — 🧹 안 쓰는 코드 전수 정리 (사장님 "전부 체크해서 정리")

**검사 방법**: `globals.css` 의 클래스 **332개**를 전부 뽑아 `src` 전체 코드와 대조 → 코드에서 안 쓰이는 것만 골라 하나씩 확인 후 삭제.

**지운 것 (CSS 51줄)**
- `.hero` 블록 30줄 — 옛 **사진 히어로**(Ken Burns 줌·배경영상 `.hero-video`·`heroIn` 스태거·`kenburns` 키프레임). 메인은 `.hero-t`, 비즈니스는 `.biz-hero` 를 씀. ⚠️ 같은 구역의 `.scrollcue`·`@keyframes bob` 은 **살아있는 코드라 남김**(경계 직접 확인 후 삭제).
- 옛 **ABOUT 섹션** 22줄 — `.about-grid`·`.pillars`·`.pillar`·`.about-visual`·`.about-frame`·`.worldtags`. 섹션 자체가 화면에서 사라졌는데 CSS만 남아 있었음.
- 낱개 4줄 — `.rev-summary .rs-links` · `.rev-empty .re-big` · `.biz-cta .bt-line`(2줄).
- **디버그 스크린샷 15장**(`docs/_*.png`, 약 3.8MB) — 내가 작업하며 만든 것. gitignore 라 깃엔 없었고 로컬만 정리.
- 결과: `globals.css` **969 → 918줄**, `page.tsx` 462 → **403줄**(패럴랙스 60줄 포함).

**검증**: 홈·비즈니스·리뷰·예약·테마상세·FAQ **6개 페이지를 지우기 전/후 픽셀 대조** → 5개 **0.00% 동일**, 홈만 4.08% 인데 확인 결과 **배경 그라디언트 애니메이션(30초 주기) 위상 차이**일 뿐 레이아웃·글자 완전 동일. 빌드 통과.

**안 지운 것 (일부러)**
- 🔴 **`react-dom`·`@types/*`** — 코드에서 이름이 안 보여 "미사용"으로 잡히지만 **Next.js 동작·타입검사에 필수**. 지우면 사이트가 죽음.
- **`playwright`**(devDep) — 화면 자동 검증에 씀.
- ✅ **안 쓰는 이미지 6장 + 협업 문서 삭제 완료** (아래 항목 참고)


## 2026-07-16 — 🧹 죽은 히어로 패럴랙스 코드 60줄 삭제 (사장님 확인 후)

- `page.tsx` 의 히어로 패럴랙스(스크롤 시차 + 마우스 깊이감) 60줄 + `heroBgRef` 제거. **`heroBgRef` 가 JSX 어느 요소에도 안 붙어 있어 첫 줄 `if (!bg) return;` 에서 항상 빠져나가던 = 0% 동작하던 코드.**
- 지금 히어로가 움직이는 건 **배경 그라디언트(`htMesh 30s`)** 와 **스크롤 이별 연출(`.ht-stack`)** 이고 **둘 다 CSS** 라 JS 가 필요 없음.
- 검증: 콘솔 에러 0 · 배경 애니메이션 정상(`htMesh`) · 이별 연출 정상(투명도 1 → 0.47) · 빌드 통과. `page.tsx` 462 → **403줄**.
- 📌 **아직 남은 죽은 CSS**: `globals.css` 의 `.hero` 블록(옛 사진 히어로 — Ken Burns 줌·배경 영상·`heroIn` 스태거) 약 30줄. 메인은 `.hero-t`, 비즈니스는 `.biz-hero` 를 쓰므로 **아무 데서도 안 씀**(grep 확인). ⚠️ 단 같은 구역의 `.scrollcue`·`@keyframes bob` 은 **살아있는 코드**라 지우면 안 됨. 지울지는 사장님 확인 대기.

## 2026-07-16 — 🚀 테마 포스터 2,014KB → 94KB (95% 감소)

> 홈 화면이 내려받는 이미지 2,065KB 중 **2,014KB(97%)가 포스터 4장**이었음.

- **원인**: 포스터를 **CSS 배경**(`style={{backgroundImage}}`)으로 넣어서 **next/image 최적화를 통째로 건너뜀**. 브라우저가 원본을 그대로 받음.
  · 같은 페이지의 **매장 지도는 `<Image>` 라 1.97MB → 28KB WebP**(70배 차이)로 잘 내려가고 있었음 — 증거가 같은 화면 안에 있었던 셈.
  · 포스터 원본 **726×1024** 인데 화면엔 **266×399** 로 표시 → 필요한 픽셀의 약 7배. 게다가 락다운시티는 사진인데 **PNG**(사진에 최악)라 1MB.
- **수정**: `<Image fill sizes="(max-width:520px) 82vw, (max-width:980px) 45vw, 280px">` + `.thumb-img{object-fit:cover}`. 파일은 그대로 두고 **넣는 방식만** 바꿈.
- **결과(실측)**: `poster-ldc.png` 1,057KB → **25KB** · `poster-duat.png` 711KB → **18KB** · `poster-bride.jpg` 164KB → **36KB** · `poster-time.jpg` 82KB → **15KB**. **합계 2,014KB → 94KB.** 전부 WebP·384w 로 자동 변환됨(고해상도 화면은 srcset 으로 더 큰 걸 받음).
- **왜 예전엔 배경이었나**: `<img>` + 호버 확대가 카드 밖으로 삐져나와서 배경으로 바꿨던 것. **그 버그는 `.tcard{overflow:clip}` 으로 이미 막혀 있어** 되돌려도 안전 — 호버 확대 정상 동작 확인.
- 체감은 **모바일에서 큼**: LTE 손님이 테마 목록 보려고 2MB 받던 것이 94KB.

## 2026-07-16 — 📊 포스터 아래 가로 스크롤바 + 🐛 클래스 이름 충돌(흰 박스) 수정

- **가로 스크롤바 추가** (`.rail-bar`) — "옆으로 더 있다 / 지금 여기쯤"이 보이게. 손잡이 **폭 = 보이는 비율**, **위치 = 진행률**. 브랜드 블루→바이올렛 그라디언트.
  · 브라우저 기본 스크롤바는 OS마다 모양이 제각각이라 계속 숨기고 **직접 그림**.
  · **끌어서 넘길 수도 있음**(진짜 스크롤바처럼). 손잡이 가운데가 커서에 오도록 보정 — 안 하면 잡을 때마다 왼쪽으로 튐.
  · ⚠️ **상태(useState)가 아니라 DOM 에 직접 씀**(`style.setProperty('--w'/'--x')`) — 스크롤할 때마다 리렌더하면 버벅임.
  · 카드가 다 보여서 넘길 게 없으면 자동으로 숨김.
  · 검증: 스크롤 0→끝에서 손잡이 `left: 0 → 380px` 이동 · 바를 끌면 레일 0 → 576 이동 · 페이지 이동 안 함.

- 🔴 **버그 수정 — 테마 카드 라벨이 흰 박스 안에 들어가던 것**: 준비중 카드를 만들다 발견. 원인은 **클래스 이름 충돌** — 테마 카드의 매장 라벨이 `.store` 인데, **매장 섹션의 `.store`(흰 배경·테두리·여백 16px·radius 14 짜리 카드)** 와 이름이 같았음. `.tcard .tt .store` 가 색·크기만 덮어써서 **배경·테두리·여백은 매장 카드 것이 그대로 딸려 들어옴**.
  · **원래부터 라이브에 있던 버그**(내가 만든 게 아님) — 활성 카드는 라벨이 평소 `opacity:0` 이고 **호버할 때만** 보여서 눈에 안 띄었음. 라이브에서 호버 시 라벨 높이 **55px**(정상 20px), 흰 박스 확인.
  · 준비중 카드는 라벨을 항상 보이게 해서 **바로 드러남**.
  · 수정: 테마 카드 쪽 라벨을 **`.tstore`** 로 분리(전역 `.store` 는 매장 카드 전용). 검증: 호버 시 높이 55px → **20px**, 배경·테두리·여백 사라짐. 매장 카드는 그대로.

## 2026-07-16 — 🎞 테마 목록: TITLE 표기 통일 + 준비중 테마 합치기 + 마우스 드래그

- **표기 통일**: 카드 번호표 `CASE No.01` → **`TITLE 01`**(02·03…). 4개 테마 전부.
- **준비중 테마를 캐러셀 안으로**: 예전엔 아래 별도 슬림 스트립(`.soon-strip`)이라 눈에 안 띄었음 → 이제 운영중 테마와 **같은 줄·같은 크기**로 붙고 번호도 이어짐(`TITLE 05`·`TITLE 06`). 여전히 **클릭 불가**(점선 테두리 + `COMING SOON` 배지로 위계 구분).
  · 준비중 테마는 `data.ts` 에 **포스터 이미지가 없어서**(`poster: ""`) 자리표시 카드로 그림(옅은 `?` + 그라디언트). 포스터가 나오면 그때 넣으면 됨.
  · 안 쓰게 된 옛 CSS(`.soon-strip`·`.soon-row`·`.scard`·`.stag`·`.sname`·`.sgen`)와 `SoonCard` 컴포넌트는 제거.
- **마우스로 잡아끌기(드래그)** 추가 — 예전엔 PC에서 화살표로만 넘길 수 있었음(모바일 터치 스와이프는 원래 됐음).
  · 마우스일 때만 붙임(`pointerType === "mouse"`) — 터치는 브라우저 기본 스와이프가 이미 있어서 둘 다 걸면 서로 싸움.
  · 끄는 동안 `.dragging` 으로 **스냅·부드러운스크롤을 잠시 해제** — 안 하면 커서에 안 붙고 튐.
  · 🔴 **함정**: 카드가 `<Link>` 라 끌고 손을 떼면 **페이지가 열려버림** → 5px 이상 움직였으면 클릭을 막음(`onClickCapture`). 이미지 기본 드래그(고스트)도 차단.
  · 커서 `grab`/`grabbing` 표시.
- 검증: 레일 카드 6장 · 번호표 `TITLE 01~06` · 드래그로 스크롤 0 → 288 이동 · **드래그 후 페이지 이동 안 함** 확인 · 화살표 정상.

## 2026-07-16 — 🎬 시네마틱 스크롤 1차 (샘플 20안 중 **히어로 이별 + 등장 연출 다양화** 채택)

> "세로로 긴 페이지를 시네마틱하게" → 에이전트 2개(레퍼런스 조사 / 우리 코드 적용성)로 분석 후 샘플 20안 제작 → 사장님이 2개 채택.

**[적용 1] 히어로 이별 연출** (`.hero-t .ht-stack`) — 스크롤하면 히어로가 **위로 밀리며 사라짐**. 예전엔 그냥 잘려 올라갔음.
  · `animation-timeline: scroll(root block)` + `animation-range: 0 38vh`.
  · 🔴 **처음엔 82vh 로 잡았다가 실패 — 사장님이 "작동 안 하는 것 같다"고 잡아냄.** 원인: 히어로 글자는 스크롤한 만큼 **자연스럽게 위로 올라가 버리는데**, 600px 쯤에서 흐려져 봐야 그땐 **이미 화면 밖**이라 아무도 못 봄. 숫자(투명도 1→0)만 보고 "된다"고 보고한 내 잘못. **글자가 아직 화면에 있는 동안(0~38vh) 끝나야** 눈에 보임.
  · 재검증(눈으로): 스크롤 90px 투명도 0.74(글자 완전히 보임) · 180px 0.47(보임) · 342px 0. 그림으로도 확인.
  · 히어로 **진입 애니메이션(4.2초 시간표)과 안 겹침** — 이건 스크롤할 때만 일어남.
  · 🔴 **흐림(blur)은 일부러 뺐음**: 화면을 다시 그려야 해서 저사양 폰에서 버벅임. `opacity`·`translate`·`scale` 만 쓰면 GPU가 처리. 전역 `.grain`(fixed + blend)이 화면을 덮고 있어 특히 중요.
  · ⚠️ **`animation-timeline` 은 반드시 `animation` 단축속성 뒤에** 와야 함(앞에 두면 덮어써져 조용히 안 됨 — WebKit 공식 문서가 경고하는 함정).

**[적용 2] 등장 연출 다양화** (`.reveal` 변주) — 지금까지 **모든 섹션이 똑같이 '아래서 위로'** 하나뿐이라 리듬이 없었음.
  · `rv-focus`(흐림→또렷) = 테마 포스터 / `rv-left` = 리뷰 요약 / `rv-right` = 매장·지도. 기본(위로)은 나머지 유지.
  · **기존 IntersectionObserver 방식을 그대로 씀** → 스크롤 연동 CSS와 달리 **파이어폭스 포함 전 브라우저 동작**. 새 시스템 도입 0.
  · 모바일(≤760px)에선 좌우 슬라이드가 답답해서 기본(위로)으로 되돌림.
  · 검증: 등장 전 `blur(7px)`/`translateX(±34px)` → 등장 후 `blur(0)`/`none` 확인.

**[안 함] 매장 지도 고정** — 사장님이 후보로 꼽았으나 **측정 결과 효과 0px**: 매장 왼쪽 칼럼 내용이 **340px**인데 지도가 **448px** 라 지도가 더 큼. 고정할 여지가 없어 적용해도 아무 일도 안 일어남. (샘플에선 내가 카드를 5장으로 늘려 만들어서 되는 것처럼 보였음 — 실제는 3장) → **매장 섹션 콘텐츠를 늘리면** 그때 가능.

**조사에서 확인한 중요한 사실**: **사파리 26(2025-09)부터 CSS 스크롤 애니메이션 지원**(아이폰 포함), 크롬은 115+(2023-07)부터. 전 세계 지원율 약 83%, 파이어폭스 스테이블만 미지원(플래그 뒤, Interop 2026 항목). **미지원 브라우저에선 연출만 빠지고 화면은 정상** → 폴리필·분기 불필요. 따라서 **GSAP(약 35KB)·Lenis 도입 근거 없음**(참고: GSAP은 2025-04부터 상업용도 무료 — 이제 비용이 아니라 **무게**가 반대 이유). Lenis는 특히 `scroll-behavior:smooth`·모바일 드로어 스크롤락·`prefers-reduced-motion` 3군데를 부숨.

**🔴 이번에 발견한 기존 문제 2개 (아직 안 고침 — 사장님 확인 필요)**
1. **히어로 패럴랙스 60줄이 죽은 코드** (`page.tsx:129-178`): `heroBgRef`가 JSX 어느 요소에도 안 붙어 있어 **0% 동작 중**. 지금 히어로에서 움직이는 건 패럴랙스가 아니라 배경 그라디언트(`htMesh 30s`). 지울지 살릴지 결정 필요.
2. **테마 포스터가 `next/image` 우회** (`page.tsx:30`): CSS `backgroundImage`로 넣어서 압축·WebP 변환·반응형이 전부 안 먹음. **`poster-ldc.png` 1.1MB 원본이 그대로** 내려감(화면엔 270px). 4장 합계 약 2MB. 예전에 `<img>` 확대가 카드 밖으로 삐져나와 배경으로 바꿨는데, 그 버그는 지금 `.tcard{overflow:clip}`로 이미 막혀 있어 **되돌릴 수 있음**.

## 2026-07-16 — 🎨 히어로 배경 (샘플 10안 중 **10번 하이브리드** 사장님 선택)

> "배경이 하얀색이라 너무 심플하다. 배경을 넣되 **과하지 않게**" → 에이전트로 레퍼런스 조사 후 샘플 10안 제작 → 사장님이 10번 선택.

- **진단**: 배경이 없던 게 아니라, 있던 그라디언트가 **투명도 30%+블러 70px** 라 사실상 안 보였음. 그레인도 2.5%로 업계 표준(5%)의 절반.
- **조사(실제 사이트 확인)**: Vercel·Stripe·건축/전시 사이트에서 검증된 "과하지 않은" 수치 = 패턴 투명도 **8~20%** · 격자 간격 **16~24px** · 선 **1px** · 그레인 **4~6%** · 사진 워시 8~15%. 목표는 "어? 뭔가 있네"가 아니라 **"화면이 왜 이렇게 좋지?"**.
- **적용(3겹, 각각 기준 하한 근처로 옅게 깔고 포갬)** — 한 겹만 쓰면 밋밋하거나 과해지기 때문:
  · `.ht-mesh` **무대 조명** — 중앙 브랜드 블루 광원(13%)+바이올렛(10%). 방탈출=무대·조명, 워드마크가 조명 아래 놓인 느낌. (라이트 배경에서 가장자리를 어둡게 하면 얼룩처럼 보여서 중앙 광원 방식)
  · `.ht-grid` **설계도 모눈** — 22px 간격·1px·7.5%. 슬로건 "기술로써 구현한다"와 직결. **글자 뒤는 마스크로 비워** 워드마크와 안 싸우게. 격자는 흐르면 어지러워서 애니메이션 없음.
  · `.ht-noise` **그레인** — 5%(전역 .grain 2.5% 위에 히어로만 추가).
- 🔴 **같이 고친 것 — 라벨 대비 미달**: 배경이 진해지자 `PREMIUM ESCAPE ROOM`(밝은 블루 #2f6fe0)이 **3.8:1** 로 떨어져 AA(4.5:1) 미달이 됨(12px 작은 글자라 큰글자 예외 3:1 도 못 씀). **딥블루(--brand #043cb2)로 교체 → 7~8:1 통과.** (원래도 4.0:1 로 아슬아슬했던 것)
- **검증**: 배경 실제 픽셀을 뽑아 대비 계산 — 본문 **14.5~16:1** · 라벨 **6.9~8.0:1** 전부 AA 통과. PC(1440)·모바일(390) 렌더 확인. 이미지 0장(전부 CSS) → 성능 영향 없음.
- 샘플 10안 페이지는 결정 후 삭제(`_bg-samples.html`). 나머지 9안 = 그리드 단독·도트·조명 단독·메시 강화·설계도 도면·세트 사진 워시·종이 질감·해저드 사선·등고선.

## 2026-07-16 — 🔤 히어로 슬로건 글씨체를 헤더 메뉴와 통일

- "환상을 기술로써 현실에 구현한다." 가 **나눔명조(세리프)** 라 헤더 메뉴(콘텐츠·리뷰·자주 묻는 질문)와 따로 놀았음 → **Pretendard**(본문/헤더와 같은 서체)로 통일. 굵기는 800→700, 자간 -.01em(고딕이 명조보다 벌어 보여서).
- 검증: 슬로건과 `nav.main a` 의 실제 적용 폰트 스택이 **문자열까지 동일**함을 브라우저에서 확인.
- 📌 `--font-display`(나눔명조)는 다른 섹션 제목들에서 계속 씀 — 히어로 슬로건만 바꿈.

## 2026-07-16 — 🐛 FANTASY 의 Y 오른쪽이 잘리던 것 수정 (사장님 발견)

- 증상: 히어로 `FANTASY + TRICK` 단계에서 **Y 의 오른쪽 26% 가 잘림**.
- 원인: Y 를 접는 애니메이션이 `max-width:1.1ch` 였는데 **`ch` = 폰트의 "0"(숫자 영) 글자 폭**이다. 새 로고 폰트엔 **숫자가 없어서** 브라우저가 규격대로 0.5em(=58px)으로 때웠고, Y 의 진짜 폭은 **.743em(=86px)** 이라 22px 가 잘려나갔음. (Bodoni 때는 0 이 있어서 안 걸리던 함정)
- 수정: `@keyframes htDy` 의 `1.1ch` → **`.8em`**(로고 폰트 한 칸 advance = .743em 기준). CSS 에 경고 주석 남김.
- 검증: 잘린 양 86.2px → **0px**. 접히는 마무리(결합 후 Y 폭 0)도 그대로 정상.
- 📌 **이 폰트를 쓰는 곳에선 `ch` 단위 금지** — 숫자 글리프가 없어 항상 0.5em 으로 계산됨.

## 2026-07-15 — 🔤 히어로 FANTASTRICK 글씨체 = 로고 글씨체 (전용 폰트 직접 제작)

> 사장님 요청: "첫 화면 가운데 큰 FANTASTRICK 을 상단 로고의 글씨체로". **로고 서체는 시중 폰트가 아니어서**, 로고 이미지의 글자를 그대로 떠서 **전용 웹폰트를 만들었음**.

- **왜 만들었나**: 로고는 PNG 이미지라 폰트 정보가 없음. 무료 폰트 20종(Michroma·Aldrich·Orbitron 등)과 비교했지만 **똑같은 게 없었음** → 비슷한 걸로 때우는 대신 로고 글자 자체를 벡터로 추출.
- **로고 서체의 규칙(추출하며 알아낸 것)**: 글자 한 칸 **375×375** · **10×10 격자(37.5 단위)** · **획 두께 75** · 자간(advance) **398**. 모듈러(격자) 서체였음.
- **검증**: 만든 폰트로 찍은 FANTASTRICK 을 로고 원본과 겹쳐 **픽셀 비교 → 1.4% 차이**. 글자 높이(140px)·좌우 폭이 **완전히 일치**했고 남은 차이는 테두리 안티에일리어싱뿐 = **로고와 같은 글씨**.
- ⚠️ **Y 는 로고에 없어서 새로 그림**: 히어로 애니메이션이 `FANTASY + TRICK` → `FANTASTRICK` 이라 Y가 필요한데 로고엔 Y가 없음. 같은 격자·같은 획 두께(75)·45도 팔로 직접 그림.
- ⚠️ **들어있는 글자 = F A N T S R I C K 과 Y 뿐**(1KB). 한글·숫자·소문자 없음 → **워드마크(`--font-logo`) 외에는 쓰지 말 것**. 리뷰 점수 등 나머지 Bodoni Moda 는 그대로 둠.
- 파일: `public/fonts/fantastrick-logo.woff2`(1KB). 만드는 스크립트 = `scripts/extract-logo-letters.cjs`(로고 PNG → 글자별 벡터) → `scripts/build-logo-font.cjs`(→ woff2). 다시 만들 땐 potrace·jimp·svg2ttf·ttf2woff2 필요(**앱 의존성엔 안 넣음** — 폰트가 이미 만들어져 있어 빌드엔 불필요).
- 히어로 3곳(`.ht-wm`·`.ht-ghost`·`.ht-shine`)만 `--font-logo` 로 교체. 자간은 폰트에 이미 들어 있어 `letter-spacing:0`.
- 검증: 빌드 통과 · PC/모바일 렌더 확인 · 결합 애니메이션(FANTASY+TRICK→FANTASTRICK)과 글린트 정상 작동.

## 2026-07-15 — 🤖 입금을 '자동매칭이 잡았나 / 사장님이 눌렀나' 표시 (마이그레이션 `migration_paid_source_APPLY_ME.sql` 적용 완료)

> 사장님 요청: "입출금 내역에 자동매칭이 처리한 건지 홈페이지에서 버튼 눌러 처리한 건지 표시 가능한가?" → 지금 붙임.

- `reservations.paid_source` 칸 추가: **manual**(사장님이 [입금 확인] 버튼) / **auto**(자동매칭 프로그램) / **빈값**(이 기능 전 기록).
- 보이는 곳: **입출금 내역**의 입금 줄에 `✋ 수동` · `🤖 자동` 배지 / **예약 상세**의 '입금 확인' 줄에 `· ✋ 사장님이 직접` / **CSV 내보내기**에 '처리' 칸 추가.
- 서버(`api/admin/reservations` PATCH): 입금 확인이 켜지는 순간 기록. **관리자 화면은 아무것도 안 보내므로 자동으로 `manual`** 이 되고, 자동매칭이 `paid_source: "auto"` 를 보낼 때만 `auto`. 엉뚱한 값은 `manual` 로 떨어짐(아무 문자열이나 못 들어감). 입금 확인을 되돌리면 같이 비워짐.
- 검증(실제 서버 API 호출): ① 버튼처럼 호출 → `manual` ② `auto` 보냄 → `auto` ③ 입금 취소 → `null` ④ `"hacker"` 보냄 → `manual`. 4/4 통과, 검증용 예약 3건은 삭제함.
- ⚠️ **이 SQL 전에 입금 확인된 예약은 값이 비어 있어 배지가 안 붙습니다**(지어내지 않고 비워둠). 오늘 이후 확인 건부터 붙음.
- 📌 **아직은 전부 `✋ 수동`으로 나옵니다** — 자동매칭(`bank-auto`, `D:\test3\bank-auto`)은 옛 워드프레스에 붙어 있고 이 사이트와 연결 안 됨. 나중에 붙일 때 **PATCH 에 `paid_source: "auto"` 만 같이 보내면** 바로 구분됨(받을 준비 끝).

## 2026-07-15 — 🕘 돈·취소가 '언제' 일어났는지 표시 + ⚠️ 화면 시각이 9시간 어긋나던 버그 수정

> 사장님 요청: "환불 완료 버튼 누른 날짜·시간도 기록" + "손님이 예약 취소한 시간도 표시"

- **환불 완료 시각**은 원래 DB에 잘 기록되고 있었어요(`refunded_at` — 버튼 누른 순간 저장). 문제는 **화면 어디에도 안 보였던 것**. [입금·환불 › 환불 처리]의 '최근 환불 완료' 목록이 **예약일**을 보여주고 있었는데 → **환불을 보낸 시각**으로 바꿈(통장 이체기록과 맞춰보는 자리라 이게 맞음).
- **예약 상세**에 시각을 다 보여줌: 접수 · 입금 확인 · **취소(+누가 취소했는지)** · 환불 완료.
- **누가 취소했나** — SQL 추가 없이 구분됨(`lib/money.ts` 의 `cancelledBy()`): 손님 취소는 환불 계좌를 반드시 입력받으므로 계좌가 남음 / 자동취소는 메모에 '자동 취소' / 나머지는 관리자 취소.
- 🔴 **버그 수정 — 화면의 시각이 전부 9시간 빨랐음**: DB는 UTC로 저장하는데 화면에서 `created_at.slice(11,16)` 처럼 문자열을 그냥 잘라 써서, 밤 **19:30** 접수가 **10:30** 으로 보였음. `lib/util.ts` 에 `formatStamp()` · `formatStampShort()` · `formatStampTime()` · `kstDateOf()` 를 만들고 **9곳**(예약 상세·변경 이력·입출금 내역·CSV 2종·자동취소 목록·문자 발송내역·리뷰·예약 옮기기)을 한국시각으로 통일. 앞으로 시각을 화면에 낼 땐 **반드시 이 함수들을 쓸 것**(util.ts 주석에도 경고 적어둠).
- 🔴 **덤으로 고친 것** — '오늘 자동취소된 예약' 목록과 입출금 내역 기간 필터가 UTC 날짜로 비교하고 있어 **새벽 0~9시 건이 전날로 밀렸음**(`kstDateOf()` 사용으로 해결).
- ✅ **자동매칭 / 수동 표시 (아래 항목에서 추가함)**

## 2026-07-15 — 🧪 테스트 예약 11건 (관리자 화면 둘러보기용)
> `scripts/seed-test-reservations.mjs` — 넣기 `node scripts/seed-test-reservations.mjs` / **지우기 `node scripts/seed-test-reservations.mjs --clean`**

- 전화번호가 전부 **010-0000-****** 이라 진짜 손님과 절대 안 섞이고, `--clean` 이 이 번호만 지웁니다(진짜 예약은 안 건드림).
- 담은 상황: 오늘 확정 3건(입금자명 다른 건 포함) · 미입금 대기 · 내일/모레 예약 · **취소 4건**(환불대기 100% / 환불완료 80% / 당일취소 0% / 미입금 자동취소) · 지난 이용 완료. 변경 이력(접수→입금확인→취소→환불완료)도 같이 넣어 이력 보기가 채워집니다.
- ⏳ **미입금 대기 건은 30분 지나면 진짜로 자동취소됩니다**(기능이 살아있는 것). 대기 화면을 다시 보려면 `node scripts/seed-test-reservations.mjs` 를 한 번 더 실행.
- 넣자마자 새벽 예약 테스트 1건이 자동취소됐는데, **지금이 오전 10시 30분을 지났으니 정상 동작**입니다(자정 예외는 10:30까지만).

## 2026-07-15 — 🎭 테마 상세 페이지 · 자주 묻는 질문 · 자정 예외 (비교 보고서 1·2·3·5·6번)
> `docs/기존사이트_비교_빠진것.html` 에서 고르신 항목. **손님 화면**에 없던 정보를 채웠어요.

**[1·3·5·6] 테마 상세 페이지 (`/rooms/[테마]`)** — 새로 만듦. 예전엔 홈에서 테마 카드를 누르면 **바로 예약폼**이라, 손님이 **가격도 스토리도 모른 채** 예약부터 하게 됐음.
  · **이용 금액** — 인원별 표 + 예약금. 테마마다 값이 다름(태초 34/32/30천원·인, 사자 29/28/27천원·인, 락다운 3인 120,000·4인 140,000 회차고정, 영속성 3인 63,000 전액).
  · **시놉시스 · 주의사항** — 기존 `fantastrick.co.kr/rooms/*` 원문 그대로. **인원 · 소요시간 · 난이도 · 장르** 한눈에.
  · **오시는 길** — 테마가 있는 매장만(1호점/2호점/TGC 각각 주소·전화·지하철·공영주차장) + **사업자 정보** 하단 표기.
  · 예약금은 관리자 설정값(`depositOf`)을 읽음 → 사장님이 금액을 바꾸면 **이 페이지도 같이 바뀜**(문구가 따로 놀지 않음).
  · 홈 테마 카드 링크: `/reserve?theme=…` → `/rooms/…` 로 변경.

**[2] 자정 이후 예약은 오전 10시까지 유예** (`lib/expire.ts`) — 🔴 **지키지 못할 약속이던 것을 고침**. 문자엔 "(자정이후 예약자의 경우 익일 오전10시까지)" 라고 보내놓고, 실제로는 **30분 뒤에 취소**해버리고 있었음.
  · 자정(KST 0시) 이후 접수 건은 **오전 10시 30분까지** 안 지움(사장님이 10시부터 순차 확인하니 30분 여유). 밤 11시 예약은 자정 전이라 그대로 30분 룰.
  · 관리자 화면 카운트다운도 수정 — 새벽 예약에 "3분 남음" 이라고 **거짓말하던 것**을 실제 마감(10:30)으로 계산하고 `새벽 예약` 표시를 붙임.
  · 검증: 7가지 상황(새벽2시→3시/10:20/10:40, 밤11시→0:05, 낮3시→3:20/3:40, 오전10:05→10:40) 전부 기대대로.

**[자주 묻는 질문 (`/faq`)]** — 새로 만듦. 헤더 메뉴에 추가. 기존 사이트의 FAQ 페이지는 **내용이 비어 있던 껍데기**라 참고할 게 없었고, **실제 운영 규칙에서만** 뽑아 썼음(지어낸 답 없음).
  · 💰 가격·결제(테마별 금액·예약금·계좌·입금 기한+자정 예외) / 📅 예약·취소(7일 전 21시 오픈·10분 전 마감·환불·조회취소·지각) / 🎭 이용 안내(인원·음주·복장) / 🚇 오시는 길(주차·매장 3곳).

**✅ 시간의 영속성 금액 확정 (사장님 확인)**: 기존 사이트엔 이용금액 60,000원(가오픈가)과 예약금 63,000원이 같이 적혀 있어 서로 안 맞았는데, **63,000원이 현재 금액**이고 **60,000원은 안 쓰는 가오픈 가격**이라고 확인받음. `(가오픈가)` 표기를 떼고 `3인 63,000원`으로 정리(테마 상세·FAQ 같이 반영 — 두 화면 다 `theme-content.ts` 한 곳을 읽음).

## 2026-07-15 — ↩️ 폰 알림(5번) 취소 — 사장님 요청
> 이유: 알림을 켜려면 Vercel 환경변수(VAPID 키)를 넣어야 하는데, 그 페이지에 들어가려면 **Vercel 2단계 인증 설정**을 먼저 해야 했음. **알림 하나 때문에 계정 보안 설정까지 하게 되는 게 과함** → 취소.
- 제거: `lib/push.ts` · `api/admin/push/route.ts` · `public/sw.js` · `supabase/migration_push_APPLY_ME.sql` · 설정의 `🔔 폰 알림` 탭(`PushTab`·`urlBase64ToUint8Array`·`guessDevice`) · `api/reservations` 의 `pushToAdmins` 호출 · `.env.local` 의 VAPID 3줄 · `web-push`·`@types/web-push` 패키지.
- **등록된 기기 0대**라 지우기 전 확인 — 아무도 구독한 적이 없어 깨끗하게 제거됨. 서비스워커가 남아 유령처럼 도는 문제도 없음.
- DB의 `push_subscriptions` 표만 빈 채로 남음. **그냥 둬도 아무 일 안 함**(등록 0대). 지우고 싶으면 `supabase/migration_push_DROP_optional.sql` (선택).
- 검증: 타입·빌드 통과 / **손님 예약 정상**(푸시 코드 뗀 자리) / `api/admin/push`·`/sw.js` 404 확인 / 잔여 참조 0.
- 📌 나머지 **11개 기능은 전부 그대로**. 관리자 화면을 켜놨을 때 오는 🔔 새 예약 알림도 그대로 작동.

## 2026-07-15 — 🛠 관리 편의기능 12종 (사장님 선택: 1·3·5·6·7·11·12·13·14·16·19·20) — ※[5] 폰 알림은 이후 취소됨(위 항목 참고)
> `docs/관리자_추천기능_20.html` 에서 고르신 12개. 마이그레이션 2개 적용(`migration_admin_v2_APPLY_ME.sql` 완료 / `migration_push_APPLY_ME.sql` — 알림용, 적용 시 켜짐).

**[1] 오늘 (관리자 홈)** — 로그인하면 바로 뜨는 첫 화면. 예전엔 7월 달력이 떠서 "오늘 뭐 하지"를 알려면 날짜→테마탭 4번→입금·환불탭까지 눌러야 했음.
  · **다음 손님**을 제일 크게(시각·이름·인원·테마·전화·입금여부) · 처리할 일(입금/환불) 있을 때만 안내 + 바로가기 · 오늘 예약 **전 매장·전 테마 시간순** 한 화면 · 30초 자동 갱신. (Checkfront Daily Manifest · FareHarbor Manifest 패턴)

**[3] 전화 바로 걸기** — `Phone` 컴포넌트(`tel:`/`sms:`). 8곳에 흩어진 전화번호를 한 곳으로. 날짜별 슬롯행은 버튼 안에 링크를 못 넣어 버튼 밖으로 분리.

**[5] 폰 새 예약 알림 (웹푸시)** — 관리자 화면을 안 켜놔도 폰에 알림. 지금 🔔알림은 화면을 띄워놔야만 와서, 현장에 있으면 **30분 자동취소가 먼저** 돌아 예약이 날아감.
  · `lib/push.ts`(VAPID) · `public/sw.js`(서비스워커 — **캐싱 안 함**: 캐싱하면 배포해도 옛 화면이 남는 사고) · `api/admin/push`(등록/해제/테스트발송) · 설정 › 🔔 폰 알림 탭(기기 목록·테스트).
  · 죽은 구독(404/410)은 자동 삭제. **알림 실패가 예약을 막지 않음**(검증함).

**[6] 예약 옮기기** — 취소·재등록 없이 날짜/시간/인원 변경. 예전엔 취소→재등록뿐이라 **환불율이 계산되고 환불 큐에 뜨고 입금상태가 초기화**돼 "손님은 그대로 오는데 장부가 취소 1건 + 신규 1건"이 됐음.
  · 이미 찬 칸이면 **이름까지 알려주며 차단**(`uq_res_slot` 과 같은 기준 — 취소건은 칸을 안 차지) · 시간표를 모달 열 때 미리 받아둠(로딩 전 후보가 현재 시간 하나뿐이라 "옮길 데 없음"으로 오해).

**[7] 테마별 예약금 수정** — 예전엔 "변경은 개발자에게 요청"이었음. `app_settings.theme_deposits`(**SQL 불필요** — 보고서에 "SQL 필요"라 적은 건 내 오류). 기본값과 같으면 저장 안 함(코드 기본값을 바꾸면 자동으로 따라감). `depositOf()` 로 손님예약·관리자등록이 같은 기준. **문자 문구에도 금액이 나가므로 화면에 경고**.

**[11] 문자 재시도·검색** — 실패 건에 [다시 보내기](로그의 body 그대로 — 그 사이 문구를 고쳤어도 원래 안내대로) · 발송내역 이름·전화 검색 · 실패만 보기. 예전엔 "저 문자 못 받았어요" 전화가 오면 50건을 눈으로 훑어야 했음.

**[12] 입금자명** — 통장에 찍힌 이름(친구·엄마·회사). 예약자와 다르면 표시. 예전엔 메모에 손으로.

**[13] 이상한 예약 경고** — 손님 카드에 경고를 띄운다. 1인이라 실수를 잡아줄 사람이 없으니 화면이 잡는다.
  · **같은 날 같은 시간에 다른 테마도 예약**(몸이 두 개가 아닌 이상 불가) · 같은 날 여러 건(연달아 두 테마는 가능하므로 알려만 줌) · 상습 취소(3회 이상) · 노쇼 횟수 배지. 색은 앰버(레드는 "되돌릴 수 없음" 전용).

**[14] 예약 변경 이력** — `reservation_logs`. **"누가"는 안 남김**(1인이라 무의미), "언제 뭐가"만. 접수·입금확인·확정·취소·노쇼·환불완료·시간옮김·인원변경·메모 + 손님 직접 취소.

**[16] 손님 카드** — 전화번호 기준 과거 이력 · **N번째 방문** · **아직 안 한 테마**(권해드릴 거리) · 노쇼 횟수. 전화번호 인덱스가 이미 있는데 화면에서 안 쓰던 것.

**[19] 통짜 백업** — `api/admin/backup` — 예약·리뷰·설정·문자문구·휴무·변경이력을 **JSON 파일 하나**로. Supabase 무료 플랜은 시점 복구가 없어 실수로 지우면 끝인데, 기존 CSV는 예약·장부뿐이라 **리뷰·시간표·문자문구는 백업 수단이 아예 없었음**(문자 문구 4종×4테마 재작성 = 반나절).
  · 🔒 **손님 비밀번호(pin)·취소토큰(cancel_token)은 일부러 제외** — 파일이 새면 남의 예약을 취소할 수 있음. 실제 필드를 훑어 안 들어가는 걸 검증. 표 하나가 없어도 백업 전체가 실패하지 않게 각각 감쌈.

**[20] 돈이 움직인 날짜** — `paid_at`·`refunded_at`. **입출금 내역을 "예약일 기준" → "돈 오간 날 기준"(거래 단위)** 으로 전환. 7월 입금 + 8월 환불이면 각 달에 나뉘어 잡힘 = **통장과 맞음**. (입금·환불 탭 만들 때 남긴 한계를 해소)

- 검증(E2E, 실데이터 후 원복): 전화/문자 링크 · 입금자명 저장 · paid_at 기록 · 자동확정 · 변경이력(`입금확인 30,000원 · 입금자 엄마이름`, `대기→확정`, `시간 옮김 20:00 → 22:00`) · 예약금 4테마 수정→DB→공개설정→원복 · 문자 이름/전화 검색·실패만·재발송 안내 · **옮기기 충돌 4종**(찬 칸 차단/빈 칸 성공/**취소된 자리는 허용**/잘못된 시간 차단) · 오늘 화면(다음 손님·처리할 일·전 테마 시간순) · **알림 표 없어도 손님 예약 정상**(알림 실패가 예약을 막지 않음) · 같은 폰 두 번 등록해도 중복 없음 · 백업(pin·cancel_token 실제 필드 제외 확인, 비로그인 401) · 이상 예약 경고(같은 시간 두 테마) · JS 에러 0.
- 📌 **폰 알림은 실제 폰에서 최종 확인 필요** — 헤드리스 브라우저는 푸시 서비스 연결이 안 돼 구독까지는 자동 검증 불가. 폰에서 `홈 화면에 추가` → 설정 › 🔔 폰 알림 › `이 기기에서 알림 받기` → `테스트 알림 보내기`.
- 📌 **배포 환경변수 추가 필요** — Vercel에 `NEXT_PUBLIC_VAPID_PUBLIC_KEY` · `VAPID_PRIVATE_KEY` · `VAPID_SUBJECT` (로컬 `.env.local` 에는 넣어둠). 없으면 알림만 안 되고 나머지는 정상.

## 2026-07-15 — 🗑 예약 탭의 "월 전체" 보기 삭제 (예약 = 날짜별 + 목록·검색 2개)
> 사장님이 처음엔 "날짜별만 남기고 월전체·목록검색 다 삭제"를 요청. **목록·검색에만 있는 기능**(이름·전화 검색 / 취소 되돌리기 / 취소건 보기 / CSV / 통계)을 확인해 알려드리고, 특히 **입금·환불 탭이 "목록·검색에서 취소 되돌리기 하세요"라고 직접 안내**하고 있어 지우면 그 안내가 거짓말이 되는 점을 짚음 → **"월 전체만 삭제"로 결정**.
- **`CalendarTab` 삭제** — 날짜별과 **같은 달력**인데 오히려 기능이 적었음(오늘 표시·자동 새로고침 없음, **읽기 전용이라 처리 버튼 0개**). 코드로 전수 확인 후 삭제해 **기능 손실 0**.
- 예약 탭 보기: `[📅 날짜별][📋 목록·검색]` 2개. 기본은 날짜별.
- 검증: 최상단 4탭 정상 / 월 전체 사라짐 / 기본=날짜별 + 달력·테마탭 렌더 / **목록·검색 검색칸 살아있음** / 입금·환불·리뷰·공지·설정 정상 / JS 에러 0.

## 2026-07-15 — 💰 "입금·환불" 탭 신설 (돈 흐름 전용 화면)
> 사장님 요청: "예약금, 예약금 처리 내역, 환불 요청, 환불 처리 결과 같은 탭으로 아예 새로 빼고, 다른 홈페이지 구성을 참고해서". 지금은 이 정보가 목록·검색 안에 파묻혀 **"오늘 돈으로 처리할 일이 몇 건인지"를 한 화면에서 볼 수 없었음**. `admin-dashboard-ux-designer` 에이전트가 실제 사례를 조사해 설계.
- **참고한 구성(조사)** — 카페24 `주문 > 입금 전 관리`(무통장은 **상태별 큐를 별도 메뉴**로 두고, 처리하면 **다음 큐로 이동해 목록에서 사라짐**) · 포트원 관리자콘솔 결제내역(상단 집계 = **총 거래액/총 취소금액/순 거래액**) · x-y.net 펜션 예약솔루션(**입출금내역/장부** 별도) · 채널톡 주문관리v2(무통장은 판매자가 직접 환불).
  - **채택**: 상태별 큐 + 처리하면 사라짐 + 끝난 건 "내역"으로 뒤에. **버림**: 일괄 입금확인 체크박스(우리는 입금확인이 **자동 확정+문자 발송**이라 오체크 1번 = 손님에게 잘못된 확정문자) · 자동 입금대사(유료 미가입).
- **탭 구조** — 최상단 4탭(`예약 / 입금·환불 / 리뷰·공지 / 설정`). 최상단인 이유는 **뱃지**: 다른 탭에 있어도 "할 일 N건"이 보임. 내부는 `.viewtoggle` 3개 — **💰 입금 확인 / 💸 환불 처리 / 📒 입출금 내역** (할 일 큐 2 + 지난 일 1).
- **💰 입금 확인** — 30분 자동취소(`expire.ts`)라 사실상 실시간 화면: **⏳ 남은시간 카운트다운**(1초 갱신, 5분↓ 앰버) + 30초 폴링 + **이름·금액을 나란히**(카카오뱅크 앱과 눈으로 대사). 처리하면 큐에서 사라짐. 하단에 **오늘 시간초과 자동취소 건**(늦게 입금한 손님 살리기).
- **💸 환불 처리** — 행을 **항상 펼쳐 둠**(계좌를 봐야 일이 시작되므로 클릭 1회 제거). **📋 계좌 복사**(하이픈 제거 → 은행앱 붙여넣기, clipboard 실패 시 prompt 폴백) → 이체 → **`✓ 96,000원 환불 완료`**(버튼 라벨에 금액을 박아 오송금 방지). 예금주≠예약자면 경고.
- **📒 입출금 내역** — 기간·구분(전체/입금만/환불만) 조회 + **받은 예약금 / 돌려준 금액 / 실수령** 3집계 + CSV(계좌·전화 제외 — 개인정보).
- **`lib/money.ts` 신설** — `isRefundPending()`·`refundAmount()`. **서버 통계와 화면 목록이 같은 기준**을 쓰게 한 곳에 모음(갈라지면 "뱃지 2건인데 목록엔 1건" 사고).
  - 환불 대기 = 취소됨 + 입금했었음 + 미환불 + 환불율>0 + 계좌 있음. → **30분 자동취소는 `deposit_paid=false`·`refund_rate=null` 이라 자동 제외**, 당일취소(0%)도 제외. 운영 DB 실데이터로 검증(자동취소 6건·0% 1건 → 환불대기 0건).
- **`route.ts`** — `buildStats` 에 `pendingUnpaidSum`·`refundPending`·`refundPendingSum` 추가. **`expire.ts`의 `EXPIRE_MINUTES` export** (카운트다운이 하드코딩되면 나중에 화면만 거짓말).
- **목록·검색에서 돈 UI 회수** — 💰입금대기 필터 버튼·`fDeposit` 상태 삭제, `.refbox` 환불 처리 → **안내만**(입구는 입금·환불 탭 하나), 돈 통계 2장(월 확정 예약금·입금확인 합계) 이사 → 건수 5장만.
- **색 규칙 위반 교정** — `.refbox` 가 **빨강**이었음. 환불은 "되돌릴 수 없음(레드)"이 아니라 **"내가 처리해야 함(앰버)"** → 앰버로. 할 일 뱃지도 `.tt-badge`(빨강) 대신 **`.vt-badge`(앰버)** 신설.
- 검증(E2E, 실제 데이터 삽입 후 원복): 탭 뱃지 2 ✅ / 카운트다운(20분 전 생성→"10분 남음") ✅ / 입금확인→DB `confirmed`+`deposit_paid` ✅ / 큐에서 사라짐 ✅ / **환불액 120,000×80%=96,000 계산** ✅ / 버튼에 금액 표기 ✅ / 계좌 즉시 노출 ✅ / **장부 150,000−96,000=54,000 실수령** ✅ / 필터(입금만·환불만) ✅ / 모바일 가로넘침 0 ✅ / JS 에러 0.
- 📌 **별도 과제(스키마 변경 필요 — 지금 안 함)**: 입금·환불 **처리 시각** 칼럼(`paid_at`·`refunded_at`) — 없어서 입출금 내역이 **"예약일 기준"**임(거래일 기준 장부 불가). 진짜 정산이 필요해지면.

## 2026-07-15 — 🧹 중복 기능 정리 + 안 쓰는 기능 삭제 (예약받을테마·외부노출)
> 사장님 요청. 삭제 전 **DB 값이 전부 비어 있음을 확인**하고 지워서 손님 화면 변화 0. 되살리려면 이 커밋 이전 참고.
- **마감 기능 중복 정리** — `[예약 › 날짜별]`의 칸별 `마감`과 `[설정 › 휴무·마감]`의 폼이 같은 일(날짜+시간+테마 마감)을 하고 있었음. → 설정은 **"휴무일 추가"(하루 종일·전 테마)만** 남기고 시간·테마 선택 제거. 칸 하나 마감은 날짜별 보기에서.
  - 🐛 **덤으로 발견한 버그**: 설정의 시간 선택 목록이 **옛 전역 시간대**(10:00·11:30…)라 실제 테마 시간표(사자의 서 12:30·13:40…)와 안 맞았음 → 여기서 고른 시간은 **어떤 예약칸도 못 막는 무용지물**이었음. 제거로 해소.
  - 목록에는 날짜별 보기에서 만든 칸 마감도 함께 보이게 유지(어디서 막았는지 놓치지 않게). 표기: `13:00 마감` / `하루 전체 휴무`.
- **"예약 받을 테마" 삭제** — 관리자 UI + `AppConfig.disabledThemes` + `api/reservations` 검증 + `reserve` 화면 필터까지 **끝까지 제거**(관리자에서만 지우면 DB 값이 남아 손님 화면에 계속 영향). DB `disabled_themes` 도 삭제(값 `[]` 였음).
  - ⚠️ 이로써 "특정 테마 예약 안 받기"(테마 점검 등) 기능이 없어짐. 필요해지면 되살려야 함.
- **"외부 노출(네이버·구글)" 삭제** — 관리자 UI + `AppConfig`(naverUrl·googleUrl·extRating·extCount) + `api/admin/settings` 저장 + **홈(`page.tsx`)·후기(`reviews/page.tsx`)의 노출 코드**까지 제거. DB 4개 키 삭제(전부 빈 값/0 이라 손님 화면 변화 없음).
  - 홈 후기 요약의 평점 폴백(`ext.extRating`)·"구글에서 더 보기"·"네이버 →" 링크, 후기 페이지의 외부 리뷰 버튼이 함께 사라짐(원래도 값이 비어 안 보이던 것들).
- **설정 탭 1열로** — 카드가 2장(예약 규칙 / 예약 시간표)이 되어 2열이면 오른쪽이 비므로 한 열로.
- 검증: 빌드·타입 통과, 손님 3화면(홈·후기·예약) 200·렌더 정상, **예약화면 테마 4개 그대로**, 휴무일 추가→DB 저장(`time:null, theme_id:null`)→목록 표기→해제 원복 전 과정 통과, JS 에러 0.

## 2026-07-15 — 🎨 관리자 화면 정리 (탭 7→3, 색·정렬 규칙 통일) — 기능 변경 0
> 탭이 늘며 기능이 흩어지고 색·정렬 규칙이 없어진 상태를 정리. **동작은 하나도 안 바꿈**(배치·색·묶기만). 실화면 16장(PC/모바일 × 8탭)을 캡처해 근거로 분석 후 적용. 비교 캡처: `docs/_ux/`(before/after — 예약목록·설정·모바일탭).
- **🐛 정렬이 죽어 있던 진짜 원인** — `.sp`(우측 밀기)가 `.admin-top .sp{flex:1}` 로 **`.admin-top` 안에서만** 정의돼 있었는데, 실제 사용처 7곳 중 5곳이 그 바깥이라 **우측 정렬이 통째로 무효**였음(슬롯행 액션이 왼쪽에 뭉쳐 있었음). → `.admin-wrap .sp` 추가로 **한 줄로 복구**.
- **탭 7 → 3** — `예약`(날짜별/월 전체/목록·검색) · `리뷰·공지`(후기/팝업 공지) · `설정`(예약 규칙·시간표/휴무·마감/문자 문구). 2단계는 기존 `.viewtoggle` 재사용. **컴포넌트 삭제 없음** — `CalendarTab`·`SlotsTab`·`SmsTab`·`NoticeTab`·`ReviewsAdminTab` 전부 위치만 이동(기능 손실 0, 8개 화면 전수 확인).
- **색 의미표 한 벌로** — 앰버=내가 처리해야 함 / 그린=끝남 / 회색=지난 일 / 레드=되돌릴 수 없음. 적용: ① 통계 숫자 색 제거(`.stat b`→`--text`), 앰버(확정 대기)만 남김 ② **취소 배지 빨강→회색**(`.st-cancelled`) — 목록이 온통 빨간 줄무늬였고, 취소는 위험이 아니라 지난 일. 이제 화면의 유일한 빨강 = `.btn.danger`.
- **통계 중복 제거** — `오늘 예약`·`확정 대기`가 **같은 값으로 두 번** 렌더되고 있었음(9장 → 7장). 보조 3장은 `.stat-row.sub3` 로 작게.
- **정렬 규칙 강제** — 우측 요소를 `.rt`(`margin-left:auto`)로 묶음. 기존엔 `.tname{flex:1}` 덕에 "우연히" 붙어 있어 **이름이 길면 배지가 줄바꿈**됐음. `SlotsTab`의 인라인 `marginLeft:auto` 도 통일.
- **모바일 탭바 깨짐 수정** — `캘/린/더`처럼 글자 단위로 쪼개져 3줄이던 것 → `white-space:nowrap` + 가로 스크롤 + 터치 44px. 탭 3개라 한 줄(45px)에 들어감.
- **버튼 위계** — `.btn.ok` 신설(인라인 초록 3곳 통합, `#137a4c`는 `.st-confirmed`가 쓰던 값이라 대비 AA). 상세 모달은 **지금 할 일 하나만 파랑**(미입금→[입금 확인] / 입금완료→[예약 확정]). 날짜별 보기의 `+예약 넣기` primary 7개 → 기본 버튼. 문자탭 파란 저장 6개 → 기본 버튼.
- **설정 탭 재배치** — 한 카드에 주제 8개·세로 1,670px·오른쪽 절반 공백 → **주제별 카드 3장(예약 규칙 / 외부 노출 / 예약 시간표) 2열** + **sticky 저장바**(실뷰포트에서 하단 고정 확인).
- **색 토큰 정리** — `--red`(정의된 적 없음→항상 브랜드 밖 폴백) → `--blood`. 관리자 별점이 `--gold`(=보라)라 손님 화면(주황 `#e0930c`)과 달랐던 것 통일. 캘린더의 옛 파랑(`59,123,240`) → 현재 `--cyan`(`47,111,224`).
- **잔버그** — 팝업공지 스위치가 이모지(⬜) 때문에 **체크박스가 두 개처럼** 보이던 것 → 상태 배지로. `accent-color:var(--brand)` 추가. 문자 문구칸 인라인 스타일 중복 2곳 → `.tpl-ta` 클래스, 공통 문구 `rows` 3→6(302자 문구가 잘려 보였음).
- 검증: 빌드·타입 통과, 8개 화면 전수 클릭, 모바일 가로 넘침 0, JS 에러 0.
- 📌 **분석 에이전트 신설** — `~/.claude/agents/admin-dashboard-ux-designer.md` (관리자·백오피스 대시보드 UX 전용). 다음 세션부터 호출 가능.

## 2026-07-15 — 🔁 미입금 자동취소 안내를 30분으로 통일 (문자 ↔ 실제 동작 불일치 해소)
> 기존 사이트 문자에는 "예약 후 **10분** 이내 미입금 시 자동취소"라고 적혀 있었으나, 우리 시스템의 자동취소는 **30분**(`lib/expire.ts` EXPIRE_MINUTES=30)이었음. 문자와 실제가 달라 손님이 혼란스러운 상태였음 → 사장님 지시로 **30분으로 통일**.
- **`lib/sms-templates.ts`** — 예약대기 문구 4개(테마별)의 `예약 후 10분 이내에 입금확인이` → `예약 후 30분 이내에`. **원문에서 일부러 바꾼 유일한 곳**이라 파일 머리말에 사유·주의사항 기록.
- ⚠️ **뜻이 다른 10분은 건드리지 않음** — 입금확정 문구의 `예약시간 10분 전 내방`(일찍 오세요)·`15분 이상 지각`은 그대로.
- **`lib/expire.ts`** — `EXPIRE_MINUTES` 옆에 "이 숫자를 바꾸면 문자 문구도 같이 바꿔야 한다"는 주의 주석 추가(다음에 또 어긋나지 않게).
- 검증: 원문과 전수 대조 → **의도한 변경 4건 / 의도치 않은 차이 0건**, "예약시간 10분 전 내방"·"15분 이상 지각" 유지 확인.

## 2026-07-15 — ⏱ 예약 임박 차단 (시작 10분 전부터 온라인 예약 불가)
> 사장님 지정: **시작 10분 전**. 시작이 코앞인 방을 손님이 덜컥 예약하는 걸 막음. 마이그레이션 불필요(app_settings 키만 추가).
- **`lib/data.ts`** — `isTooSoon(date, time, leadMinutes, nowMs)` 신설. 서버(UTC)·브라우저(KST) 어디서 계산해도 같은 답이 나오도록 `T{time}:00+09:00` 로 **한국시간 고정 해석**. leadMinutes=0 이면 지난 시간만 차단.
- **`lib/settings.ts`** — `AppConfig.minLeadMinutes`(app_settings의 `min_lead_minutes`), 기본 **10**. 0도 유효값(제한없음)이라 `isFinite` 로만 판정.
- **`api/reservations` POST** — 서버에서 차단(화면만 막으면 우회 가능). 메시지: "시작 N분 전부터는 예약할 수 없습니다. 매장으로 전화 주세요."
- **`reserve/page.tsx`** — 임박한 칸은 `⏱` 로 잠금. **30초마다 현재시각을 갱신**해 시간이 흐르면 실제로 잠기고, 이미 골라둔 시간이 그 사이 임박해지면 **선택을 자동 해제**(모르고 신청 방지). 안내문에 ⏱ 설명 추가.
- **관리자 전화예약(`api/admin/reservations` POST)은 이 제한을 받지 않음** — 임박 예약은 전화로 받아 사장님이 직접 등록하는 흐름이라 의도적으로 제외.
- **설정 탭** — "예약 임박 차단" 선택(제한없음/10분/30분/1시간/2시간).
- 검증: `isTooSoon` **경계 케이스 14건 전수 통과**(딱 10분=허용, 9분=차단, 지난 시간, 0/120분, UTC↔KST). E2E — 손님 임박 예약 400 차단 ✅ / 내일 칸 정상 접수 ✅ / **관리자 전화예약은 임박해도 통과** ✅ / 화면에서 13:00만 ⏱ 잠김 ✅. 테스트 데이터 원복.

## 2026-07-15 — ✏️ 테마별 문자 문구 편집 (관리자에서 직접 수정)
> 기존 사이트처럼 **문자 문구를 테마마다 따로** 편집할 수 있게 함. 마이그레이션 `migration_sms_theme_APPLY_ME.sql` **(사장님 적용 완료)** — `sms_templates` 에 `theme_id` 추가 + 기본키를 `(type)` → `(type, theme_id)` 로 교체.
- **문자탭 재구성** — `테마별` 딱지가 붙은 종류(예약대기·입금확정)는 **테마 탭**으로 골라 각각 편집. 문구마다 `기존 사이트 문구` / `✎ 직접 수정함` 표시. `↩️ 기존 문구로 되돌리기`(DELETE) 지원.
- **문구 우선순위** — 테마별 종류: `저장한 그 테마 문구 > 기존 사이트의 그 테마 문구 > 기본값` / 공통 종류: `저장한 공통 문구 > 기본값`.
- **⚠️ 만들다 발견한 사고 경로(수정함)** — 처음엔 테마별 종류에도 "모든 테마 공통" 탭을 뒀는데, 그 탭에 **태초의 신부 문구(예약금 3만)가 미리 채워져** 있었고 공통 문구가 테마별 문구를 **덮어쓰는** 구조였음. 사장님이 무심코 저장하면 사자의 서(2.5만)·락다운시티(12만) 손님에게 **3만원으로 잘못 안내**될 수 있었음. → 기존 사이트에 애초에 "공통" 개념이 없었으므로(테마별 16개뿐) **테마별 종류에서 공통 탭 제거** + 서버에서도 차단(`PER_THEME_TYPES`: 테마별은 테마 지정 필수, 공통 종류는 테마 지정 금지).
- 검증: 공통 저장 시도 차단 ✅ / 사자의 서 수정해도 락다운시티 문구 무오염 ✅ / 수정한 문구가 실제 발송에 반영 ✅ / 되돌리기 ✅. 테스트 데이터 DB 원복 완료.
- 📌 참고: 앞선 기록에서 "문자 로그 0건"이라 적었던 건 **오독**이었음(`channel` 칸이 없어 조회가 실패한 것을 0건으로 표시). 실제로는 7/13 이전 로그가 있었고, `channel` 을 넣기 시작한 7/13 커밋부터 기록이 끊겼던 것.

## 2026-07-15 — 📢 팝업 공지사항 (기존 modal-window 이식)
> 기존 사이트의 "팝업 공지사항"(modal-window 플러그인)을 이식. 사장님이 코드 없이 관리자에서 공지를 켜고 끌 수 있음. 빌드·타입 통과, 실브라우저 E2E 전 과정 통과(켜기→홈·다른페이지 노출→하루 안 보기→재방문 미노출→ESC→끄기), 테스트 데이터는 DB에서 원복.
- **기존 동작 그대로** (설정 원문 확인): 페이지 열자마자 표시(지연 0) · **모든 페이지**(`show=all`) · **쿠키 1일**(`use_cookies=yes, modal_cookies=1`) = "오늘 하루 보지 않기" · 닫기 = 우측상단 버튼 + **바깥 클릭**(`close_button_overlay=1`) + **ESC**(`close_button_esc=1`) · 폭 500px · 배경 흰색 · 오버레이 `rgba(0,0,0,.7)`.
- 기존 팝업은 2개(`DLC 연기안내`·`공지사항`) 모두 **꺼진 상태**였고 내용은 **이미지 한 장**이었음 → 우리도 이미지 지원 + 제목·본문 텍스트도 가능하게. 시작값은 꺼짐.
- **`lib/settings.ts`** — `Notice` 타입 + `AppConfig.notice`(app_settings의 `notice` 키). 내용이 바뀌면(`updatedAt`) "안 보기" 눌렀던 손님에게도 다시 보임.
- **`api/admin/settings`** — `sanitizeNotice`: 제목 120자·본문 2000자 제한, 이미지·링크는 `http(s)` 또는 `/` 로 시작하는 주소만 허용(`javascript:` 차단). 켤 때 제목·본문·이미지가 모두 비면 거부.
- **`components/NoticeModal.tsx`** 신설 + `layout.tsx` 에 마운트(모든 페이지). 본문은 `white-space:pre-wrap` 으로 **글자 그대로** 렌더(HTML 실행 안 됨 → 안전). "안 보기"는 localStorage(`fx-notice-hide`).
- **관리자 `팝업 공지` 탭 신설** — 켜기/끄기 스위치, 제목·내용·이미지주소·링크·노출 종료일·"안 보기" 기간(오늘하루/3일/7일/없음), **미리보기** 버튼.

## 2026-07-15 — ✉️ 입금확인 문자 + 기존 문자 문구 그대로 이식 (4종 × 4테마)
> 기존 사이트의 문자 **16개(4이벤트 × 4테마)**를 원문 그대로 이식. 사람이 옮겨적지 않고 원본 설정에서 **기계 변환** 후 역검증 → **16/16 글자단위 일치**. 바뀐 건 치환자 표기뿐(`%name%`→`{이름}`, `%date%`→`{날짜}`, `%time%`→`{시간}`, `%calendar%`→`{테마}`).
- **`lib/sms-templates.ts` 신설** — `reservation`(예약대기·계좌안내, 테마마다 금액 다름) · `payment`(입금확인→예약확정, **사자의 서만 인스타·길안내 추가**) · `cancel`(손님 취소) · `admin_cancel`(관리자 취소). 발신번호(사자의 서만 2호점 `01049950482`)·제목도 기록.
- **`lib/sms.ts`** — `getTemplate(type, themeId)`: **DB 문구 > 테마별 기존 문구 > 기본값**. `sendReservationSms`에 `theme_id` 전달.
- **입금확인 = 예약확정 (기존과 같은 방식)** — 입금 토글 시 대기→확정 자동 전환 + `payment` 문자 **1통만**. 안 합치면 [입금확인]·[예약확정] 두 번 눌러 비슷한 문자가 2통 갔음.
- 관리자 취소 시 `admin_cancel` 문자 추가. 문자탭에 `admin_cancel` 편집 추가.
- ⚠️ `payment`·`reservation` 은 **테마별 문구라 문자탭에 노출 안 함**(통째로 저장하면 사자의 서 문구가 날아감). 테마별 편집은 후속 과제.
- **🐛 별도 발견·수정** — `sms_log.channel` 마이그레이션이 **운영 DB에 미적용**이라 문자 로그가 통째로 저장 안 되고 있었음(코드가 오류를 조용히 무시). `writeLog()`로 실패를 서버로그에 남기게 수정 + `supabase/migration_sms_channel_APPLY_ME.sql` **(2026-07-15 사장님 적용 완료)**.

## 2026-07-15 — 📅 예약관리 "날짜별 보기" 신설 + 테마별·요일별 시간표 (기존 Booked 예약확인 이식 2차)
> 기존 `fantastrick.co.kr` 관리자의 **예약확인** 화면을 우리 관리자로 이식. 기존 사이트는 **읽기 전용으로 분석만**(로그인→조회→스크린샷, 무변경). 빌드·타입 통과, 실제 브라우저로 예약 등록→입금확인→취소→마감→해제까지 E2E 통과(테스트 데이터는 DB에서 완전 삭제해 원복).
- **⚠️ 큰 발견 — 시간표는 "매장별"이 아니라 "테마별·요일별"**: 7/17~23 7개 요일을 실측하니 테마마다 시작시각·간격이 전부 달랐음(사자의 서 70분 간격 9칸 / 락다운시티 2시간 간격 5칸). 3호점은 한 매장에 시간표가 다른 테마가 2개라 어제 만든 **매장별 시간표로는 표현 자체가 불가능**했음. 주말은 더 일찍 시작해 칸이 늘어남(태초의 신부 평일 7칸 → 주말 10칸).
- **`lib/data.ts`** — 실측 시간표를 `THEME_SLOTS` 상수로 심음(테마 4개 × 요일). `slotsForThemeDate()` 신설 — 우선순위 **테마 > 매장 > 전역**이라 기존 매장별·전역 설정은 그대로 살아있음(하위호환). `slotEndTime()` 추가.
- **`lib/settings.ts` · `api/admin/settings`** — `themeSlots`(app_settings의 `theme_slots` 키) 추가. 저장 시 `sanitizeSlots`로 알 수 없는 테마·잘못된 시간 제거(기존 매장별 정화 로직을 공용화).
- **`api/reservations` POST · `reserve/page.tsx`** — 손님 예약화면·서버 검증도 테마 시간표 기준으로 전환. 이제 실제 운영 시간표대로만 예약을 받음(기존엔 전 테마 공통 `10:00~22:00` 9칸이라 현실과 안 맞았음).
- **`admin/page.tsx`** — 예약관리에 **날짜별 보기**(기본) 신설: 달력(날짜별 건수) → 테마 탭(건수 배지) → 그 날 시간대별 행. 각 행은 손님이면 `이름·전화·인원`+입금/상태 배지(클릭 시 상세 모달에서 입금확인·확정·노쇼·취소·메모), 비었으면 `+ 예약 넣기`(테마·날짜·시간 자동 입력)·`마감`, 닫힌 칸은 `열기`. 기존 목록·검색·통계·입금대기 큐·CSV는 **목록·검색** 보기로 그대로 유지. 30초 폴링.
- **표시 규칙 — 시작시각만 표시(끝시각 없음)**: 기존 사이트 설정이 `booked_hide_end_times=on`(끝시각 숨김)이고, 저장된 시간대 원본을 열어보니 **끝시각 값 자체가 의미 없는 껍데기**였음(같은 9칸 시간표가 한쪽은 `1200-1210`=10분, 다른 쪽은 `1200-1300`=60분으로 뒤섞여 저장). 실제 데이터는 **시작시각뿐**. 소요시간은 머리말에 한 번만 표시(`3호점 · TGC · 80분`). 시간표 밖에 잡힌 예약(옛 시간·수동)도 `시간표 밖` 표시로 함께 노출해 누락 없음.
- **사장님 확인 완료(2026-07-15)** — 사자의 서가 80분짜리를 70분 간격으로 받는 것은 **의도된 운영**이 맞음. 시간표는 기존 그대로 유지.
- **검증** — 우리 `THEME_SLOTS` ↔ 기존 사이트 실측을 **4테마 × 7요일 = 28건 전수 대조하여 불일치 0건**.
- 수정: `src/lib/data.ts`, `src/lib/settings.ts`, `src/app/api/admin/settings/route.ts`, `src/app/api/reservations/route.ts`, `src/app/reserve/page.tsx`, `src/app/admin/page.tsx`, `src/app/globals.css`.

## 2026-07-14 — 🎠 테마 섹션을 가로 슬라이드(캐러셀)로 — PC·모바일 동일 구성
> 기존 테마 섹션은 CSS 그리드(모바일에서 카드가 세로로 쌓임). 요청대로 **PC·모바일 모두 좌우 스와이프 슬라이드**로 통일. 세로 포스터 카드 디자인은 그대로.
- **`page.tsx`** — `ThemeCarousel` 컴포넌트 신설(가로 스크롤 레일 + 좌우 화살표). `theme-grid` div → `<ThemeCarousel themes={THEMES}/>`. 화살표는 `scrollLeft`로 현재 위치 감지해 끝이면 자동 숨김.
- **`globals.css`** — `.theme-grid`(그리드) → `.theme-rail`(flex 가로 스크롤 + `scroll-snap-type:x`). 카드폭: PC=4개 한 줄(딱 맞음), 태블릿=2개, 모바일=82%(다음 카드 살짝 보임). 스크롤바 숨김, 호버 리프트 안 잘리게 여백. `.tc-arrow` 화살표(모바일은 스와이프라 숨김).
- 검증: PC(1280) 4개 한 줄·overflow 0(기존과 동일한 모습), 모바일(390) 스와이프 이동 OK, 태블릿(860) 화살표 표시·이동 OK.

## 2026-07-14 — 🌙 모바일 다크모드 배경 검게 뒤집히는 문제 수정 (color-scheme 선언)
> 증상: 폰(삼성 인터넷)이 다크모드일 때 홈 히어로 등 배경이 새까맣게 반전됨. 원인: 사이트는 **밝은 팔레트 전용**인데 `color-scheme`을 선언 안 해서, 삼성 인터넷 다크모드가 "이 사이트는 다크 대응 안 하나 보다" 하고 **자동으로 색을 반전(force-dark)**시킴.
- **`layout.tsx`** — `viewport`에 `colorScheme: "light"` 추가 → `<meta name="color-scheme" content="light">` 렌더.
- **`globals.css`** — `:root`에 `color-scheme: light;` 추가.
- 이 두 선언이 브라우저에 "밝은 테마 전용"임을 알려 강제 반전을 막음(삼성 인터넷·크롬 모바일이 존중). ⚠️ 헤드리스 크롬의 강제다크 플래그는 color-scheme을 무시하고 무조건 반전시켜 자동 재현 불가 → **실제 폰에서 최종 확인 필요**(캐시 때문에 새로고침/재접속 권장).

## 2026-07-14 — 🛠️ 관리자 이식(기존 워드프레스 Booked) 1차: 입금대기 큐 + 매장별·요일별 시간대
> 기존 `fantastrick.co.kr`(워드프레스+Booked 예약 플러그인)의 관리자 기능을 **읽기 전용으로 분석**(원본 무변경)해 우리 Next.js 관리자로 이식하는 작업. 분석 보고서 `docs/관리자_이식_분석_보고서.html`. 그중 P1 두 가지 구현. 빌드·타입 통과, prod DB 왕복 E2E(저장→config 반영→요일계산→원복)까지 PASS.
- **① 입금대기 전용 큐** — 예약 관리 탭에 "💰 입금대기 N건" 버튼(대기&미입금 건이 있을 때만 노란색으로 노출). 누르면 대기+미입금만 필터, 다시 누르면 해제. `api/admin/reservations` GET에 `deposit=unpaid` 필터 + 통계에 `pendingUnpaid` 추가.
- **② 매장별·요일별 예약 시간대** — 기존은 시간대가 전 매장 공통이라 2호점(월~목 부분운영)에도 예약칸이 떴음. 이제 설정 탭에서 매장을 켜면 그 매장 전용 시간표를 쓰고, 요일마다 "기본 사용/휴무/직접 지정" 선택 가능. 설정 없는 매장은 기존 전역 시간대(fallback) 그대로 → **하위호환 유지**.
  - `lib/data.ts`: `StoreSlots` 타입 + `slotsForStoreDate()` 헬퍼 + `DOW_LABELS`·`isSlotTime` 추가.
  - `lib/settings.ts`: `AppConfig.storeSlots`(app_settings의 `store_slots` 키) 추가.
  - `api/admin/settings` PUT: `storeSlots` 저장 + `sanitizeStoreSlots`(알 수 없는 매장·잘못된 시간 자동 제거).
  - `api/reservations` POST: 시간 검증을 `slotsForStoreDate(store, 요일)` 기준으로 변경(서버도 요일별 반영).
  - `app/reserve/page.tsx`: 선택 테마(매장)·날짜(요일)로 `activeSlots` 계산해 렌더, 휴무 요일이면 "예약 안 받음" 안내.
  - 관리자 설정탭: `StoreSlotsEditor`·`SlotChips` 컴포넌트 신설.
- 수정: `src/lib/data.ts`, `src/lib/settings.ts`, `src/app/api/admin/settings/route.ts`, `src/app/api/admin/reservations/route.ts`, `src/app/api/reservations/route.ts`, `src/app/reserve/page.tsx`, `src/app/admin/page.tsx`. 신규 문서: `docs/관리자_이식_분석_보고서.html`.

## 2026-07-13 — 🎨 밝은 팔레트 교체: 웜 아이보리 → 쿨 갤러리(A안)
> 밝은 팔레트 3안 비교(`docs/샘플_라이트팔레트_비교.html`) 중 **A안 "쿨 갤러리"(차가운 흰빛·미술관 모던)** 채택. 브랜드 3색(블루 #043cb2·퍼플 #622698·레드 #b20e19)은 그대로 두고 배경·텍스트 중립톤만 웜→쿨로 교체. 홈 실렌더로 히어로 톤 전환 확인.
- **`globals.css`** — 표면/텍스트 토큰 교체: `--bg` #f4f1ea→#f4f6f9, `--bg2` #eae6dc→#e9ecf1, `--surface2` #f3efe6→#f0f2f6, `--text` #17182a→#131620, `--muted` #565b74→#535a69, `--faint` #7b8199→#8b93a3. 토큰을 우회하던 하드코딩 웜색(그라디언트 표면·히어로 오버레이 `rgba(244,241,234,…)` 등)도 전부 쿨톤으로 일괄 치환.
- **`layout.tsx`** — 브라우저 테마색 `#f4f1ea`→`#f4f6f9`.
- 🔙 되돌리려면 백업 태그 `backup-light-v1-20260712`(웜 아이보리) 참고.

## 2026-07-10 — ✍️ 역할별 폰트 시스템 정리(재설계 X · 폰트 역할만)
> UX 분석·결정 반영: **큰 제목=나눔명조 ExtraBold(세리프)**, 본문·버튼·숫자·라벨·카드제목=Pretendard(산세리프) 유지. 빌드 통과(에러 0)·콘솔 에러 0·데스크톱/모바일/비즈니스 실렌더로 한글 제목이 나눔명조로 보이는 것 확인.
- **무용지물 Gothic A1 제거** (`layout.tsx`) — 기존 `next/font/google`의 Gothic A1은 한글 subset 미노출(라틴만 받는 낭비)이라 삭제. `--font-display` 주입도 CSS 변수로 이전.
- **나눔명조 800 로드** (`layout.tsx`) — Google Fonts `<link>`로 `Nanum+Myeongjo:wght@800`만 로드(preconnect 포함). 한글 글리프는 unicode-range split 서빙이라 CWV 안전. `document.fonts.check` 로 실제 로드 확인.
- **CSS 변수 정의** (`globals.css`) — `:root`에 `--font-display:"Nanum Myeongjo","Pretendard",serif` 추가.
- **적용 맵** — ✅나눔명조: `.hero h1`·`h2.title`·`.reserve h2`·`.biz-hero h1`(큰 제목). 세리프 자간 완화(-.02em/-.01em → -.005em, 삐침 겹침 방지). ❌Pretendard 유지: `.eyebrow`(.3em 트래킹 라벨)·`.rev-summary .score`(숫자)·버튼·카드제목(`.tcard .tt h3` 17px)·배지·칩.
- **숫자 tabular** — `.hero .meta b`·`.biz-hero .meta b`·`.stat b`·`.rev-summary .score`에 `font-feature-settings:"tnum"`(자릿수 정렬).
- 수정 파일: `src/app/layout.tsx`, `src/app/globals.css`.

## 2026-07-10 — 🎨 디자인 업그레이드(재설계 X · 품질만 상향, 12항목)
> 구조·정체성·페이지 구성·URL·콘텐츠는 그대로 두고 "디자인 품질"만 벤치마크급으로 끌어올림. 승인 계획서: `docs/디자인_업그레이드_계획서.html`. 빌드 통과(에러 0)·콘솔 에러 0·데스크톱/모바일 실렌더 확인 완료.
- **① 모바일 햄버거 메뉴 실구현** (`Header.tsx`) — 기존엔 숨겨진 요소로 스크롤해 먹통이었음. `useState` 슬라이드 드로어(오버레이+패널)로 재작성: 메뉴 5개 + 예약/조회 버튼, `aria-expanded`/`aria-controls`, ESC·바깥클릭·링크클릭 시 닫힘, 열릴 때 body 스크롤 잠금 + 첫 항목 포커스. 데스크톱 동작 불변.
- **② 브랜드 컬러 정본화** (`globals.css`) — 공식 3색 반영. 기존 토큰명(`--cyan`/`--cyan-deep`/`--gold`/`--danger`)은 유지(참조 보존)하되 값·의미를 브랜드로 조정: 딥블루 #043CB2(메인), 보라를 Phantom Violet(#622698, 다크 텍스트엔 #8f6ae0), 레드를 Illusion Red(#B20E19, 텍스트엔 #e05561). 시맨틱 토큰 `--brand/--brand-bright/--violet/--blood/--grad` 추가.
- **③ 미정의 토큰 버그 수정 + Primary CTA 생기** — `--amber`·`--green` 실제 정의(관리자 `.stat.amber/.green` 참조 버그 해결). `.btn.primary`를 그라디언트(#3b7bf0→#043cb2)+hover 글로우+active `scale(.98)`로 격상.
- **④ 포커스 링 + 소형 텍스트 대비** — 전역 `:focus-visible` 링, 폼 `outline:none`을 `:focus-visible`로 대체. `--faint` 대비 상향(#6a7298→#828cb0, AA), 터치타깃(.chip/.btn.sm/.menu-btn/.opt) min-height 44px.
- **⑤ 히어로 시네마틱화** (`globals.css`·`page.tsx`) — 초저속 Ken Burns 줌(24s), 스크롤 패럴랙스(JS), 비네트+오로라 글로우, 진입 스태거(eyebrow→h1→sub→cta→meta). `prefers-reduced-motion` 전면 정지.
- **⑥ 디스플레이 폰트 + 타입 스케일 토큰화** (`layout.tsx`) — 제목/eyebrow 전용 디스플레이 폰트를 `next/font/google`(Gothic A1, self-host)로 도입(본문 Pretendard 유지). `--fs-xs~--fs-2xl` 타입스케일 토큰 정의.
- **⑦ 다크 크래프트 폴리시** — 히어로/비즈 배경 Fantasy Blue→Phantom Violet 오로라 글로우, 카드(.tcard/.cap/.ref) 호버 글로우 격상, **전역 필름 그레인 오버레이 1장**(`layout.tsx`의 `.grain`).
- **⑧ 홈 리뷰/평점 섹션 신설** (`page.tsx`) — STORES↔BUSINESS 사이에 별점 요약(4.8/5) + 대표 후기 4건 발췌 카드 + "전체 후기 보기" 링크. 데이터는 정적 대표 샘플(전체·작성은 기존 `/reviews`).
- **⑨ 스무스 스크롤 + reveal 스태거** — CSS `scroll-behavior` 유지 + `.reveal`에 인덱스(`--i`) 기반 `transition-delay` 스태거(비즈 역량카드·리뷰카드·레퍼런스).
- **⑩ 스티키 핀 연출 1곳** — ABOUT 포스터를 데스크톱에서 `position:sticky`로 고정(좌측 텍스트가 흐르는 Apple식). 모바일 폴백=정적.
- **⑪ next/image 전환 + 폼 시맨틱화** — 주요 이미지(히어로 LCP=`priority`, 포스터·지도·레퍼런스 lazy)를 `next/image`로(CLS 제거). 예약 화면 `.opt` div→`<button aria-pressed>`(마감시간 `disabled`).
- **⑫ radius 3단계 통일 + 인라인 색 정리** — `--r-sm/--r/--r-lg/--r-full`로 축소·치환. 소형 인라인 `--faint`→`--muted` 등 주요부 정리.
- 수정 파일: `src/app/globals.css`, `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/reserve/page.tsx`, `src/app/business/page.tsx`, `src/components/Header.tsx`.

## 2026-06-24 (이어서4)
- 🗓️ **관리자 2차** — 캘린더 뷰(월별·날짜별 예약), 시간대 열기/닫기(마감·휴무), 설정(예약금·시간대·노출테마). 설정은 예약 화면에 실시간 반영(마감시간 🚫 표시·예약 차단).
- 💬 **관리자 3차(문자/알림 구조)** — 문자 템플릿(확정/취소/리마인더) 편집·발송내역, 방문 전날 리마인더 자동발송(Vercel Cron 매일). 확정/취소 시 문자 발송 연결. ⚠️실제 발송은 **알리고(ALIGO) 키 등록 시 작동**(미등록 시 발송내역만 '미발송'으로 기록). 새 예약 30초 폴링 알림(#14).
- env 추가: ADMIN_PASSWORD, CRON_SECRET (Vercel 등록 완료). (나중에 ALIGO_API_KEY/USER_ID/SENDER, 토스 결제 키)

## 2026-06-24 (이어서3)
- 🛠️ **관리자 페이지 1차 (`/admin`)** — 비밀번호 로그인 보호. 예약 목록·검색·필터, 상세·메모, 상태변경(대기/확정/취소/노쇼), 입금확인, 확정처리, 취소·환불 보기/완료, 수동 예약 등록, 테마별 인기 통계, 새 예약 폴링 알림. 환경변수 ADMIN_PASSWORD(로컬 .env.local + Vercel). 남은 관리자 기능: 캘린더·시간대관리·설정·문자/알림(2·3차)

## 2026-06-24 (이어서2)
- 🔒 **예약 조회·취소 본인확인 강화** — 전화번호만 → **전화번호 + 예약자 이름** 둘 다 일치해야 조회·취소 가능 (남의 번호만으로 못 보게). 조회/취소 API와 화면에 이름 입력 추가. (더 강한 SMS 인증은 알리고 연동 시 업그레이드 예정)
- 📦 **저장소 독립 + GitHub 자동배포** — fantastrick2026 → 본인 계정 `tndn1102-git/fantastrick-homepage`로 이전. main push 시 Vercel 자동배포(CLI 배포는 신규계정 차단되어 GitHub 연동으로 해결)

## 2026-06-24
- 🔌 **Supabase 연결 완료** — 예약·조회·취소·리뷰 전 과정 실제 저장 동작 확인(E2E PASS). 키는 `.env.local`(깃 제외). ⚠️Supabase 새 키(sb_secret_)는 supabase-js에서 거부됨 → **Legacy service_role 키(eyJ...)** 사용
- 🚀 **Vercel 배포 완료** — 라이브: **https://fantastrick-homepage.vercel.app** (프로덕션에서도 예약·리뷰 작동 확인). Supabase 환경변수 2개는 Vercel에 등록됨. (※ GitHub 자동배포는 아직 미연결 — 현재는 CLI로 수동 배포. 다음에 대시보드에서 Git 연결 예정)
- 🔜 다음: `fantastrick.co.kr` 도메인 연결 → 가비아 호스팅 해지(이메일·tgc 이전 확인 후) → 예약금 결제(토스)·자동문자(알리고)

## 2026-06-23
- 🏗️ **Next.js 앱으로 전환 + 자체 예약/리뷰 시스템 1차 구현** (정적 HTML 프로토타입 → 실제 웹앱)
  - 기술 결정: **Next.js(App Router) + Supabase + Vercel** (사장님 다른 앱들과 동일 스택, 무료). 디자인은 기존 시네마틱 CSS를 그대로 이식해 100% 보존
  - 결정 사항: **고객 구분=전화번호**(회원가입 X), **예약=자체 시스템**
  - 페이지: `/`(메인·테마 캐러셀), `/reserve`(예약), `/reservation`(전화번호로 조회·취소), `/reviews`(후기 목록·작성), `/business`(B2B)
  - API: 예약 생성/조회, 예약 취소, 리뷰 목록/작성 (`src/app/api/...`)
  - DB 스키마: `supabase/schema.sql` (reservations·reviews, 전화번호 기반, 중복슬롯 방지, RLS)
  - 안전장치: Supabase 미연결 시에도 화면은 정상, API는 친절한 안내(503) 반환
  - 예약금 결제(토스)·자동문자(알리고)는 **자리만 마련** — 가입 후 연결 예정(유료)
  - 설정 방법은 `docs/설정_안내.md` 참고
- 📝 **기능 로드맵 문서 신설** → `docs/기능_로드맵.md` — 사용자가 불러준 필요 기능 정리: 예약(자체/외부 결정 필요)·예약금 자동 처리·예약 취소·자동 문자(SMS, 메일 제외)·리뷰 페이지. 결정 대기: 고객 구분을 아이디 vs 전화번호 중 무엇으로 할지

## 2026-06-18
- 🎉 프로젝트 시작! `E:\fantastrick_hompage` 폴더 생성
- `CLAUDE.md`(작업 약속) 작성 — databank 프로젝트의 작업 스타일·규칙을 홈페이지 제작에 맞게 차용
- `README.md`(이 파일), `DIRECTORY_MAP.md`(폴더 지도) 생성
- 📚 운영자료(마스터 기획서·마케팅 보고서·매장 자료·게임 이미지) 분석 → 브랜드 정체성 파악: **강남 11년차 이머시브 방탈출·머더룸 브랜드**, 슬로건 "일상이 멈추고, 이야기가 시작된다", 매장 3곳·테마 4종(태초의 신부/사자의 서/락다운시티/시간의 영속성)
- 🖼️ 실제 게임 이미지 7장을 `assets/images/`로 복사(영문 이름 정리)
- 🔎 경쟁사(키이스케이프·비트포비아/던전·핌아트웍스·셜록홈즈) 홈페이지 구조 리서치 → `docs/홈페이지_설계시안_10.md`
- 🎨 **홈페이지 설계 시안 10가지** 작성 + 시각 미리보기 페이지 제작 → `docs/시안미리보기.html` (크롬으로 확인 완료)
- 🛠️ 화면 확인용 도구 설치: `playwright`(npm, 설치된 크롬 사용) → `package.json`, `node_modules/`
- ✅ **방향 확정: 하이브리드(시안 4+3/10+8)** — 사용자 선택
- 🧩 하이브리드 **고화질 설계 프로토타입** 제작 → `docs/하이브리드_설계.html` (다크 시네마틱, 반응형, 스크롤 등장 애니메이션, 테마 필터). 크롬+모바일 화면 확인 완료
- 🔗 예약 버튼에 실제 주소 연결: 전체예약 `/booking/`, 테마별 `/rooms/firstfoundbride·bookofduat·ldc·time/`. 상단/히어로/플로팅 "예약하기"는 스크롤 없이 예약 페이지로 직행
- 🏢 **비즈니스(B2B) 전용 페이지** 신설 → `docs/비즈니스.html` (역량 3축·서비스·진행 5단계·레퍼런스·문의 폼, 골드 톤). 키이스케이프식으로 메인은 B2B를 티저로만 보여주고 버튼으로 전용 페이지 이동. 메인↔비즈니스 이동 동작 확인
- ✉️ 공식 이메일을 `fantastrick@fantastrick.co.kr`로 통일(메인·비즈니스 양쪽)
- 🗂️ 메인 **테마 섹션을 컴팩트 카드**로 개편(이미지 축소·줄거리 제거, 카드 전체가 예약 링크)
- 🎠 테마를 **가로 캐러셀(슬라이더)**로 전환 — 한 화면에 **정확히 4개**, **마우스 클릭&드래그**·터치 스와이프로 이동, **하단 슬라이드바**(드래그 가능)로 더 있는지 표시(화살표 버튼 제거). 드래그 후 오클릭(이동) 방지
- 🔜 3호점(TGC)에 **coming soon 테마 2개** 추가: `흑백사서 : ?`(테마명 추후 공개), `? ? ?`(미정) — 클릭 불가·"준비중" 표기
- 📝 테마 섹션 카테고리 용어 확정: **"인터랙티브 콘텐츠"**(업계 표준처럼 들리되 우리가 선점하는 용어 전략). 설명문은 경쟁사(키이스케이프 '몰입형 경험 콘텐츠') 템플릿과 겹치지 않게 '객석 vs 무대' 대비 구조로 새로 작성 (레퍼런스 `homepage_ref/플레이어블 스토리에 대한 문장 레퍼런스.png` 참고하되 차별화)
- 📥 `homepage_ref` 폴더에 자료 입수 확인: **공식 로고 3종**(검정/투명파랑/투명흰), **브랜드 컬러 팔레트**, **사업소개서**, 사업자등록증
- 🏷️ **공식 로고 적용**: 흰색 투명 로고를 메인·비즈니스 헤더/푸터에 삽입 → `assets/images/logo-white.png`(+blue/black 백업). 임시 'F' 마크 대체
- 🎨 **공식 브랜드 컬러 파악**(차후 적용 예정): Fantasy Blue `#043CB2`, Midnight Tricks `#13142A`, Engineered Sky `#2D67D8`, Structure Gray `#455071`, Fog White `#DBDFE7`, Illusion Red `#B20E19`, Phantom Violet `#622698` — 현재 시안(cyan/gold)에서 블루 계열로 통일 검토
- 🖼️ **테마별 공식 포스터** 4종 적용 → `assets/images/poster-bride·duat·ldc·time`. 테마 카드 이미지 비율 **1:1 정사각**(정중앙 크롭). 장르 태그(상단 좌)·매장 태그(상단 우)·난이도(자물쇠)·테마명 라벨
- 🛠️ 카드 호버 확대를 `<img> transform:scale` → **`background-image` + `background-size` 확대**로 전환. transform 클리핑 버그(확대 시 이미지가 카드 밖으로 삐져나옴)를 구조적으로 차단
- 🔖 **파비콘** 적용(`assets/images/favicon.png`) — 메인·비즈니스 양쪽 `<head>`에 연결
- 🔒 테마 난이도 표시를 ★ → **자물쇠 아이콘**(SVG)으로 변경 (태초4/사자3/락다운2/시간2, 5점 만점)

- 🎨 **공식 브랜드 컬러 적용**: B2C/메인 = 브랜드 블루(Fantasy Blue `#043CB2`~`#2f74e6`, 로고 색), B2B/비즈니스 = Phantom Violet, 머더룸 = 레드 계열, 배경 미드나잇 블루. (양쪽 페이지 CSS 변수+rgba 일괄 적용)

- 🗺️ 매장 섹션을 **2단 압축 레이아웃**으로 변경: 왼쪽=매장 카드 3개(세로·컴팩트), 오른쪽=지도 이미지(`assets/images/stores-map.png`). 지도 클릭 시 My Maps 뷰어로 연결. 모바일은 1단 스택
- 📍 매장명 라벨 PNG 3종(Pretendard, 다크 블록) 제작해 ref 폴더 저장 → 사용자가 My Maps에 올려 라벨 포함 지도 재캡처 → 고해상도(1592×1231) 이미지로 교체. 중복되던 HTML 라벨 오버레이는 제거
- 🔗 **Git 저장소 초기화 + GitHub 연결**: `.gitignore`(node_modules 등 제외) 작성, 로컬 커밋 후 **비공개(Private) 원격저장소** 생성·푸시 → `github.com/fantastrick2026/fantastrick-homepage` (bwl과 별개 독립 저장소, 기본 브랜치 main). Vercel 배포는 아직 안 함

### 다음 할 일 (제안)
- [ ] 프로토타입 피드백 반영 → 정식 `index.html`(+`business.html`) + `css/` + `js/`로 분리·정교화
- [ ] 예약 방식 결정(자체 예약 vs 네이버/외부 연동)
- [ ] 부족한 테마 대표 이미지(태초의 신부·사자의 서) 확보
